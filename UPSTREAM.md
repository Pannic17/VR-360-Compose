# UPSTREAM.md — 上游（UE 侧）可改造的地方

**上游目前不可修改**（用户 2026-09-09 确认）。所以这份清单**不是计划，也不进 ROADMAP** ——
[ROADMAP.md](ROADMAP.md) 只写我们自己能做完的事。

这里存在的理由只有一个：下面每一条都是**已经量过的**、并且**只能在上游解决**的画质或吞吐上限。
哪天上游能改了，不必重新推导一遍；在那之前，读它的正确方式是
「知道天花板在哪、别在下游白费力气」。

每条都写清三件事：**现状是什么**、**改了能拿到什么**、**不改我们做了什么**。

---

## 1. 那 5 路重复视角（上游省 25% 渲染与落盘；下游省不到）

**现状**：20 个 `Camera*` 目录，但**只有 15 个唯一视角**（AGENTS.md 第 3 节反解结论）。
重复的 5 路是同一个方向渲染了两次 —— 我们靠 `Rig.unique_indices` 只读其中一份，
日志里那句 `reading 15 of 20 files` 就是这件事。

**改了能拿到**：UE 侧渲染时间直接省 **25%**，落盘与占用的磁盘同比例减少。
**下游一分钱也省不到** —— 见下一段。

**不改我们做了什么**：只读 15 份，另外 5 份完全不碰。**下游已经没有余量可挖** ——
重复的那部分我们本来就不读。省下的是上游自己的渲染时间，只有上游能收。

---

## 2. 全 255 的 alpha 通道（省 25% 字节）

**现状**：源 PNG 是 RGBA，而 **alpha 全是 255**（`tools/inspect_data.py` 实测），
也就是四分之一的字节没有携带任何信息。

**改了能拿到**：落盘与读盘字节各省 **25%**。

**不改我们做了什么**：`io.py` 读进来就把 alpha 丢掉，不让它进入后面的任何一步。
代价只剩「多读了 25% 的磁盘」，而实测**解码已经被 warp 完全盖住**
（等解码 0.01 s/帧，AGENTS.md 第 8 节），所以这条对我们的吞吐**几乎没有影响**。
它是上游的存储成本，不是我们的瓶颈。

---

## 3. 8-bit PNG 源（这是画质硬上限，也是唯一真正重要的一条）

**现状**：源是 **8-bit PNG**。所有下游画质工作的天花板由此锁死。

**改了能拿到**：输出 **EXR 或 16-bit**（最好是线性光）之后，

- 母版位深不再是「把 8 bit 抬成 16 bit」而是真有新信息 —— 现在 `--bit-depth 16`
  保住的只是重采样的亚灰阶精度，**不是渲染器的新信息**（AGENTS.md 第 10 节 P4 实测）；
- **线性光混合那两次否决会自动失效**：P4 与 P7 各否决过一次，两次的理由都是
  「重叠 tile 之间只差半个灰阶，平方律下差异可以忽略」＋「编码端要为 8850 万个浮点做幂运算」。
  源是线性数据的话，管线本来就在线性空间里，这个代价直接消失；
- 极区重建的余量变大：极区次轴放大约 16×，8-bit 量化在那里最先见底。

**不改我们做了什么**：P4 把能在 8-bit 内做的都做了 —— 采样器按保真度选（对源 +1.24 dB）、
羽化指数化、16-bit 母版通道留着。**再往上就不是算法问题了。**

---

## 4. 上一代流程会删掉源帧（这条是数据安全，不是画质）

**现状**：`E:\22\Camera*\Tempory\` 是上一代流程的输入队列，**它处理完会删除源帧** ——
帧 1..1655 已经被消耗掉且没有备份，剩下的 778 帧是唯一副本
（AGENTS.md 第 2 节约束 1）。

**改了能拿到**：一个不会自毁的源目录，以及**可以与参考产物逐像素对比的能力** ——
现在做不到，因为参考产物覆盖的帧其源 tile 已经不存在了（第 9 节指标 C 的限制就来自这里）。

**不改我们做了什么**：全程只读，绝不启动上一代流程。
画质结论因此只能用无参考指标和自己两版之间的 A/B 支撑 —— 这一条会一直影响
「我们能对外声称什么」，而不只是影响开发。

---

## 5. 那张孤零零的 `0000` 帧

**现状**：源里除了 `1656..2433`，还有一张 `0000`。它和主序列不连续。

**改了能拿到**：少一个坑。

**不改我们做了什么**：默认帧范围取**最后一段连续帧**而不是 `all`，并在日志里说明跳过了什么
（P5 / GUI 都是这个行为）。用 `all` 会让视频开头跳一帧。

---

## 这份清单不包含什么

- **不包含分发包体积**：用户 2026-09-09 明确「分发包大小不重要」。
- **不包含 8K H.264 的播放兼容性**：用户 2026-09-09 确认**目标头显可以播 8K H.264**。
- **不包含续跑与磁盘余量**：都已由用户降级（中断就重跑；测试盘不算约束）。
  相关代码（分段续跑、磁盘预检）留着，因为它们已经写好且不碍事。
