# ROADMAP.md — VR-Compose

目标：把 UE 渲染的 20 路方形序列帧合成 360° 球形全景视频。
领域事实、坐标约定、实测基线在 [AGENTS.md](AGENTS.md)，本文件只讲**做什么、做到什么程度算完**。

## 当前进度

| | 阶段 | 状态 |
|---|---|---|
| ✅ | **P0** 现状固化 | rig 反解完成，验证工具入库 `tools/` |
| ✅ | **P1** 单帧正确 + 来源目录抽象 | `vr-compose frame` 可用，指标 A PASS |
| ✅ | **P2** 序列吞吐 + 直出 MP4 | `vr-compose sequence`：778 帧 8K 实跑 **2.10 s/帧**（22.3×，~26 min），196.9 Mbps，可续跑，产物合规，186 个测试全绿 |
| ✅ | **P3** 收尾与母版 sink | 四项全部完成：内存报告 + 64 GB 软上限（23.8 GiB，与帧数无关）、取消语义（进程组 + `Cancelled`）、4k/h265/60fps 真实数据 smoke 全部合规、PNG 母版 sink（2.40 s/帧）。228 个测试全绿。**只剩你手动按一次 Ctrl-C 验收** |
| ✅ | **P4** 画质（母版） | 采样器 catmullrom（按**保真度**选，对源还原 +1.24 dB）、羽化指数化（接缝比 0.79 < 1）、时域稳定（凸核证明 + 跳变界）、16-bit 母版；线性光与「极区重加权」两条**验证后否决**，理由都留了数字 |
| ◇ | **可选** GPU 加速 | 未决定。warp 1.84 s → 估计 30–40 ms，778 帧从 ~26 min 到 ~3 min；代价 ~1 GB 依赖、仅 NVIDIA |
| ✅ | **P5** 输出命名与两种输出模式 | 视频 `<stem>_<月日>_<时分>.mp4`；帧模式同名目录 + `<stem>_S_<帧号>.png`，**按 input 密度、无损**（结构性保证）。自动命名不续跑（撞名加 `_2`），要续跑传 `--out` |
| ✅ | **P6** GUI（PySide6） | 参考 VR-Installer 的样式；作业跑在独立子进程，靠 NDJSON 读进度、写一行取消；选项全部来自库，不与 CLI 漂移。进度条在日志上方，带百分比与帧数，准备阶段走不确定态，百分比同时写进窗口标题 |
| ✅ | **P8** 打包 exe | `dist/VR-Compose/` **537.8 MiB**（含 ffmpeg/ffprobe），一条命令可重复，冻结产物与源码运行**逐字节一致**；中文说明随包分发。**干净机器验收待你** |
| | P7 / 运维 | 交付合规 / 运维 |

**能跑的东西**

```bash
python -m vr_compose discover                                   # 检测来源集与 rig
python -m vr_compose --source E:/22 frame --frame 1656 --out pano.png
python -m vr_compose --source E:/22 sequence --frames 1656-2433 --out L_Cathedral.mp4   # 8K h264 高档，可续跑
```

**已验证的数字**（每一个都能一条命令复现，见 [tools/README.md](tools/README.md)）

| 项 | 值 | 出处 |
|---|---|---|
| 重叠一致性（指标 A） | median **0.68** / mean 1.02 / p95 3.10 | `vr-compose frame` |
| 环绕接缝 | **1.10** / 255（参考产物 1.15） | 同上 |
| 单帧 8K 耗时（逐帧算几何） | 16.1 s（上一代 46.85 s） | 同上 |
| **序列 8K 端到端** | **2.17 s/帧**（21.5×）：warp 1.84 + 解码等待 0.01 + 写管道 0.06 | `vr-compose sequence` |
| 球面覆盖 | **100%**，平均 2.50 个 tile/方向 | `tools/coverage_map.py` |
| 交付参数矩阵 | **12/12 组合合规** | `tools/encode_probe.py matrix` |
| 打包体积 / 热启动 | **158 MB / 0.86 s**（onedir） | `tools/package_probe.py` |

**质量门**：`ruff check` + `ruff format --check` + `mypy --strict` + `pytest`，
23 个源文件、113 个测试，全绿。几何与目录发现的测试不依赖真实数据。

---

## 现状一句话

上一代流程用 **46.85 s/帧** 完成了 1655 帧（21.5 小时）。相机装配已经反解清楚：
**15 个唯一视角、共节点、理想针孔、90° 方形 FOV、yaw ∈ {0,72,144,216,288}、elevation ∈ {+45,0,−45}**。
几何是**完全固定**的 —— 不需要标定、不需要特征匹配、不需要光流。
剩下的问题本质上只有两个：**把固定重采样做对**，以及**把 I/O 和编码喂满 32 线程**。

交付侧也已经探明（AGENTS.md 第 5 节全是实测数字）：**8K 编码全长只要 ~10 分钟，不是瓶颈**；
**真正吃画质的是规格强制的 4:2:0 色度下采样（一项 −4.3 dB，占总误差的 46%）**，
而码率从 100 Mbps 提到 200 Mbps 只换来 0.39 dB。

产品形态是 **PySide6 GUI + Windows exe**，**来源目录可选**（不指定就在 exe 同级按目录结构找）。
这两条也都实测过了：onedir 打包 158 MB / 热启动 0.86 s，冻结后的进程池可用；
来源目录发现不依赖任何 `E:\22` 特征（相机数、内层目录名、文件 stem 全靠推断）。

因此路线图不是「先做个能用的拼接器再优化」，而是：
**先把固定几何固化成 LUT（几何正确性一次性解决），
再把 PNG 中间层整个去掉（拼接直接管道进 ffmpeg），
剩下的精力全部投在色彩管线和极区重建上 —— 那才是画质真正的杠杆。**

---

## 里程碑一览

| 阶段 | 交付 | 完成判据 | 预估 |
|---|---|---|---|
| P0 | rig 反解 + 验证工具 | ✅ 已完成（结论写入 AGENTS.md，脚本在 `tools/`） | — |
| P1 | 单帧正确 + 来源目录抽象 | ✅ **已完成** —— median 0.68，113 个测试全过，无硬编码路径 | — |
| P2 | 序列吞吐 + 直出 MP4 | ✅ **已完成** —— 8K 2.17 s/帧（21.5×），120 帧实测合规，续跑经测试 | — |
| P3 | 收尾与母版 sink | ✅ **已完成** —— 内存 + 软上限、取消语义、三组 smoke 全合规、PNG 母版 sink | — |
| **可选** | GPU 加速（CuPy） | warp 1.84 s → 估计 30–40 ms；CPU 路径保留为逐字节基准；不在关键路径上 | 2–3 天，**未决定** |
| P4 | 画质（母版） | ✅ **已完成** —— 采样器按保真度选定、无可见接缝、时域稳定、16-bit 母版 | — |
| P5 | 输出命名与两种输出模式 | ✅ **已完成** —— 命名、帧模式的无损与原生尺寸都是结构性保证 | — |
| P6 | GUI（PySide6） | ✅ **已完成** —— 子进程 + NDJSON 进度、可取消可续跑、薄壳无第二份逻辑（只剩 778 帧全长验收） | — |
| P7 | 交付合规 | 24 种参数组合全部合规；球面元数据；色彩管线实验有结论 | 3–4 天 |
| P8 | 打包 exe | ✅ **已完成** —— onedir 537.8 MiB，一条命令可重复，冻结产物与源码逐字节一致（干净机器验收待用户） | — |
| 运维 | 磁盘/完整性守护，UE 侧上游改造 | 持续 |

> **GUI（P6）排在交付合规之前**，用户 2026-09-08 决定把这两个阶段对调。
> 原来的理由是「交付合规现在就能做完（源数据在手），而且 GUI 要暴露的参数得先确定下来」——
> 那个理由现在弱了很多：P0–P5 已经把 GUI 要暴露的绝大部分参数定死了
> （尺寸、编解码、码率档、帧率、采样器、羽化指数、位深、输出模式与命名）。
>
> **仍然存在的一点代价，如实记下来**：P7 有可能再加参数 ——
> 色彩范围 limited/full 还没确认（第 11 节第 1 项），球面元数据也可能带出开关。
> 所以 GUI 先做的话，P7 之后可能要给它补一两个控件。这是个小代价，不是返工。

---

## P0 — 现状固化（已完成）

做了什么：枚举数据、反解 rig、量化覆盖与采样密度、建立验证指标、测出性能基线。

关键产出（细节见 AGENTS.md）：

- **15 个唯一视角**，序号 → 视角映射表已确定。
- **5 组文件逐像素重复**（`Camera4≡5, 8≡9, 12≡13, 16≡17, 20≡1`）→ 每帧少读 25%。
- 球面覆盖 100%，任意方向 ≥2 个 tile，加权平均 2.50。
- 输出分辨率 7680 与 tile 中心密度 **精确匹配**（21.33 px/度）。
- 极区最好 tile 的相对采样密度只有 **0.355**，是画质软肋。
- 上一代流程比 I/O 下界慢 **15–25×**。

> **P0 的脚本已经入库到 [`tools/`](tools/README.md)**，五个：`inspect_data.py`、
> `fit_rig.py`、`coverage_map.py`、`stitch_probe.py`、`encode_probe.py`。
> 上面每一个数字都能一条命令复现，并且它们就是回归基线。
> `ruff` / `mypy --strict` 已覆盖 `tools/`。

---

## ~~P0.5 — 对照基准集~~（已否决）

原计划：先备份 10 帧源 tile，再让上一代流程处理那 10 帧，从而得到与参考产物**逐像素可比**的一对。
**用户已明确否决** —— 不启动上一代流程，也不做备份。

后果要如实记录：**「超过参考产物」无法用逐像素 diff 证明。**
改为无参考指标 + 相邻帧定性 A/B + 自己两个版本之间的 A/B 量化，
方法与限制见 AGENTS.md 第 9 节指标 C。汇报时不要把无参考指标包装成对参考产物的胜出。

---

## P1 — 单帧正确性（已完成）

几何一次性解决了。`vr-compose frame` 能拼出单帧并自己判定几何是否正确。

**交付的模块**

| 模块 | 职责 |
|---|---|
| `vr_compose.projection` | equirect ↔ 针孔的双向映射、旋转、可见性。纯数学，零 I/O |
| `vr_compose.rig` | `View` / `Rig` / `REGISTRY`。20 路装配是**数据**，未登记布局明确拒绝 |
| `vr_compose.source` | 按目录结构发现来源集，不合规的地方逐条报出来 |
| `vr_compose.stitch` | 按横向条带重投影 + 羽化混合，同时累计指标 A 需要的统计量 |
| `vr_compose.verify` | 指标 A（重叠一致性）与环绕接缝检查，带门限和基线 |
| `vr_compose.io` | 读 tile（丢掉无用的 alpha）、写 RGB 全景 |
| `vr_compose.cli` | `discover` / `frame` 两个子命令 |

**完成判据的实际结果**

- 指标 A：`median 0.68 / mean 1.02 / p95 3.10 / std>8 0.25%`，**PASS**（门限 median ≤ 1.0）。
- 环绕接缝：1.10–1.61 / 255（参考产物是 1.15）。
- **113 个测试全过**，`ruff` + `ruff format` + `mypy --strict` 全绿（23 个文件）。
  几何、rig、目录发现的测试全部用合成数据，不依赖 `E:/22`；
  真实数据的检查单独放在 `tests/test_reference_data.py`，标了
  `needs_reference_data`，数据不在时自动跳过。
- 产品代码里没有任何硬编码来源路径（唯一一处 `E:/22` 是 `rig.py` 里说明 rig 来源的 docstring）。
- 输出是 RGB，不带 alpha。

**过程中修掉的两个真问题**

1. **量化是截断而不是四舍五入。** `astype(np.uint8)` 向零取整，所以一个恒定输入
   在加权平均后落到 29.9999 就会输出 29 —— 平坦区域会出现 banding，
   而且平均量化误差翻倍。改成 `np.rint`。
   这是「常量球面必须原样输出」这条测试抓出来的。
2. **rig 的重复组原先是硬编码的。** 现在从 `views` 派生：
   相同朝向的文件自动归为一组。派生结果与实测的 5 组重复完全一致，
   而且换装配时不会失效。

**与原计划的两处偏离（有意的）**

- **静态 LUT 推到 P2。** 原计划 P1 就建 LUT，但那是性能优化 ——
  先让直接计算的路径正确，LUT 在 P2 引入时必须产出**逐字节相同**的结果。
  现在 8K 单帧 16.1 s，够单帧用，不够序列用，正好是 P2 的起点。
- **按条带处理代替了一次性全图。** 8K 的方向数组单独就是 708 MB（float64），
  按条带算之后峰值内存只跟条带高度有关（`band_rows=512` 时约 94 MB），
  与输出尺寸无关。测试断言了**任意条带高度输出逐字节一致**。

---

## P2 — 序列吞吐 + 直出 MP4（已完成）

**实际结果**（8K h264 高档，同机）：**778 帧实跑 2.10 s/帧端到端，22.3× 于上一代，约 26 分钟**；
全长平均码率 196.9 Mbps（≤ 200 上限）；分解为等解码 0.01 s / warp 1.90 s / 几何门 0.01 s /
写管道 0.06 s；产物 `ffprobe` 逐项合规（High L6.0 avc1 yuv420p B=0 I-gap=60），
8 个采样帧几何门全部 PASS。

交付的模块：`vr_compose.warp`（静态 LUT，与 P1 逐字节一致，任意线程数）、
`vr_compose.encode`（ffmpeg 定位、按规范表算 level/tier、`SegmentWriter`、`probe`、`concat`）、
`vr_compose.pipeline`（解码预取 → warp → 分段编码 → concat，可续跑、几何门抽检、磁盘预检、
`stages` 分解计时）、`vr-compose sequence` 子命令。186 个测试全绿。

与原计划的三处偏离（都是有意的）：

1. **解码用线程池而非进程池。** Pillow 解码时释放 GIL，4 线程就完全藏在 warp 后面，
   还省掉跨进程搬 166 MB/帧。同时发现解码线程开太多会拖慢 warp（GIL 争用），
   所以默认是解码 4 线程 + warp 8 线程，见 AGENTS.md 第 8 节。
2. **「续跑逐字节一致」按编码器区分**（AGENTS.md 第 5 节「编码器的确定性」）：默认模式保证
   结构一致；x264 长分段通常逐字节一致但短尾段不保证；`--deterministic` 两种编码都逐字节
   一致，代价是编码器慢 6.6–7.7×。
3. **LUT 落盘**（`WarpPlan.save/load`）实现了但 CLI 未暴露 —— 8K 构建只要 9.7 s，
   每次启动重建即可，不值得多一个缓存目录要管。

过程中修掉的问题：亮度走 BLAS 点积导致跨批次末位不一致（改逐元素）；MP4 容器时间戳让
哈希不稳（`+bitexact`）；`.mp4.part` 让 ffmpeg 猜不出封装（显式 `-f mp4`）；
`select_level` 的 tier 遍历顺序与文档矛盾（改为先留在分辨率所在的等级家族）。

**P2 收尾清单**（核心已完成；以下是 120 帧实测之外、完成判据还没兑现或新冒出来的项）：

| | 项 | 状态 |
|---|---|---|
| ✅ | 778 帧一次跑完（≤ 45 min、码率 ≤ 上限、产物合规） | **达标**：~26 min，2.10 s/帧（22.3×），全长平均 **196.9 Mbps ≤ 200**，`conformance: OK`，8 个采样帧几何门 PASS。120 帧时的 249.8 Mbps 确为短片段 VBV 超调 |
| ✅ | ~~内存峰值 ≤ 8 GB~~ → 64 GB 软上限 | **判据已作废并在 P3 第 0 项做完**（用户 2026-09-08 决定）：不设内存预算，**软上限 64 GB，超出只报 Warning，不报错、不中断**。整机峰值实测 **23.8 GiB commit**（拼接 3.7 + 编码 20.1），60 帧与 180 帧完全相同 —— 与总帧数无关 |
| ✅ | 默认输出目录 = 程序所在目录 | `--out` 可省略，落在程序目录，文件名自动生成；测试覆盖 |
| ✅ | 提交 | `c2c508d`（P2 全部代码与文档） |

取消语义、真实数据 smoke、PNG/EXR 母版 sink 三项按用户决定移到 **P3**，内存一项同上。

原计划保留如下，作为记录。

目标：**≤ 3 s/帧**（比上一代快 ≥15×），剩余 778 帧从 ~10 小时压到 ~40 分钟，
并且**直接产出一个能播的 MP4**，不落 PNG 中间层。拉伸目标 ≤ 1 s/帧。

**做什么**

1. **跳过重复文件**：只读 15 路，`Camera4/8/12/16/20` 不碰。
   省 25% 读 I/O（每帧 91 MiB → 68 MiB）。
2. **去掉 PNG 中间层**：sink 做成可插拔 ——
   - `--out mp4`（默认交付路径）：float32 → uint8 RGB → rawvideo 管道给 ffmpeg，**一次落盘**；
   - `--out png` / `--out exr`：无损母版，仅 QA 抽帧与归档时用，不是必经环节。

   这一步直接消掉 0.68–2.77 s/帧的 PNG 编码 —— 它是 P0 识别出的瓶颈。
   顺带每只眼省掉 64 GiB 落盘。
3. **三段流水线**：解码（进程池，PNG 解码是 CPU-bound 且在 GIL 内）→ warp+blend → 送入 ffmpeg。
   队列有界；不同帧的三段并行，用满 32 线程。ffmpeg 单进程串行吃 rawvideo，
   所以**上游必须稳定快于 4 fps**（x265 8K 实测 3.76 fps），否则编码器空转。
4. **warp 内核**：LUT gather 是访存密集型。先 numpy/numba，不够再上 GPU
   （4090 上这一步应 <50 ms/帧）。**如果 CPU 版已经让 ffmpeg 成为瓶颈，就不要引 GPU 依赖。**
5. **作业调度器**：`vr-compose sequence --frames 1656-2433 --fps 30 --codec h265 --bitrate 200k --resume`。
   - MP4 是单一输出文件，**没法靠「文件已存在」做续跑**。方案：按 GOP 对齐切分成分段
     （每段整数个 2 秒 GOP），分段落盘 + 幂等跳过，最后用 `-c copy` concat 拼接。
     GOP 闭合、B=0 正是让无损 concat 成立的前提 —— 规格在这里帮了我们。
   - 进度日志带帧号、`s/帧`、ETA，直接和 46.85 s/帧 对比。
6. **写盘前预检空余空间**，不足就拒绝启动，而不是跑到一半写坏。

**完成判据**

- 778 帧（1656..2433）一次跑完，产出一个可播的 MP4。
- 平均 ≤ 3 s/帧，端到端 ≤ 45 分钟。
- `ffprobe` 校验：`avc1`/`hvc1`、profile、`yuv420p`、**B 帧数 = 0**、I 帧间隔严格 = 60、
  实际码率 ≤ 上限。
- 中途 Ctrl-C 再启动只补缺段，最终文件与不中断的**逐字节一致**。
- 抽 5 帧过指标 A（重叠一致性）。
- 内存峰值 ≤ 8 GB（与总帧数无关）。

---

## 可选 — GPU 加速（不在关键路径上，未决定）

**为什么是可选**：ROADMAP 的原则是「CPU 到不了目标才引 GPU」，而 CPU 版已经到了
（8K 2.0 s/帧 < 3 s 目标）。GPU 买到的是**体验**：778 帧从 ~26 分钟变 ~3 分钟，
对反复迭代画质（P4）的人差别很大，但不是交付的必要条件。

**为什么可行**：warp 是一张 843 MB 的静态查找表 + 每帧一次 gather/加权累加，
是 GPU 最擅长的形状。本机有 RTX 4090（24 GB），当前空闲。

| | 现状（CPU，8 线程 numpy） | GPU 估计 |
|---|---|---|
| warp | **1.84 s** | 30–40 ms（上传 166 MB tile ~10–15 ms + 核函数几 ms + 下载 88 MB ~5–8 ms） |
| 之后的瓶颈 | — | x264 软编 ~0.25 s/帧；换 NVENC HEVC 后是 PNG 解码 ~0.1 s |
| 778 帧 | ~26 min | **~3.5 min**（x264）/ **~2 min**（NVENC HEVC） |

**这些是估算，不是实测。** 引入前先用 CuPy 在本机跑一次 8K warp 拿实测毫秒数（约 15 分钟工作量）。

**代价（决策依据）**：

1. **依赖体积**：CuPy + CUDA 运行时约 1 GB，分发包从 528 MB 变 ~1.5 GB。做成 `[gpu]` extra，不进默认安装。
2. **仅 NVIDIA**：CPU 路径必须保留为回退，**并且继续是逐字节一致的基准实现**（AGENTS.md 第 9 节指标 D）。
3. **数值一致性**：GPU 编译器默认把乘加融合成 FMA，舍入与 CPU 不同。要么关掉融合
   （`-fmad=false` / 逐操作内建函数）保住逐字节一致，要么**明确**改成 ±1 灰阶容差并重记基线。不能糊过去。
4. **打包**：PyInstaller 有 CuPy hook，但要在没有 NVIDIA 的干净机器上验证回退路径能起来。
5. **H.264 8K 不能走 NVENC**（硬件上限 4096×4096，已实测）；`hevc_nvenc` 8K 可以，但必须
   `-tier high -level 6.2`，且不可复现（AGENTS.md 第 5 节）。

**中间选项**：numba（~100 MB，纯 CPU）把 warp 的 4 遍内存访问融合成 1 遍，估计 0.6–0.9 s/帧，
778 帧 ~10 分钟。不需要显卡，体积小得多。同样要过指标 D。

**完成判据（如果做）**：GPU 与 CPU 路径在合成数据与真实帧上逐字节一致（或按上面第 3 条显式重基线）；
无 NVIDIA 时自动回退且提示；分发包在无 NVIDIA 机器上可用；8K 实测 s/帧写进 AGENTS.md 第 8 节。

---

## P3 — 收尾与母版 sink

从 P2 收尾清单移来的四项（三项按用户决定，内存一项判据改写后并入），加上原计划里的母版输出。

下面每张表都是 2026-09-08 在本机实测的，**复现脚本是本阶段的交付物之一**
（按第 10 节约定，数字落进 AGENTS.md 时必须同时有一条命令能跑出来）。

### 0. 编码器内存与 64 GB 软上限 ✅

**判据改了**：不再要求 ≤ 8 GB。用户决定 **软上限 64 GB，超出只报 Warning，不报错也不中断**。

做完的：

- `vr_compose.memory` —— 取进程的 commit 与 resident 高水位，跨 ffmpeg 与自己两侧；
  `PeakMemory` 报的是两侧峰值之和（真实同时峰值的**上界**，告警宁可偏高）。
  `vr-compose sequence` 每次跑完打印一行 `memory :`，超限追加一条 `WARNING :`，**继续跑**。
- `tools/encode_probe.py memory` —— 复现下表；x264 与 x265 都量了。数字见 AGENTS.md 第 5 节。
- `--encoder-threads N` —— x264 走 `sliced-threads=1:threads=N`，x265 走 `pools=N:frame-threads=2`。
  **默认不设**，因为 `threads=auto` 既是最快的一档、21 GiB commit 也远在软上限内。

**度量本身是这一项最大的收获**：比较配置必须看 **commit**，不能看 resident ——
同一条 8K 命令的 resident 实测在 **6.5 / 8.6 / 14.8 GiB** 之间跳（Windows 在缓存压力下修剪工作集），
拿它比会得出相反结论；commit 稳定，而且它才是「机器会不会 OOM」的那个量。
（P2 收尾清单里记的「16 GB」就是一次 resident 读数。）

| 8K h264 | commit | resident | 喂入速率 |
|---|---|---|---|
| 默认 `threads=auto`（本机 48） | **21.01 GiB** | 5.99 GiB | 7.32 fps |
| `threads=16` | 11.11 GiB | 5.63 GiB | 4.92 fps |
| `sliced-threads=1:threads=16` | **7.93 GiB** | 5.75 GiB | 5.06 fps |
| x265 默认 `pools=auto` | 8.29 GiB | 7.22 GiB | 4.94 fps |
| x265 `pools=8:frame-threads=2` | **6.49 GiB** | 5.76 GiB | 2.58 fps |

吃内存的是**帧级并行**（每个帧线程持有自己的 8K 参考帧与插值平面），x264 的 commit 就是按帧线程数走的；
切成片级并行后与线程数解耦。管线只需要 0.48 fps，上表最慢的一档也有 4 倍余量。
`-filter_threads` 实测无效（swscale 不是贡献者）。`sliced-threads` **不能**替代 `--deterministic`
（跑两遍哈希不一致，已排除）。

**整机峰值与总帧数无关（实测）**：8K h264 跑 60 帧与 180 帧，
两次都是 **23.8 GiB commit（拼接 3.7 + 编码 20.1）、10.5 GiB resident**，
**帧数翻 3 倍一位小数都没动**。相对 64 GB 有 2.7 倍余量。

同一个 180 帧作业加 `--encoder-threads 16`：峰值 **9.8 GiB（−59%）**，
吞吐只掉 **4%**（2.24 → 2.33 s/帧），产物照样 `conformance: OK`。
所以那个旋钮是真的可用，只是默认不需要。

### 1. 取消语义 ✅

原来的测试是绿的，但它在 progress 回调里抛 Python 异常，测不到真实 Ctrl-C 的形状：
Windows 的 Ctrl-C 发给控制台里**每一个**进程，ffmpeg 自己处理 SIGINT，所以真实次序是
**编码器先死**，管线随后撞上断管，报 `ffmpeg exited early`（消息还是空的）——
**取消与真故障在退出码和消息上完全无法区分**。磁盘状态一直是对的，错的是分类。

做完的三处（细节与实测见 AGENTS.md 第 8 节末）：

1. **ffmpeg 进自己的进程组**（`encode.child_creation_flags()`）—— Ctrl-C 只到 Python。
2. **`pipeline.Cancelled` + `_CancelScope`** —— SIGINT 变成标志，帧循环在**帧边界**检查，
   不会有半帧进编码器；取消最多晚一帧生效（8K 约 2 s），再按一次 Ctrl-C 立刻硬中断。
   GUI（P6）与测试走 `run_sequence(..., cancel=threading.Event())`，不碰信号。
   CLI 把它映射到退出码 130 —— 注意 `Cancelled` 是 `RuntimeError` 的子类，
   except 分支的**顺序是有意义的**（低一档就会变成 SystemExit(1)，也就是崩溃），有测试钉住。
3. **`concat` 清自己的 `.part`** —— 中断落在合并阶段时输出旁边不留残留。

**新写的测试抓到一个真 bug**：Windows 上被 kill 的 ffmpeg，往它 stdin 写数据抛的是
`OSError: [Errno 22] Invalid argument`，**不是** `BrokenPipeError`。原来只接后者，
所以真的编码器死亡会以裸 `OSError` 冒出去，CLI 接不住，用户看到 traceback。
现在接 `OSError` 全族，并把退出码与 stderr 一起放进消息。
`SegmentWriter` 的 stderr 也改成常驻 drain 线程（原来只在结束时读，
ffmpeg 输出超过 64 KiB 管道缓冲就会双向死锁）。

**还差**：真实 8K 任务上手动按一次 Ctrl-C。键盘事件没法在非交互会话里合成，这条留给你验收：

```bash
python -m vr_compose --source E:/22 sequence --frames 1656-1835 --segment-gops 1
```

跑起来后按 Ctrl-C，应当看到 `interrupted (cancelled after N of 180 frame(s)); finished
segments are kept.`、退出码 130、`.segments` 目录里只有完成的整段且没有 `.part`；
重跑同一条命令只补缺段。

### 2. 真实数据 smoke（4k / h265 / 60fps）✅

**帧数问题（已修）**：`StreamInfo.i_intervals` 是相邻 I 帧位置的差集，30 帧 @30fps 只有 1 个 I 帧
→ 空集合 → `conformance_problems` 里 `any(())` 是 `False`，**GOP 检查空过**。
这条在 P3 的一次 60 帧实跑上原样复现了（`I-gap=[]` 却 `conformance: OK`）。
改成 **130 帧 @30fps / 250 帧 @60fps，配 `--segment-gops 1`**，三段、两个 I 间隔，GOP 与 concat 都真的被验到。

三组全部通过（真实数据 E:/22）：

| 配置 | 帧数 | 产物 | s/帧 | 内存峰值 |
|---|---|---|---|---|
| **4K** h264 高档 | 130 | `h264 High L5.1 avc1 4096x2048 yuv420p B=0 I-gap=[60]` | 2.16 | — |
| **h265** 8K 高档 | 130 | `hevc Main L6.2 hvc1 7680x3840 yuv420p B=0 I-gap=[60]` | 2.33 | 9.8 GiB（编码 6.1） |
| **60 fps** 8K h264 | 250 | `h264 High L6.1 avc1 7680x3840 yuv420p B=0 I-gap=[120]` | 2.11 | 26.6 GiB（编码 22.8） |

全部 `conformance: OK`，几何门 PASS（最差 median 0.75）。等级与 AGENTS.md 第 5 节逐项一致
（4K 30fps → 5.1、8K h265 → 6.2 Main、8K 60fps → 6.1）。
**x265 比 x264 省内存**（编码侧 6.1 对 20.1 GiB），60 fps 因为 GOP 翻倍反而更吃内存（22.8 GiB）。

短片段码率照例超档位（4K 69.7 / h265 216.2 / 60fps 245.8 Mbps），是 2 秒 VBV 在短片段上的超调，
不是合规问题 —— 全长 778 帧那次是 196.9 ≤ 200。等级本身也都容得下这些瞬时码率。

**4K 走哪条路 —— 已定案并实现：按母版拼接，编码时 Lanczos 重采样**

ROADMAP 原文写「4K 走另一张 LUT（4096×2048）」，AGENTS.md 第 5 节写「4k 由 8k 母版 Lanczos 降采样得到」。
两者矛盾，且不是接线方式之差：第 3 节实测 7680 **精确等于** tile 中心密度（21.33 px/度），
4096 是 11.38 px/度，直接出 4K LUT 等于用**最近邻**在 1.875 倍过采样的源上点采样。

真实帧 1656/1657 同时出两版（`A` = 直接 4096×2048 LUT，`B` = 8K 母版经 ffmpeg
`scale=4096:2048:flags=lanczos`，即生产路径）：

| 量 | 结果 | 说明 |
|---|---|---|
| PSNR(A, B) | **43.89 dB**（赤道 42.45 / 中纬 43.83 / 极区 48.00） | 这个选择的影响量级，与 4:2:0 那 4.3 dB 同级，远大于码率 100→200 Mbps 的 0.39 dB |
| 升回 8K 后对母版 M 的 PSNR | 直接 41.84 dB / **Lanczos 44.36 dB**（**+2.52 dB**） | 各纬度带一致（+2.34 ~ +2.65 dB）。点采样丢掉的信息不可逆，任何升采样都补不回来 |
| 亮度 mean \|Laplacian\| | 直接 4.44 / Lanczos 3.38（**直接多 31.3%**） | 折叠进通带的高频。**注意方向**：直接版高频多 31%，却把母版重建得差 2.5 dB —— 那 31% 不是细节，是走样 |
| 相邻帧 mean 差（同样的场景运动） | 直接 2.919 / Lanczos 2.778（**直接多 5.1%**） | 多出来的是采样爬行，头显里就是闪烁 |
| 直接 4K 的指标 A | median **0.67 / 0.68，门 PASS** | **几何门对走样完全无感** —— 它拿多个 tile 在同一采样点互比，走样在 tile 之间是相关的 |

> 后两项（Laplacian、时域）不依赖 `B` 作参考，所以「用同族 Lanczos 升采样略微偏袒 B」这个疑虑
> 不影响结论方向：四个量一致指向同一边。

→ **已采纳并实现**（用户 2026-09-08 决定「采用你认为最好的」）：

- **母版尺寸 = `Rig.native_width(tile_size)` = `tile_size × 360 / fov_deg`**（本 rig 即 4× tile），
  跟着源走；**交付尺寸跟着规格走**。两者相等时（1920 的 tile 交 8K）什么都不重采样，
  与 P2 验证过的产物逐字节相同；不等时由 swscale 在**同一个 scale 滤镜**里做
  Lanczos + RGB→YUV + 4:2:0（分两个 scale 会重采样两次；放在 `format=yuv420p` 之后
  会先把色度抽掉一半再平均）。母版**不落盘**。
- `EncodeSpec.master` 是新增的那一个字段，`width/height` 继续是交付尺寸（等级、合规都按它算）。
  warp / projection / verify **一行没动** —— 指标 D 与 P1 的几何仍然是基准。
- `--stitch-at {native,delivery}`，默认 `native`；`delivery` 是直接 warp 到交付尺寸的快速预览档。
- 代价：4K 按 8K 的价钱渲染（LUT 20.9 M 条 → 73.6 M 条），778 帧约 28 min 对约 15 min。
- **顺带解决「16K 渲染、8K 交付」**（用户提出的将来场景）：3840 的 tile → 母版 15360×7680，
  交付 7680×3840，同一条代码路径，不需要新概念。**16K 只能是母版** ——
  H.264/HEVC 都没有等级容得下 15360×7680，`select_level` 会拒绝把它当交付尺寸。
  代价在 warp：LUT 约 295 M 条、约 3.4 GiB，单帧约 7.6 s（8K 是 1.9 s），
  778 帧约 1.6 小时，整机峰值估计约 27 GiB —— 都在 64 GB 软上限内，
  但这是 GPU 加速真正开始值钱的地方。
- **4K 这组 smoke 不能只看 `conformance: OK` + 门 PASS**（门是瞎的），要带上上面这组对比。

顺手验过 `select_level` 与 AGENTS.md 第 5 节逐项对齐（4K h264 30fps → 5.1、60fps → 5.2；
4K h265 57/43 → 5.2 Main、28 → 5.1 Main；8K h265 → 6.2 Main），这部分不用动。

h265 那组走默认模式：`--deterministic` 慢 7.7×，不适合当 smoke。


### 3. PNG / EXR 母版 sink ✅

`vr-compose sequence --out-format png`：一帧一个无损 PNG，按源的原生密度，`--out` 是目录。

- **两个 sink 共用一套核心**：`_prefetch_tiles`（解码预取）与 `_stitch_one`（warp + 几何门）
  被两条路径共同调用，所以几何、预取、抽检是同一份代码。
  MP4 路径的循环只是改成消费这两个 helper，**逐字节行为不变**（续跑逐字节一致的那批测试原样通过）。
- **按文件存在续跑，而且在解码之前就判断** —— `pending_frames()` 先过滤帧列表，
  所以续跑不会去解码 + warp 一个马上要丢掉的帧。
- **写 `.part` 再原子改名**，半个 PNG 不可能被当成写完的（有测试往目录里放一个假 `.part` 验证）。
- **取消时不丢已经拼好的帧**：还在飞的写任务是「已经 warp 完」的帧，让它们落地而不是取消，
  续跑就不用重算 warp。
- 磁盘预检独立于码率：按 **1.6 字节/像素**估（实测 1.41，留余量），778 帧 8K ≈ 30 GiB。
- `--out-format png` 时 `--codec/--bitrate/--fps/--size/--segment-gops/--deterministic/--encoder-threads/--keep-segments/--stitch-at`
  一律**报错**而不是忽略；反过来 `--compress-level/--write-workers` 在 mp4 下也报错。
- **EXR 只留接口**：`--out-format exr` 给一条说明为什么空着的错误（源是 8-bit PNG，
  EXR 装不进更多信息；等第 11 节第 5 项的上游改造）。没引 OpenEXR 依赖。

**实测（8K，10 帧真实数据）**：**2.40 s/帧**、**39.6 MiB/帧**、内存 3.7 GiB（没有编码器那份）。
`--write-workers 1` 比 2 快（2.40 对 2.52 s/帧）—— 第二个写线程把「阻塞在写」从 0.12 s 压到 0.02 s，
代价是把 warp 从 2.06 拖到 2.28，**总线程数比单项各多少更重要**，和第 8 节解码池那条同一个道理。

**顺手抓到一个真 bug**：Pillow 按文件扩展名猜格式，所以写 `<name>.png.part` 会抛
`unknown file extension: .part`。`write_png` 现在显式传 `format="PNG"` —— 它是 PNG writer，
本来就不该依赖文件叫什么。
### 顺序与工期

按计划的顺序做完了：编码器内存 → 取消语义 → smoke（含 4K 路径落地）→ PNG sink。
把内存排在最前面是对的 —— smoke 的合规结论必须用最终编码参数得出，否则要重跑。

**完成判据（逐条兑现情况）**

| | 判据 | 结果 |
|---|---|---|
| ✅ | `tools/encode_probe.py memory` 可复现；整机峰值有实测数字且与总帧数无关；超 64 GB 只 Warning；x264/x265 都量过 | 23.8 GiB，60 帧与 180 帧完全相同；告警分支有测试（软上限设成 1 字节仍产出合规文件） |
| ✅ | 编码器死亡 → 走失败路径而非取消；无 `.part`；完成分段保留；续跑跑完 | 有测试；并因此抓到 `OSError` 分类那个 bug |
| ✅ | 三组 smoke 全部 `conformance: OK`，帧数足够让 GOP 检查非空过 | 4k / h265 / 60fps 三组齐过，I-gap 分别 60 / 60 / 120 |
| ✅ | 4K 路径按实测定案，母版尺寸与交付尺寸在代码里分开 | `EncodeSpec.master` + `Rig.native_width`，顺带覆盖 16K→8K |
| ✅ | PNG 母版 sink：像素与 `frame` 恒等、同 compress_level 下字节恒等、按文件续跑、空间不足拒绝启动 | 全部有测试；8K 实测 2.40 s/帧 |
| ✅ | `ruff check` + `ruff format --check` + `mypy --strict` + `pytest` 全绿 | 228 个测试 |
| ▶ | **真实 8K 任务上手动 Ctrl-C 一次** | **留给用户验收** —— 键盘事件没法在非交互会话里合成，命令见第 1 项末 |

---

## P4 — 画质（母版层）

这一阶段只管**母版**，不碰编码。理由：AGENTS.md 第 5 节实测显示编码量化几乎是透明的
（4:4:4 能到 47.5 dB），所以母版的每一点改善都能穿透到成品；反过来，
在母版还糊的时候去调码率是白费力气。

**做什么**

1. **重采样滤波** ✅ **已完成，但设计与原计划相反 —— 原计划是错的。**

   原文要「按局部缩放比选 mip 层级，或 EWA」。先测了映射的雅可比
   （`tools/resample_probe.py`，数字在 AGENTS.md 第 3 节），结论是：
   **原生密度下映射几乎处处在放大** —— 次轴在每个纬度带都 < 1（赤道中位数 0.82、极冠 0.06），
   两轴同时缩小的方向只占 **0.1%**。放大时没有东西可以预滤波，mip/EWA 只会把画面变糊。
   最近邻在这里的真实代价是**最多半像素的位置误差**（块状复制），要的是**插值**。

   所以做的是 `--sampler {nearest,bilinear,catmullrom}`，**默认 catmullrom**
   （用户 2026-09-08 要求「尽可能还原单帧」），`nearest` 保留为 P1 的逐字节基准和预览档。

   | 指标 | nearest | bilinear | **catmullrom** |
   |---|---|---|---|
   | **对源的还原 PSNR** | 47.85 dB | 47.66 dB | **48.90 dB** |
   | 指标 A median | 0.68 | **0.48** | 0.52 |
   | std>8 占比 | 0.250% | **0.036%** | 0.047% |
   | 8K 环绕接缝 | 1.23 | **0.78** | 0.90 |
   | 8K warp | 1.97 s | 3.62 s | **10.11 s** |
   | 8K 母版文件 | 39.6 MiB | 37.1 MiB | 39.3 MiB |

   **两组指标给出相反的排序，这本身是这一项最重要的发现。**
   指标 A 量的是重叠 tile 之间**一致**到什么程度 —— 越模糊越容易得高分，
   而更锐的核在边缘过冲会让两个 tile 更不一致。所以它**不能**当保真度用。
   保真度要用往返测量（`tools/fidelity_probe.py`）：把母版按 tile 的真实射线重采样回
   tile 网格，和渲染器出的那张比 PSNR。三个仰角环结论一致：
   **catmullrom 比 bilinear 好 1.24 dB，而 bilinear 并不比 nearest 好**
   （它去掉了半像素误差却换来模糊，两者抵消）。

   另外两个意外：(a) 指标 A 里原来有四分之一测的是我们自己采样器的半像素误差；
   (b) 母版文件大小的排序（39.6 / 37.1 / 39.3 MiB）和还原 PSNR 一致 ——
   bilinear 最小是因为磨平了最近邻的假高频，catmullrom 回到 39.3 是因为留住了**真**高频。

   **代价**：778 帧母版约 2.4 小时（bilinear 53 min、nearest 32 min），
   交付 MP4 约 2.2 小时。要快就 `--sampler bilinear`，代价 −1.24 dB。
   16 个 gather 正是 GPU 白送的东西 —— **这一项让「可选 GPU」那条的性价比明显变高了**。

   指标 D（LUT 与逐帧计算逐字节一致）**按采样器各自成立**，有测试钉住；
   `verify.BASELINES` 也按采样器分开记。门限没跟着调紧（理由见 AGENTS.md 第 9 节）。

   **那个「要不要更锐的核」的问题已经答了**：答案是要。
   当时的疑虑是「bilinear 把局部对比度降了 26–46%，其中有多少是真被磨掉的边缘」——
   往返测量给了答案：**有相当一部分是真的**，catmullrom 把它们留住了并且还原好 1.24 dB。
   顺带也说明当时那四条「证据」（指标 A、接缝、PNG 大小、σ<1）**全都不是保真度证据**，
   它们一致地偏爱更平滑的结果。这个教训写进了 `verify.BASELINES` 的文档字符串。
2. **极区专项** ✅ **前提被实测推翻，重新表述后按可做的部分做完了。**

   原文说「极区有 2–3 个 tile 重叠且存在亚像素错位，应当多 tile 联合重建而不是取最好的那个」，
   并断言「现在的加权平均是在平均这些错位，等于主动模糊」。**后半句不成立。**

   把羽化做成 `距边缘^power` 之后，这个指数正好张开原文要问的那一族：
   指数越高，混合越从「平均所有能看到的 tile」滑向「只取最中央的那个 tile」。
   用往返保真度扫它（3840 宽，`tools/fidelity_probe.py --feathers`）：

   | 羽化指数 | 0.5 | 1 | **2（默认）** | 4 | 8 | 16（≈只取最好的） |
   |---|---|---|---|---|---|---|
   | 对源还原 | 44.04 | 44.43 | **44.76** | 44.87 | 44.76 | 44.55 dB |

   **曲线在默认值附近见顶，朝「只取最好 tile」的方向是下降的。** 平均不是在损失细节 ——
   每个 tile 的插值都是同一个输出方向上的无偏估计，平均若干个无偏估计**降噪**而不引入模糊。
   而且这个探针按每个 tile 的中央 50% 打分，**结构上偏袒高指数**，即便如此高指数还是输了。
   最优在 2–4，差 0.11 dB 且各 tile 不一致 —— **不值得为它换默认值并重记全部基线**。

   **余量是真的存在**（`tools/detail_probe.py`：源 tile 在极区方向局部对比度 2.23–2.83，
   是中纬/赤道的 60–75%），只是它**不在重新加权里**。真要拿到它，需要的是**真正的联合重建**：
   把若干 tile 的原始样本按各自在输出局部坐标里的位置放到一起，用一个核在**并集**上求解
   （多帧超分那一类），而不是「各自插值再平均」。那要重做 LUT 的布局（每个输出像素要存
   跨 tile 的样本位置），收益**未测**。**结论**：重新加权这条路已经走到头（有数字），
   联合重建作为一个独立课题留给以后，理由和代价都写在这里，不再挂在 P4 里假装快做完了。

3. **线性光混合** ✅ **验证完成，结论是不做**（原文要求「现在就要验证」—— 验了）。

   拆成两件事分别测（`tools/linear_light_probe.py`，数字在 AGENTS.md 第 8 节）：
   只改混合是 **0.017 灰阶**，混合加插值都改是 **0.040 灰阶** ——
   方向确实是「线性光更亮」，但**不到一个量化步**，比换采样器本身小 12 倍。

   原因可以推出来：共节点装配下重叠的 tile 看同一束光，彼此只差约 0.5 灰阶，
   而两个空间的平均值之差按差值的**平方**增长。**这条对这套 rig 的任何场景都成立**，
   不是这一帧的巧合（帧 2433 复核同量级）。
   代价则是编码端要对 8850 万个浮点做幂运算，8K 下把帧成本再翻一倍。

   **另外更正一处**：原文说这条是「P7 色度工作的前提」—— 不是。
   4:2:0 那 4.3 dB 发生在 ffmpeg 的 swscale 里，与我们混合用什么空间无关。
   真正让这条自动成立的是源头换 16-bit/EXR（那时管线本来就在线性空间）。

   管线内部一律 float32、只在写盘/送编码器时量化 —— 这半句本来就已经是现状。
4. **接缝** ✅ **完成。** 羽化做成了 `距边缘^power`（`--feather-power`，默认 2，逐字节不变）——
   **指数比「宽度」有用**，因为距边缘同时是采样质量的代表（tile 在自己中央放大最少），
   所以按它的幂加权等于**优先采信最中央地看到这个方向的那个 tile**；
   而「宽度」（内部平坦 + 边缘斜坡）恰好会把这个性质丢掉。

   `tools/seam_probe.py` 按判据在 tile 相邻的大圆上等距取 20 个位置（共 140 个），
   量「跨边界梯度 / 沿边界梯度」—— 场景内容在比值里对消。
   **median 0.79 < 1：跨边界比沿边界还平滑，没有接缝**（阶梯边会远大于 1）。
   p95/max 的高值在所有羽化指数下**完全相同**，说明它们是真实景物边缘穿过边界，不是伪影。
   原文担心的「宽羽化引入极区糊」也没有出现（保真度在 0.5→2 之间是**上升**的）。

   顺带一个有用的性质：**指标 A 完全不随羽化变化**（0.524 一路不动），
   因为它统计各 tile 的亮度离散度、不带混合权重 —— 它是纯几何检查，调混合调不动它。

5. **时域稳定性** ✅ **完成，而且判据本身被证明在极点不成立。**
   `tools/temporal_probe.py`，连续 10 帧 8K。四个纬度带里三个满足「母版方差 ≤ 源方差」，
   极冠是 1.140。**但那个 >1 不是抖动**：`nearest` 与 `bilinear` 是**凸组合**
   （权重非负、和为 1，含羽化），输出必然被输入夹住，**数学上不可能制造方差** ——
   可 nearest 在极冠给出 1.159，比 catmullrom 还高。所以那是**两个网格在极点差 16 倍**
   造成的比较偏差，判据按字面理解在那里没有意义。

   于是改报一个**与网格无关的界**：任一输出像素的最大帧间跳变 vs 任一源像素的最大跳变。
   凸核必然 ≤ 1（nearest 四带全过，反过来印证度量是对的）；
   catmullrom 因负瓣在赤道超出 **1.1%**（231.3 对 228.7 —— 1400 万像素里最极端那一个，
   而那个位置本来就在发生 228 灰阶的跳变）。**没有可感知的新增抖动**，
   这 1.1% 是更锐的核在硬边过冲的已知代价。

6. **位深** ✅ **完成。** `--bit-depth 16` 出 16-bit 母版。源是 8-bit，
   所以 16 位不带来渲染器的新信息，它保住的是**重采样与混合产生的亚灰阶精度**
   （8-bit 量化会扔掉），对「还要被下游再重采样一次的母版」有意义，
   也是将来源头换 EXR 的落点。

   **Pillow 写不了 16-bit RGB PNG**（`Image.fromarray` 直接拒绝三通道 uint16），
   所以 `io.write_png16` 自己写。用 filter type 2（Up）—— equirect 相邻行相似、
   极点附近几乎相同，正好对上；正确性拿 Pillow 的读取当**独立解码器**校验。
   实测 8K **133.4 MiB/帧、13.14 s/帧**（8-bit 39.3 MiB、11.01 s），
   Up 滤波把它从 153.9 降到 133.4（−13%，不是减半 —— 16 位低字节本来就接近噪声）。
   **778 帧 = 101 GiB**，磁盘预检改成按位深查表（8 位 1.6、16 位 5.0 字节/像素）——
   原来那个单一常数在 16-bit 下会少预留 3 倍多。

   EXR 仍然只留接口（源是 8-bit，见第 11 节第 5 项）。


**完成判据（逐条兑现情况）**

| | 判据 | 结果 |
|---|---|---|
| ✅ | 极区细节保留度相对最近邻有**可量化**改善（给出指标与对比图） | 指标有两个：`fidelity_probe.py`（还原 PSNR，**保真度**）与 `detail_probe.py`（按纬度局部对比度）。catmullrom 对源还原 **+1.05 dB** 于 nearest、**+1.24 dB** 于 bilinear；极冠局部对比度 1.15（bilinear）→ **1.42**。对比图在 P4 的 A/B 目录里 |
| ✅ | 线性空间混合已验证 | 验了，**结论是不做** —— 0.017 灰阶，理由可推导（见第 3 项） |
| ✅ | 全景无可见接缝（等距抽 20 个 tile 边界做梯度不连续性检查） | `tools/seam_probe.py`，140 个位置：跨边界/沿边界梯度比 **median 0.79 < 1**，各羽化指数下 p95/max 相同（是真实景物边缘，不是伪影） |
| ✅ | 相邻帧时域方差不高于源 tile 自身的时域方差 | 四带里三带满足；极冠的 1.14 **被证明是网格偏差而非抖动**（凸核不可能制造方差，而 nearest 更高）。改用与网格无关的跳变界：凸核全过，catmullrom 在赤道超 1.1% |
| ✗ | ~~指标 C：用 P0.5 基准集给出逐像素证明~~ | **做不到，接受这个限制**：P0.5 被用户否决，参考产物与在手源帧永远没有同一帧（AGENTS.md 第 9 节）。替代方案就是上面这些无参考指标 + 自己两版之间的 A/B。**汇报时不要把它们包装成对参考产物的胜出** |

**六项全部处理完**（第 1、4、5、6 项做完；第 3 项验证后不做；第 2 项的前提被实测推翻，
重新加权那条路已走到头，真正的多帧联合重建作为独立课题留给以后 —— 理由与代价见第 2 项）。

---

## P5 — 输出命名与两种输出模式（已完成）

用户 2026-09-08 追加的需求，加上同日追加的限制条件。

**做完的**

1. **视频命名**：`<stem>_<月日>_<时分>.mp4`，实跑 `L_Cathedral_0908_1751.mp4`。
2. **帧模式**：`<stem>_<月日>_<时分>/` 目录 + `<stem>_S_<帧号>.png`，
   实跑 `L_Cathedral_0908_1751/L_Cathedral_S_1656.png`。
   帧号沿用源的补零位数 —— 不补零的话 `999` 会排在 `1000` 后面。
   （引擎侧的单帧输出是 P3 做的 `--out-format png`，P5 是把它变成明确的模式并改命名。）
3. **时间戳格式 `MMDD_HHMM`（本地时间）**：无空格；
   **冒号在 Windows 文件名里非法**，所以不能用 `HH:MM`。
   原先是 `YYYYMMDD_HHMMSS`，用户 2026-09-08 指定收窄到只留月日与时分。
   **这换掉了两个性质，如实记下来**：
   - **只在一年之内可排序** —— 没有年份，`0102` 永远排在 `1231` 前面。已接受：
     这个名字是给发起渲染的人看的标签，不是归档主键。
   - **不再唯一** —— 同一分钟内两次运行会拿到同一个名字。这个**没有**接受：
     自动命名的承诺是「每次跑都是一份新产物」（所以它不续跑），而 MP4 的分段目录是从
     输出名派生的，撞名就会静默接管上一次的分段，参数不同的话产出就是错的。
     修法是 `pipeline.unique_path`：自动生成的路径若已存在就加 `_2`、`_3`……
     **显式 `--out` 不受影响** —— 复用路径正是请求续跑的方式，两者的区别就在这里。
4. **一次运行只读一次时钟**，有测试钉住。这不是洁癖：MP4 的分段目录是从输出名派生的，
   读两次时钟可能跨分钟，就会把一个作业的分段撒到两个目录里。

**限制条件（用户同日追加）—— 做成结构性的，不是约定**

| | 视频模式（默认） | 帧模式 `--out-format png` |
|---|---|---|
| 尺寸 | 按 `--size` 交付，需要时 ffmpeg 里 Lanczos 降采样 | **按 input 自己的密度**（`Rig.native_width(tile_size)`，本 rig = 4× tile = 7680），**不接受 `--size`** |
| 压缩 | H.264/H.265 有损 | **无损** |

- 帧模式那条路径**没有** codec / 码率 / 帧率 / 交付尺寸这些字段可用；
  CLI 上传了会**明确报错**而不是被忽略（`_reject_inapplicable`）。
- `MasterJob.__post_init__` 校验 `width == rig.native_width(source.tile_size)`，
  别的宽度直接拒绝，有测试。想要别的尺寸用 `frame --width` 出单张。
- **「不要压缩」指的是有损压缩。** PNG 的 zlib 等级在任何取值下都无损 ——
  0 到 9 解码出完全相同的像素，`tests/test_master.py` 用 0/1/9 三档断言了这件事，
  所以它不是「压缩」在会掉画质那个意义上。默认 1（0.68 s/帧 对 6 的 2.77 s）；
  要字面意义上不压缩就 `--compress-level 0`，磁盘约 2.3 倍。

**一个必须说清楚的副作用：带时间戳的自动命名关掉了「续跑」**

P2/P3 的续跑靠**输出路径相同**来认已完成的分段/帧。名字里带时间戳，
再跑一次同一条命令就得到新名字，于是什么都不复用。

这不是 bug —— 带时间戳本来就是「每次跑都是一份新产物」的意思。所以：

- 自动命名（默认）：每次一份新产物，**不续跑**。
- 显式 `--out <路径>`：沿用原语义，**可续跑**（分段或按文件）。
- 中断后想接着跑：把日志里那行 `output :` 原样传给 `--out`。
  帮助文本、README、这一段都写明了这一点。

**完成判据（逐条兑现）**

| | 判据 | 结果 |
|---|---|---|
| ✅ | 视频名 `<stem>_<MMDD>_<HHMM>.mp4` | 有测试；真实数据实跑确认（宽度按用户 2026-09-08 的要求收窄） |
| ✅ | 单帧 `<stem>_<MMDD>_<HHMM>/<stem>_S_<帧号>.png` | 有测试；真实数据实跑确认 |
| ✅ | 自动命名撞名时加后缀而不是接管上一次的产物 | 有测试（同一分钟跑两次，断言两份完整产物各自独立） |
| ✅ | 时间戳一次运行内只取一次 | 有测试（把 `run_stamp` 换成计数器，断言只被调用一次） |
| ✅ | `--out` 显式给出时行为不变、续跑仍有效 | 原有续跑测试未改动仍全绿 |
| ✅ | 帮助文本与 README 说明「自动命名不续跑」 | `--out` 的 help、README 快速开始与「两种输出模式」小节 |
| ✅ | 帧模式无损、按 input 尺寸，且是结构性保证 | 宽度校验 + 无可用的有损参数 + zlib 三档像素恒等，都有测试 |

---

## P6 — GUI（PySide6，已完成）

**GUI 只能是薄壳** —— 调用 CLI 走的同一套代码，不允许出现第二份逻辑。
所以它排在 P1–P5 之后：引擎和参数先定下来，再包外壳。
（用户把 GUI 与交付合规对调后，它不再排在 P7 交付合规之后 —— 见「里程碑一览」下面那条说明。）

**样式与实现参考** `D:\VR-DEV\Python\VR-Installer\ui`（用户指定）：
`.ui` 由 `QUiLoader` 运行时加载、`.qss` 用 `setStyleSheet` 应用、`findChild` 按 objectName 绑定、
`variant` / `status` 动态属性承载样式状态。配色与卡片写法直接沿用，两个工具看起来是一家的。

**有一处故意不照抄**：那个工具把任务放在 `QThread` 里。本项目 AGENTS.md 第 7 节要求
**作业跑在独立子进程**，理由具体：管线本来就有解码线程池、warp 线程池和一串 ffmpeg 子进程，
把它们放进正在跑 Qt 事件循环的那个解释器里，正是这条约束要避免的干扰；
独立进程还让作业崩溃只带走子进程，而取消也拿到一条不会卡在 GIL 上的通道。

**「薄壳」是字面意思**：GUI **不 import 管线**，它把 CLI 当子进程跑。为此 CLI 加了两个选项：

- `--progress-json`：stdout 只走 NDJSON（`start` / `progress` / `log` / `done` / `cancelled` / `error`），
  所有人类可读的行改走 stderr。父进程于是有一条能直接解析的流，不用去刮文字。
- `--cancel-on-stdin`：父进程写一行就停；**stdin 到 EOF 也算取消** ——
  那意味着「父进程没了」，这时候把片子渲染完对谁都没好处。
  它设置的是管线本来就接受的那个 `threading.Event`，所以取消仍然是**同一个机制、同一套保证**：
  完成的分段/帧保留、无半成品文件、可续跑。

`QProcess` 在事件循环上读这条流，所以窗口这边**一个线程都不需要**。

**界面**（`src/vr_compose/gui/`：`window.py` + `main_window.ui` + `style.qss`）

来源目录（带自动发现）、渲染名（文件名前缀，代码里叫 stem）、帧范围、输出位置、
输出模式（视频/单帧）、交付尺寸/编码/码率档/帧率、采样器、母版位深、
进度条 + 明细、深色日志、开始/取消/重新检测。

几个不只是摆控件的地方：

- **选项列表全部来自库**（`encode.SIZES` / `encode.LADDER` / `stitch.SAMPLERS` / `stitch.BIT_DEPTHS`），
  不在 `.ui` 里写死 —— 否则第一次改参数就会和 CLI 漂移。有测试钉住。
- **按模式禁用另一半参数**。CLI 对不适用的参数是**报错**而不是忽略（P5），
  所以 GUI 干脆置灰：视频模式没有母版位深，单帧模式没有 codec/码率/帧率/交付尺寸。
  argv 也只带该模式接受的那些。
- **帧范围默认填「最后一段连续帧」**，不是 `all`。参考数据是 `0000` 加 `1656..2433`，
  填 `0-2433` 正好踩进文档一直在警告的那个坑（开头跳帧）。
  新增 `pipeline.contiguous_tail()` 算这个范围，并在日志里说明为什么跳过了前面那帧。
  CLI 的 `all` 仍然是「全部」—— 改掉一个明确写出来的词的含义，比它避免的坑更糟。
- **目录不合规时原样显示 `source.problems`**（「tile 非方形」这种），不是一句「无效目录」。
- **多个 stem 时不替用户选**：下拉框第一项是「— 请选择 —」，选它时「开始」是灰的。
  标签写的是**渲染名**而不是「场景 (stem)」（用户 2026-09-08 反馈看不懂）：
  stem 就是 UE 给文件的前缀（`L_Cathedral.1656.png` 的 `L_Cathedral`），
  是关卡/序列名，**与左右眼无关**（`L_` 是 UE 的 Level 前缀）。
  一组 `Camera*` 目录里只有一套渲染时它自动填好，放了多套才需要选。

**完成判据（逐条兑现情况）**

| | 判据 | 结果 |
|---|---|---|
| ✅ | 作业跑在独立子进程，GUI 通过管道读进度 | `QProcess` + NDJSON；有测试断言 `window.process` 是子进程而不是线程 |
| ✅ | 进度显示当前帧/总帧、s/帧、ETA，与 46.85 s/帧 可比 | `帧 1659  4/10  段 1/1  2.67 s/帧（17.57× vs 46.85）  剩余 0.3 分钟` |
| ✅ | 进度条在日志上方，条上有百分比与帧数 | 22 px 高、`%p% · %v / %m 帧`；文字居中会跨过填充边缘，所以填充色特意调亮一档，让深色粗体在白底和蓝底上都读得清（用户 2026-09-08 提出） |
| ✅ | 准备阶段可见 | 点开始到 `start` 事件之间走**不确定态**（来回扫动）+「正在发现来源、构建 warp 表、预检磁盘…」；卡在 0% 的进度条和卡死的作业看起来一样 |
| ✅ | 最小化也能看进度 | 百分比写进窗口标题，任务栏可见 —— 8K 全长要跑两个多小时，没人会一直盯着窗口 |
| ✅ | 不确定态一定会退出 | `_leave_busy()` 挂在每条退出路径上（含**从未发出 `start`** 的前期失败：发现失败、缺 ffmpeg、磁盘预检）；有测试 |
| ✅ | 跑作业期间界面不卡 | 作业在别的进程里，窗口这边没有阻塞调用；事件循环全程空闲 |
| ✅ | 取消 → 重新开始 → 只补缺段 | 真实数据实跑：4/10 帧时取消，4 张留下、无 `.part`、重跑补完；另有一条不依赖 Qt 的子进程测试 |
| ✅ | 不合规目录给出可操作的错误信息 | 有测试（长方形 tile → 日志里出现 "not square"，状态转 fail，「开始」禁用） |
| ▶ | 从 GUI 完整跑通一次 778 帧作业，产出与 CLI **逐字节一致**的 MP4 | **待你验收**：需要约 2.2 小时机器时间（catmullrom）。逐字节一致这一点在结构上是成立的 —— GUI 传的是同一条 CLI 命令行，产物由同一份代码写出；差异只可能来自参数，而参数由测试逐项断言过 |

**还差的一件事**：上面那条 778 帧全长跑。命令 GUI 已经会拼了，
你也可以直接用 CLI 跑同一条来对比哈希。

---

## P7 — 交付合规与色彩管线调优

源数据在手，这一阶段**现在就能完整做完**。

**做什么**

1. **参数矩阵全覆盖并逐项 `ffprobe` 校验**：
   `{8k, 4k} × {h264, h265} × 三档码率 × {30, 60} fps`，共 24 种组合。
   码率按尺寸取档（8k 200/150/100 Mbps，4k 57/43/28 Mbps），
   level/tier 由 `select_level()` 按规范表**算出最小合规等级**，不查表也不交给编码器。
   逐项断言 FourCC / profile / level / tier / pix_fmt / **B 帧数=0** / GOP 长度 / 无音轨。
   **12 种 30fps 组合已实测全部通过**（`tools/encode_probe.py matrix`），
   这一步是固化成自动化断言并补上 60fps 的一半。
2. **尺寸参数化**：`--size {8k,4k}`（8k = 7680×3840 默认，4k = 4096×2048）。
   4k 由 8k 母版 Lanczos 降采样；equirect 的水平方向是均匀的，直接降采样是安全的。
   两个必须写成断言的防呆：
   - `-preset` 不得为 `ultrafast`（会把 H.264 降成 Constrained Baseline）；
   - `libx265` 不得使用 auto level（会产出 HEVC 标准里不存在的 Level 7.1）。
   这两个都是**静默**失败，只有 `ffprobe` 断言能抓到。
3. **球面元数据**：自己写 `sv3d`（equirectangular projection）box。
   ffmpeg 写不了，PyPI 也没有可用包（都已核实）。
   用 GPAC MP4Box（`winget install GPAC.GPAC`）做交叉验证。
4. **色彩管线 —— 吃回 4:2:0 的那 4.3 dB**。这是整个交付侧唯一有量级收益的地方。
   实测基线：4:2:0 = 43.21 dB / 直流偏移 −1.23；4:4:4 = 47.54 dB / 偏移 +0.05。
   ffmpeg 现成开关无效（swscale 精确舍入无变化；zscale 反而更差）。
   要试的是**在我们自己的 float32 RGB 上做色度下采样**：
   - 在线性光下做 4:2:0 下采样（而不是在 gamma 编码值上做）；
   - 正确的色度取样位置（chroma siting）；
   - 抖动（dither）抑制 8-bit 量化的直流偏移。

   **这是实验，不是既定收益。** 判据是「相对 43.21 dB 有可测量的提升」，
   提升不明显就如实记录并停手，不要为了 0.1 dB 引入复杂度。
5. **不带音轨**（已确认）：显式 `-an`，并断言产物里没有音频流。
6. **播放验收**：在目标设备上确认识别为 360°、无接缝、无极区破面、不掉帧。

**完成判据**

- 24 种参数组合全部通过 `ffprobe` 断言，写成自动化测试。
- profile 一定是 High/Main，level 一定在 HEVC/AVC 标准定义范围内（不出现 L7.1）。
- 球面元数据被 MP4Box 与目标播放器同时正确识别。
- 色彩管线实验有明确结论（不论正负），基线数字与结果都记录在案。
- 成品在目标设备上播放验收通过。

---

## P8 — 打包 exe（PyInstaller，已完成）

打包成本已经实测过（AGENTS.md 第 7 节），这一阶段是把它固化成可重复的构建。

**一条命令**：`python tools/build_exe.py --ffmpeg C:/ffmpeg/bin --source E:/22`
（`--source` 可选，给了就多跑一次真数据渲染并与源码运行逐字节对比）。
产物 `dist/VR-Compose/`，**537.8 MiB**（应用本体 167.5 MiB + ffmpeg/ffprobe 370.3 MiB），
构建 74 s。中文使用说明 `docs/使用说明.md` 由构建脚本一并拷进去。

**入口文件已经就位**：根目录的 `main_ui.py`（用户要求，与 VR-Installer 的布局一致）。
`.spec` 指向它。它已经处理了三件冻结后才会暴露的事：

- **一个 exe，两副面孔**：冻结后只有一个可执行文件，而窗口是靠**启动自己**跑作业的
  （`gui.window.worker_command()` 冻结时返回 `[sys.executable]`）。
  所以 `main_ui.py` 的规则是 **没有参数 = 开窗口，有参数 = 当 CLI**。
  少了这一条，打包版按「开始」会再开一个窗口而不是渲染。
- **`freeze_support()` 是 `__main__` 下第一条语句**（第 2 节约束 4），有测试读源码断言。
- **src 布局**：`vr_compose` 在 `src/` 下，所以它在需要时把 `src/` 加进 `sys.path`，
  这样未安装的检出也能 `python main_ui.py` 直接跑。

**做什么**

1. **onedir 分发** ✅（探针 158 MB / 热启动 0.86 s；**本项目实测 167.5 MiB，
   `--version` 热启动 0.37–0.57 s**）。不要用 onefile ——
   省 94 MB 下载量换来每次启动多 5 秒。
2. ~~`freeze_support()`~~ 已在 `main_ui.py` 里做完，见上。
3. **不要写 Qt 模块排除列表** —— 实测加 47 条 `--exclude-module` 体积一点没变，
   PyInstaller 的 PySide6 hook 已经只打包用到的模块。省下这份工。
4. **ffmpeg + ffprobe 打进分发包** ✅，放在 exe 同级，**解压即用**。
   实测合计 537.8 MiB（167.5 应用 + 185.2 ffmpeg + 185.1 ffprobe）。
   查找顺序：exe 同级优先，其次 PATH。构建脚本用**应用自己的查找逻辑**定位待拷贝的
   ffmpeg，所以缺工具是在构建时报错，而不是等用户第一次渲染才发现。
   GPL 许可问题用户已决定不处理（非商业软件）。
5. **构建脚本入库** ✅ `VR-Compose.spec` + `tools/build_exe.py`（构建 → 拷 ffmpeg 与说明
   → 冒烟），一条命令可重复。
6. **冒烟测试** ✅ 由构建脚本自动跑，且跑的是**打出来的 exe** 而不是源码树：
   `--version`、真数据两帧渲染、以及与源码运行的 **sha256 逐字节对比**。

**完成判据**

| | 判据 | 结果 |
|---|---|---|
| ✅ | onedir 包解压即用，跑通一个小作业 | 构建脚本每次都跑两帧真数据渲染 |
| ✅ | 冻结后的 exe 与源码运行产出**逐字节一致** | sha256 相同；不一致就直接让构建失败 |
| ✅ | ffmpeg 缺失时给出明确可操作的提示 | `stage_ffmpeg` 在构建时就报错并说清怎么办 |
| ✅ | 双面孔规则冻结后仍成立 | 冒烟渲染跑的就是 `VR-Compose.exe --source ... sequence ...`，与窗口按「开始」时走的是同一条路（`worker_command()` 冻结时返回 `[sys.executable]`） |
| ⏳ | **在干净的机器/账户上**解压即跑 | **只有用户能验** —— 本机装着 Python、PySide6 与 ffmpeg，「解压即用」在这里证明不了 |
| ⏳ | 打包版按「开始」真的渲染出完整一版 | 用户明日实机验收（与 P6 的 778 帧验收是同一次） |

**console 应用 + 自己隐藏控制台**：`.spec` 里 `console=True` 不是疏忽 ——
windowed 构建会让 `sys.stdout` 变成 None，那会同时打断两副面孔
（CLI 什么都不打印，窗口的 NDJSON 管道也写不出去）。
于是双击会多出一个黑窗口，所以 `main_ui.py._hide_own_console()` 在
**且仅在** `GetConsoleProcessList` 说本进程是这个控制台的唯一主人时把它隐藏 ——
那正是双击的样子；从别人的终端里启动时它什么都不做。

---

## 运维与上游改造

1. **完整性守护**：批量校验输出可解码、尺寸正确、无缺帧；MP4 分段 concat 后校验总帧数。
2. **磁盘压力**：长任务前预检空间，运行中低于阈值就优雅停止。
3. **上游改造（性价比最高的一项）**：推动 UE 侧
   - 不再重复渲染那 5 路重复视角 → 渲染时间直接省 25%；
   - 输出去掉全 255 的 alpha → 再省 25% 字节；
   - 输出 EXR/16-bit → 抬高整条管线的画质上限。

   这三项加起来的收益超过下游任何优化，但需要改 UE 工程，属于跨团队协调事项。

---

## 主要风险

| 风险 | 影响 | 应对 |
|---|---|---|
| `E:\22\Camera*\Tempory` 的 778 帧是唯一副本 | 不可恢复的数据丢失 | 全程**只读**；不启动上一代流程、不写入、不删除（第 2 节约束 1–2） |
| **无法与参考产物逐像素对比**（用户否决了造对照的方案） | 「超过参考产物」只能定性论证 | 用无参考指标 + 自己两版之间的 A/B 量化；**汇报时如实说明这个限制**，不要把无参考指标包装成胜出（AGENTS.md 第 9 节指标 C） |
| **冻结后缺 `freeze_support()`** | 每个进程池 worker 启动一份完整 GUI，递归 | 入口第一行必须是它；三种打包配置已实测验证；P8 的冒烟测试要覆盖 |
| **产品代码里残留硬编码来源路径** | 换来源目录就跑不起来，exe 更是必然失败 | 来源目录是参数 + 自动发现；P1 完成判据里有一条全仓库 grep |
| **rig 只对 20 路装配成立** | 换装配会静默拼错或直接崩 | rig 做成按相机数索引的注册表，未登记布局明确拒绝（AGENTS.md 第 6 节末） |
| 分发包约 528 MB（含 ffmpeg+ffprobe） | 下载/拷贝成本 | 已是用户决策，换来解压即用。许可问题用户决定不处理（非商业软件） |
| elevation 符号 / 扇区顺序搞反 | 拼出「看起来正常」但实际错位的全景 | 每次改动都跑指标 A，不靠肉眼 |
| 8K H.264 只能软解，头显放不了 | 交付一个目标设备打不开的文件 | 已由 `--size 4k`（4096×2048 / H.264 L5.2）解决；8K H.264 仍可产出，定位为 PC 软解用 |
| **`libx264 -preset ultrafast` 静默降级为 Constrained Baseline**；**`libx265` auto level 产出标准里不存在的 L7.1** | 交付物形式上不合规，且肉眼与播放都看不出来 | preset 固定 medium 或更慢；level 一律显式钉死；24 种组合逐项 `ffprobe` 断言 |
| **`hevc_nvenc` 会静默产出不合规流**（auto level = Main tier L6.1，200 Mbps 超其 120 Mbps 上限） | 交付物形式上不合规 | 默认用 `libx265` + `level-idc=6.2:high-tier=0`；用 NVENC 必须 `-tier high -level 6.2` |
| 4:2:0 强制损失 4.3 dB，且 ffmpeg 现成开关救不回来 | 成品画质天花板被规格锁死 | P7 做线性光色度下采样实验，**但不承诺一定有收益**；同时 P4 抬高母版质量以穿透编码 |
| 4K 码率缩放后画质下降 0.67–0.89 dB | 4K 版比缩放前略软 | 已是用户决策；换来文件小 72% 且 H.265 从 High tier 回到 Main tier（兼容性更好）。数字记录在 AGENTS.md 第 5 节 |
| MP4 是单一输出文件，无法靠「文件已存在」续跑 | 长任务中断后要重跑 | 按 GOP 边界分段 + `-c copy` concat（闭合 GOP、B=0 使无损拼接成立） |
| `D:` 只剩 131 GB | 长任务写盘失败 | 产物写 `E:`（978 GB）；启动前预检。直出 MP4 后落盘量骤减（每套序列约 2 GB 而非 64 GiB） |
| 8-bit 源 + 极区极端各向异性（次轴放大约 16×） | 画质天花板已被源数据锁死 | P4 做多 tile 联合重建（源里确实有细节，余量已量过）；同时推 P8 的上游 EXR。注意「极区欠采样 2.7×」是旧说法，P4 已更正 |
| （可选）GPU 加速引入 ~1 GB 依赖、仅 NVIDIA，且 FMA 融合会破坏与 CPU 的逐字节一致 | 分发包变大；无卡机器跑不起来；指标 D 失效 | 做成 `[gpu]` extra，CPU 路径保留为基准与回退；先关 FMA 争取逐字节一致，做不到就**显式**改容差重基线；无 NVIDIA 机器上验证回退 |

## 下一步

**P2–P6 与 P8 已完成**（8K 2.10 s/帧，22.3×；P3 只剩手动 Ctrl-C 验收；P4 六项全部处理完；
P5 命名与两种模式的边界已落地；P6 GUI 已完成；P8 打包与中文说明已随包分发），
下一步是 **P7 交付合规** —— 它现在是唯一剩下的开发阶段。
原 P4 的说明保留在下面：各向异性重采样替换最近邻、
极区多 tile 联合重建、线性光混合、时域稳定性检查。P4 只动母版层，不碰编码；
每一步都要先过指标 A（几何）和指标 D（`WarpPlan` 与 `stitch_bands` 逐字节一致，
或者在有意换采样器时**同时**更新两者并重记基线）。

现在就可以用 `vr-compose sequence` 把剩余 778 帧直出成交付 MP4（预计约 28 分钟），
不必等 P4 —— P4 的画质改进会改变像素，届时重跑一次即可。

**GPU 加速列为可选项**（见上面「可选 — GPU 加速」）：CPU 版已达标，GPU 买的是把 26 分钟变
3 分钟的体验，代价是 ~1 GB 依赖和仅 NVIDIA。是否做、何时做，由用户决定；决定前不引入依赖。

需要用户拍板、且会影响后续阶段设计的（AGENTS.md 第 11 节）：

1. **色彩范围** limited 还是 full（P7 的前提，建议 limited）。
2. **GUI 除了最小可用集还要什么**（P6 的范围）—— 预览、批量队列、日志导出、记住上次设置？
3. **后续项目会出现哪些装配**（决定 rig 注册表要预留多少）。

一件要如实记在这里的事：**逐像素对照已经放弃**（用户否决），
所以画质结论只能靠无参考指标和自己两版之间的对比来支撑。这不影响工程推进，
但影响我们能对外声称什么 —— 见 AGENTS.md 第 9 节指标 C。
