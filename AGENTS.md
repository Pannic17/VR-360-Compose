# AGENTS.md — VR-Compose

给在本仓库工作的 AI agent 与新同事的上下文文件。**先读完本文件再动手。**
路线图与阶段目标见 [ROADMAP.md](ROADMAP.md)。
**上游（UE 侧）不可修改**（用户 2026-09-09），所以「上游若能改则值得做」的那几条
单独记在 [UPSTREAM.md](UPSTREAM.md) —— 那是参考清单，不是计划。

---

## 1. 项目是什么

把 Unreal Engine 渲染出的 **20 路方形相机序列帧**合成为 **360° 球形全景视频**（equirectangular，等距圆柱投影）。

- **这是单目（monoscopic）全景。** 20 路相机共节点，一个节点只能出单目。
- **最终交付物是 MP4**（H.264 默认 / H.265 可选，30 fps 默认，
  输出尺寸 **8k = 7680×3840** 默认 / **4k = 4096×2048** 可选，码率按尺寸取档）。
- **产品形态是带 GUI 的 Windows exe**（PySide6 + PyInstaller）。
  **来源目录可选**；不指定时在 exe 同级按目录结构自动查找。见第 6 节。
  完整规格与实测验证见第 5 节。目标是**画质超过现有参考产物**。
- 参考数据（**只读**）：`E:\22`。这是唯一一份、且正在被上一代流程消耗的生产数据。
- 参考产物质量基线：`E:\22\FinishTaskOutput`，1655 帧已完成。

**代码现状**（详见 [ROADMAP.md](ROADMAP.md) 的「当前进度」）：**P0、P1、P2 已完成**。
`src/vr_compose/` 下有 `projection` / `rig` / `source` / `stitch` / `warp` / `encode` /
`pipeline` / `verify` / `io` / `cli` 十个模块。`vr-compose frame` 拼单帧并自判几何；
`vr-compose sequence` 把一段帧直出成交付 MP4，8K 实测 **2.17 s/帧**（上一代 46.85），
可中断续跑，产物 `ffprobe` 逐项合规（第 8 节）。下一步是 P3（收尾：取消语义验证、真实数据 smoke、PNG/EXR 母版 sink），随后 P4 画质。

---

## 2. 硬约束（违反会造成不可恢复的损失）

1. **绝不写入、移动、删除 `E:\22` 下的任何内容。** `Camera*/Tempory/` 是上一代流程的输入队列，
   它会在处理完成后删除源帧 —— 帧 1..1655 已经被消耗掉，**没有备份**。剩余 778 帧是唯一的源数据。
2. **不要启动上一代流程，也不要为它做备份**（用户已明确否决）。
   要验证什么就自己写脚本跑 —— `tools/` 就是干这个的。
   代价是**永远拿不到与参考产物逐像素可比的一对帧**，见第 8 节指标 C。
3. **产品代码里不准出现 `E:\22` 或任何硬编码来源路径。**
   来源目录是参数（CLI `--source` / GUI 选择器），不指定时按目录结构自动发现（第 6 节）。
   `E:\22` 只能作为只读参考出现在文档、测试和 `tools/` 的默认值里。
   **用户 2026-09-09 说这不算风险了**（上游不可修改，来源结构不会变）。
   规则照旧 —— 代码现在是干净的（唯一提及是 `rig.py` 说明 rig 出处的 docstring），
   而它是「exe 拷到哪儿都能跑」的前提，没有理由为此放松；
   变的只是不再当风险跟踪（ROADMAP 的风险表里已撤）。
4. **`multiprocessing.freeze_support()` 必须是 `__main__` 下的第一条语句。**
   PyInstaller 冻结后，进程池靠重新执行 `sys.executable` 派生 worker；缺了这一句，每个 worker
   都会启动一份完整 GUI，而且是递归的。已实测加上之后正常（第 7 节）。
5. 所有产物写到**新目录**，绝不写进来源目录。`--out` 不给时的默认位置是**程序所在目录**
   （冻结后是 exe 的目录，从源码跑是工程根目录；`pipeline.default_output_dir()`），
   文件名 `<stem>_<日期>_<时间>.mp4`（P5，用户 2026-09-08 指定）。这是用户的决定，GUI（P6）再加目录选择器。
   **带时间戳的自动命名意味着「每次跑都是一份新产物」，因此不会续跑** —— 要续跑必须显式传 `--out`。
   帧模式是同名目录 `<stem>_<月日>_<时分>/`，里面 `<stem>_S_<帧号>.png`。
   时间戳是 `MMDD_HHMM`（用户 2026-09-08 指定），只到分钟因此**可能撞名**，
   自动生成的路径撞名时由 `pipeline.unique_path` 加 `_2` —— 显式 `--out` 不动，
   复用路径就是请求续跑。
   `.gitignore` 已排除 `*.mp4` / `*.segments/`，免得 2 GB 的产物进仓库。
6. 磁盘余量：`E:` 978 GB（SATA SSD，读写都在这块盘上）、`D:` 131 GB（NVMe）。
   单帧无损输出约 27 MiB；直出 MP4 之后落盘量骤减（每只眼 0.3–2 GB 而非 64 GiB）。
   **用户 2026-09-09：这台机器的盘只做测试用途，不算约束。**
   启动前的磁盘预检代码留着（已经写好、有测试、且只在真的写不下时才拦），
   但**不要再把「`D:` 只剩 131 GB」当成风险**。
7. 不要一次性把整个序列读进内存。单帧输入 15×1920×1920×3 = 166 MB，输出 7680×3840×3 = 88 MB。
   机器有 128 GB，但必须按帧流式处理，内存占用与总帧数无关。
8. 任务必须**可中断**、且中断后不留坏产物。**「可续跑」2026-09-09 从硬约束降级为
   「已经有了就留着」** —— 用户明确「中断就重跑」。
   分段 + `-c copy` concat 那套（ROADMAP P2）照旧工作、有测试、也照旧默认开着；
   变的只是：**别再为了续跑去限制别的设计**，也别把它当必须满足的验收项。
   仍然不许退步的是「中断不留半个坏文件」—— 那是数据完整性，不是便利性。
9. **8K 的 H.264 必须用 `libx264`（软编）**。`h264_nvenc` 编不了 7680×3840
   （实测 `No capable devices found`，NVENC 的 AVC 上限是 4096×4096）；`libx264` 可以。
10. **`libx264` 的 preset 会悄悄改 profile**：`-preset ultrafast` 输出的是
    **Constrained Baseline**（关了 CABAC 和 8×8 变换），即使显式写了 `-profile:v high` 也一样。
    规格要求 High profile → **preset 必须 medium 或更慢**。只能靠 `ffprobe` 断言发现。
11. **`libx265` 的 auto level 会产出非法等级**：150/200 Mbps 下实测输出 **Level 7.1**，
    而 HEVC 标准最高只定义到 **6.2** —— 没有解码器会接受。
    **等级必须显式算出来**，见 `tools/encode_probe.py: select_level()`。
12. **交付用的 MP4 必须是 I+P only / B=0 / 闭合 GOP**。`x265` 的 open-GOP **默认是开的**，
    必须显式 `open-gop=0`；只写 `-bf 0 -g 60` 不够，还要 `min-keyint` 和 `scenecut=0`。

---

## 3. 相机装配（rig）—— 已从数据中反解并验证

**这是本项目最重要的领域知识。已经解出来了，不需要再做特征匹配或标定。**

反解方法：把每个 tile 投影到成品 equirect 上做 NCC 搜索（yaw/pitch/roll/FOV），
再用理论标称值重新拼接一帧、测量重叠区一致性。结论：

| 项 | 值 |
|---|---|
| 相机模型 | 理想针孔（pinhole），无畸变 |
| FOV | **水平 = 垂直 = 90.0°**（正方形画幅，`tan(FOV/2) = 1`，等价于 cubemap 的一个面） |
| roll | 0 |
| 节点 | 15 路**共节点**（同一光心），重叠区无视差 |
| 方位角 yaw | 5 个扇区：0°, 72°, 144°, 216°, 288° |
| 仰角 elevation | 每扇区 3 档：**+45°（朝上）, 0°（水平）, −45°（朝下）** |
| 唯一视角数 | **15**（不是 20） |

### 文件序号 → 视角

设 `i` 为 `CameraN` 的 N（1..20），`s = (i-1) // 4`（扇区 0..4），`slot = (i-1) % 4`：

| slot | yaw | elevation | 说明 |
|---|---|---|---|
| 0 | `72·s` | **+45°** | 朝上 |
| 1 | `72·s` | **−45°** | 朝下 |
| 2 | `72·s` | **0°** | 水平 |
| 3 | `72·(s+1) mod 360` | **+45°** | **下一扇区 slot 0 的重复渲染** |

因此存在 5 组**逐像素完全相同**的文件：

```
Camera4 ≡ Camera5    Camera8 ≡ Camera9    Camera12 ≡ Camera13
Camera16 ≡ Camera17  Camera20 ≡ Camera1
```

（帧 0000 上逐字节像素相同；后续帧因渲染噪声/TAA 最大差 1 灰阶。）

**结论：每帧只需读 15 个文件，`Camera4/8/12/16/20` 整目录可跳过 —— 直接省掉 25% 的读 I/O。**

### 坐标与投影约定（与成品完全一致，已验证无翻转、无全局 yaw 偏移）

世界系右手、Z 轴向上。方向向量由经纬度给出：

```
d(lon, lat) = (cos(lat)·cos(lon),  cos(lat)·sin(lon),  sin(lat))
```

Equirect 采样（W=7680, H=3840）：

```
u = (lon / (2π) + 0.5) · W        lon ∈ [-π, π)，图像最左 = -π
v = (0.5 - lat / π)   · H        lat = +π/2 在图像最上
```

Tile 像素 → 方向（N = 1920，`t = tan(45°) = 1`）：

```
a = 2·(px + 0.5)/N - 1           # 列，左 -1 → 右 +1
b = 2·(py + 0.5)/N - 1           # 行，上 -1 → 下 +1
d_cam   = normalize(1, a·t, -b·t)
d_world = Rz(yaw) · Ry(-elevation) · d_cam

Rz(θ) = [[cosθ, -sinθ, 0], [sinθ, cosθ, 0], [0, 0, 1]]
Ry(θ) = [[cosθ, 0, sinθ], [0, 1, 0], [-sinθ, 0, cosθ]]
```

注意 `Ry(-elevation)` 的负号：elevation 为正表示**朝上**。这个符号搞错的话画面会上下颠倒地拼合，
而且因为球面是对称的，低分辨率预览不一定看得出来 —— 一定用第 6 节的重叠一致性指标验证。

### 覆盖与采样密度（已实测）

- 球面覆盖 **100%**，无空洞；加权平均每个方向被 **2.50** 个 tile 看到，最大 **4** 个。
  按立体角加权的覆盖分布：1 个 tile 仅 **0.03%**（tile 边界的数值边缘）、2 个 **61.2%**、
  3 个 **27.5%**、4 个 **11.3%**。极区（|lat|>85°）实测覆盖 2–3 个。
- 分辨率是**精确匹配**的，不是巧合：`1920 px / 90° = 7680 px / 360° = 21.33 px/度`。
  输出宽度 7680 就是 tile 中心的像素密度。
- **这是一条规则，不是一个常数**：母版宽度 = `tile_size × 360 / fov_deg`
  （本 rig 即 4× tile），由 `Rig.native_width()` 算出，**跟着源走，不写死**。
  1920 的 tile → 7680 母版；将来 UE 按 16K 渲染（3840 的 tile）→ 15360×7680 母版。
  比它高就是在编细节，比它低就是在对过采样的源做点采样（走样，见第 5 节「母版尺寸与交付尺寸」）。
  所以**母版尺寸由源决定，交付尺寸由规格决定，两者是两个独立的量**。
- `tools/coverage_map.py` 报的「相对采样密度」是 `cos³(离轴角)`：median 0.78、p5 0.43、min 0.355。
  **P4 更正了这个数的读法。** 它是「每个源像素占多大立体角，相对 tile 中心」——
  **越小表示源采样越密**（离轴处一个像素覆盖的立体角更小）。所以极区那个 0.355 的意思是
  最好的 tile 在那里比它自己的中心**密 2.8 倍**，不是「欠采样 2.7×」。
  原文那句「比输出网格欠采样约 2.7×」把这个量当成了源对输出的比值，是错的。
- **真正决定重采样滤波的是雅可比**，见下面「P4 实测：重采样到底在做什么」。
  极区确实是画质软肋，但机理是 equirect 网格在极点要求的密度任何针孔 tile 都给不出
  （次轴放大 16 倍），不是源本身采得太粗。

### P4 实测：重采样到底在做什么（决定滤波器的那组数）

`tools/resample_probe.py` 直接测映射的雅可比：一个输出像素往右/往下各走一格，
落在 tile 像素坐标里走了多远。两个奇异值就是两个主方向的缩放比。

**σ > 1 = 缩小（会走样，需要预滤波）；σ < 1 = 放大（需要好的插值核）。**
原生密度下（1920 tile → 7680 输出，即母版与 8K 交付走的那条路）：

| 纬度带 | σ_max（min/median/max） | σ_min | 各向异性 | 主轴在缩小 |
|---|---|---|---|---|
| 赤道 \|lat\|<30 | 0.79 / 0.92 / 1.28 | 0.71 / 0.82 / 1.02 | 1.14 | 27.7% |
| 中纬 30–70 | 0.79 / 0.84 / 1.03 | 0.30 / 0.54 / 0.85 | 1.51 | 0.1% |
| 极区 70–85 | 0.96 / 1.06 / 1.26 | 0.10 / 0.25 / 0.36 | 4.12 | 82.5% |
| 极冠 \|lat\|>85 | 1.10 / 1.17 / 1.43 | 0.00 / 0.06 / 0.13 | 20.0 | 100% |

**结论（推翻了 ROADMAP P4 原来的设计）**：

- **次轴在每一个纬度带都 < 1**，两个轴同时缩小的方向只占 **0.1%** ——
  这条路径上映射几乎处处在**放大**。放大时没有东西可以预滤波，
  所以原计划的「按局部缩放比选 mip 层级 / EWA」只会把画面变糊。
- 最近邻在这里的实际代价是**最多半个像素的位置误差**（块状复制），修它要的是**插值**。
- **各向异性才是极区糊的机理**：极冠 σ_min 中位数 0.06，即一个源像素在次轴方向被拉成
  约 16 个输出像素。这也说明极区的「多 tile 联合重建」有真实空间 ——
  在放大 4–16 倍的地方，2–3 个亚像素错位的 tile 是真的能补信息的。
- 反过来，**直接 warp 到更小的网格就落进缩小区**：3840 tile 直出 7680 时主轴 100% 在缩小
  （σ_max 中位数 1.84），这正是 P3 测到的直出 4K 走样（高频多 31%、帧间差多 5.1%）的机理。
  「按母版拼 + 编码时 Lanczos 降采样」把重采样交给一个真正带预滤波的缩放器，是对的。

复现：`python tools/resample_probe.py`（原生）、`--tile 3840 --output-width 7680`（16K 直出 8K）。

---

## 4. 数据布局

### 输入（只读）

```
E:\22\Camera{1..20}\Tempory\L_Cathedral.{frame:04d}.png
```

- 1920×1920，**8-bit RGBA**，PNG，单文件 3.6–5.5 MiB（平均 4.6 MiB，随画面复杂度变化）。
- **alpha 通道全为 255，没有信息** —— 25% 的字节是浪费的，读进来直接丢掉 alpha。
- 帧号现状：每个相机目录有 **779** 个文件 = 帧 `0000` + 帧 `1656..2433`。
  帧 `1..1655` 已被上一代流程消耗删除。`0000` 是留在那里的参考帧，**不在 1..2433 的正片序列里**。
- 序列总长 **2433** 帧。fps 未确认（见第 8 节）。
- PNG 里的 XMP 只有 SkyGuard DLP 水印，`Reason="UnrealEditor.exe"` —— 证明来源是 UE，
  **没有**相机内外参、帧率、色彩空间等有用元数据。别指望从 PNG 元数据里读配置。

### 参考输出（只读，质量基线）

```
E:\22\FinishTaskOutput\MonoEye.L_Cathedral.{frame:04d}.png
```

- 7680×3840（2:1）、8-bit RGBA、约 28 MiB/帧，帧 `0001..1655` 连续无缺口，共 43.41 GiB。
- 左右边界环绕连续（首末列平均差 1.15/255）。
- alpha 为 253..255（96.7% 是 255）—— 说明上一代流程做的是 alpha 加权混合，且有取整损失。
  **我们的输出应当输出不带 alpha 的 RGB，或者 alpha 恒为 255。**

### 待办工作量

| | 帧数 | 状态 |
|---|---|---|
| 帧 1..1655 | 1655 | 已完成（参考产物） |
| **帧 1656..2433** | **778** | **待处理，源数据在手** |

---

## 5. 交付规格（已确认 + 实测验证）

最终交付物是 **MP4**。以下参数已由用户确认，编码器行为已在本机实测。

### 用户给定的规格

| 参数 | H.264 | H.265 |
|---|---|---|
| Container | MP4 | MP4 |
| Codec | AVC / H.264（**默认**） | HEVC / H.265（可选） |
| FourCC | `avc1` | `hvc1` 优先 |
| Profile | High | Main |
| Pixel Format / Chroma / Bit Depth | `yuv420p` / 4:2:0 / 8-bit | 同左 |
| Frame Structure | **I + P only，B=0** | 同左 |
| GOP | 2 秒（30 fps → 60，60 fps → 120），**Open GOP 关闭** | 同左 |
| Audio | ~~AAC-LC 48 kHz~~ → **不带音轨** | 同左 |

- 输出尺寸：默认 **8k = 7680×3840**，可选 **4k = 4096×2048**（都是 2:1 等距圆柱）。
  4k 由 8k 母版 Lanczos 降采样得到 —— **实测依据见下面「母版尺寸与交付尺寸」**。
  （备注：`3840×1920` 是母版的整数 2× 降采样，重采样更干净；`4096×2048` 多 14% 像素、
  且 4096 是多数解码器的宽度上限。按用户指定用 4096×2048。）
- 帧率：默认 **30 fps**，可选。2433 帧 @30fps = **81.1 s**。
- 码率上限：单位是 **kbps（已确认）**。**每种尺寸一套档位，按像素数缩放（已确认）**：

  | 档位 | 8k 7680×3840 | 4k 4096×2048 | bit/像素 |
  |---|---|---|---|
  | 高（默认） | **200 Mbps** | **57 Mbps** | 0.226 |
  | 中 | **150 Mbps** | **43 Mbps** | 0.170 |
  | 低 | **100 Mbps** | **28 Mbps** | 0.111 |

  缩放系数是像素比 `(7680×3840)/(4096×2048) = 3.515625`，即**保持 bit/像素恒定**
  （实测两列对齐：8k 0.226/0.170/0.113 对 4k 0.226/0.171/0.111）。
  81.1 s 成品：8k 2.03/1.52/1.01 GB，4k 0.59/0.46/0.30 GB。
  代价与收益见下面的「4K 码率缩放的实测代价」。

- **不带音轨**（已确认）。源头是渲染帧，本来没有声音；规格表里的 AAC-LC 48 kHz 一行按此作废。
  编码时显式 `-an`，**不要**为了「形式上符合规格」去生成静音轨。

### 母版尺寸与交付尺寸（实测，2026-09-08）

**交付尺寸小于母版时，一律「按母版拼接 + 编码时重采样」，不要直接 warp 到交付尺寸。**

真实帧 1656/1657，`A` = 直接 4096×2048 LUT，`B` = 8K 母版经 `scale=4096:2048:flags=lanczos`：

| 量 | 直接 4K LUT | 8K 母版 + Lanczos |
|---|---|---|
| 两者之间的 PSNR | 43.89 dB（赤道 42.45 / 中纬 43.83 / 极区 48.00） | —— 影响量级与 4:2:0 那 4.3 dB 同级 |
| 升回 8K 后对母版的 PSNR | 41.84 dB | **44.36 dB（+2.52 dB）**，各纬度带一致（+2.34 ~ +2.65） |
| 亮度 mean \|Laplacian\| | 4.44（**多 31.3%**） | 3.38 |
| 相邻帧 mean 差（同样场景运动） | 2.919（**多 5.1%**） | 2.778 |
| 指标 A | median 0.67，**门 PASS** | 门 PASS |

- 直接版高频多 31%，却把母版重建得差 2.5 dB —— **那 31% 不是细节，是折叠进通带的走样**；
  多出来的 5.1% 帧间差就是它在头显里的表现（爬行/闪烁）。
- **指标 A 对走样完全无感**（多个 tile 在同一采样点互比，走样在 tile 之间是相关的），
  所以换采样网格时不能只看几何门。
- 实现：重采样**并进 RGB→YUV 那一个 scale 滤镜**
  （`scale=W:H:flags=lanczos:in_range=full:out_range=limited:out_color_matrix=bt709,format=yuv420p`）。
  分成两个 scale 会重采样两次；放在 `format=yuv420p` 之后会先把色度抽掉一半再平均。
  母版**不落盘**，只在 ffmpeg 里过一次。
- 代价：4K 按 8K 的价钱渲染（warp 的 LUT 从 20.9 M 条变 73.6 M 条），778 帧约 28 min 对约 15 min。
- 这条规则同时覆盖**「16K 渲染、8K 交付」**：母版 15360×7680（`Rig.native_width(3840)`），
  交付 7680×3840，代码路径与 4K 完全相同。注意 **16K 永远只能是母版** ——
  H.264 与 HEVC 都没有等级容得下 15360×7680，`select_level` 会直接拒绝把它当交付尺寸。
- 例外：`--stitch-at delivery` 保留直接 warp 到交付尺寸的快速预览档，**不要用于交付**。

### 实测：可用的编码路径

| 编码器 | 7680×3840 | 4096×2048 | 速度 (8K) | 2433 帧耗时 |
|---|---|---|---|---|
| `libx264` | ✅ | ✅ | 4.59 fps | **8.8 min** |
| `libx265` | ✅ | ✅ | 3.76 fps | **10.8 min** |
| `hevc_nvenc` | ✅ | ✅ | 很快 | — |
| `h264_nvenc` | ❌ `No capable devices found` | ✅ | — | — |

**H.264 是能输出 7680×3840 的** —— 用 `libx264` 软编，实测通过，High / Level 6.2 / `avc1` / B=0，
全长只要 8.8 分钟。编不了 8K 的只有硬件编码器 `h264_nvenc`。

**解码端：用户 2026-09-09 确认目标头显可以播 8K H.264。**
本文件原来写着「消费级硬件 H.264 解码器普遍卡在 4096×4096，8K AVC 只能软解，
头显不行」—— 那是通用经验，**不是这台设备的实测**，而用户已经在真机上确认能播。
所以：

- **8K H.264 是可交付的**，不再需要为了「能播」而降到 4K；
- **`--size 4k` 仍然留着**，但它的定位从「兼容性必需」变成「想要小文件时的选项」
  （4K 版文件小 72%，代价是画质降 0.67–0.89 dB，见下面「4K 码率缩放的实测代价」）；
- 这一条**只对这台设备成立**。换设备就是换一个未知，别把它当成普适结论。

**结论：编码不是瓶颈。**8K 全长编码只要 ~10 分钟，相比 P2 的拼接耗时可以忽略。
更重要的是 —— **拼接结果直接管道进 ffmpeg，就完全省掉了 0.68–2.77 s/帧 的 PNG 编码**，
而这正是第 8 节里识别出的吞吐瓶颈。

### level / tier：由规范表算出最小等级，不查表也不交给编码器

等级不是查表钉死的，而是由 `tools/encode_probe.py: select_level()` 按
H.264 Table A-1 与 HEVC Tables A.6/A.9 算出**最小合规等级**：画面尺寸、采样率、
码率三者各给一个下限，取最大。等级越低对解码器的要求越低，兼容性越好。
HEVC 优先 Main tier，只有 Main tier 无解时才退到 High tier。

**绝不能把等级交给编码器决定**（第 2 节约束 10）。

**24 种组合已经是自动化测试**（P7，`tests/test_conformance_matrix.py`）：
`{8k,4k} × {h264,h265} × 三档 × {30,60} fps`，**每一种都按自己的交付尺寸真编一遍再 probe** ——
尺寸必须是真的，因为等级正是由画面尺寸、采样率、码率三者算出来的。
逐项断言 codec / profile / level / tier / FourCC / `yuv420p` / `color_range=tv` /
`color_space=bt709` / **B 帧数 = 0** / 无音轨，并且断言 level 落在标准自己的表里。
整个矩阵 **44 s**（8K 最慢一档 2.45 s），所以它就在默认测试集里跑。

一条**故意的分工**：矩阵里每种组合只编几帧，因此只有一个 I 帧，说明不了 GOP。
GOP 结构在每个尺寸下都由同一组 `keyint` / `min-keyint` / `scenecut` 决定，
所以它在小尺寸上单独验（`test_the_gop_is_two_seconds_at_either_frame_rate`，30/60 fps ×
两种编码，各跑两个 GOP 多一帧；外加一条「硬切不得插入 I 帧」——`scenecut=0` 如果失效，
分段边界就不再落在 I 帧上，`-c copy` 的无损拼接也就不成立了）。
带真实帧、带实测码率的全长版本仍然是 `python tools/encode_probe.py matrix`（`--rates 30,60`）。

30 fps 下的结果（12 种组合已逐项 `ffprobe` 实测通过，
profile / level / FourCC / `yuv420p` / **B 帧数 = 0** / I 帧间隔 = 60 全部符合）：

| size | codec | 码率 | profile | level | tier | FourCC |
|---|---|---|---|---|---|---|
| 8k 7680×3840 | H.264 | 200 / 150 / 100 Mbps | High | **6.0** | — | `avc1` |
| 8k 7680×3840 | H.265 | 200 / 150 Mbps | Main | **6.2** | **Main** | `hvc1` |
| 8k 7680×3840 | H.265 | 100 Mbps | Main | **6.1** | **Main** | `hvc1` |
| 4k 4096×2048 | H.264 | 57 / 43 / 28 Mbps | High | **5.1** | — | `avc1` |
| 4k 4096×2048 | H.265 | 57 / 43 Mbps | Main | **5.2** | **Main** | `hvc1` |
| 4k 4096×2048 | H.265 | 28 Mbps | Main | **5.1** | **Main** | `hvc1` |

60 fps 下采样率上限抬高一档：8k H.264 → 6.1，4k H.264 → 5.2，H.265 各档不变。

**4K 码率缩放顺带解决了一个兼容性问题**：原来的 200/150 Mbps 会把 4K H.265 逼到
Main tier L6.2（给一个 4K 文件打 6.2 的标记会拖低兼容性），只能退到 High tier L5.2；
缩放到 57/43/28 Mbps 之后**全部落回 Main tier L5.1/L5.2** —— 这是 4K 解码器最普遍支持的档。
4K H.264 也从 L5.2 降到 L5.1，8K H.264 从 6.2 降到 6.0。

关键的规范数字：

- **H.264 MaxFS**：7680×3840 = 115,200 个宏块，超过 Level 5.2 的 36,864 → 8K 必须 6.0+。
  4096×2048 = 32,768 ≤ 36,864 → 4K 用 5.1 就够（30 fps 时 MaxMBPS 刚好等于上限 983,040）。
- **HEVC Main tier MaxBR**：L5.1 = 40、L5.2 = 60、L6.0 = 60、L6.1 = 120、L6.2 = 240 Mbps。
- **HEVC MaxLumaPs**：L6.2 上限 35.65 Mpx。7680×3840 = 29.49 Mpx 可以。
- **`hevc_nvenc` 的坑**：显式 `-level 6.1/6.2` 会被拒（`Invalid Level`），除非加 `-tier high`；
  auto 模式静默输出 Main tier L6.1 —— 200 Mbps 下不合规。
- **`h264_nvenc` 的坑**：4K + `-level 5.2` + 200 Mbps 会被拒；auto 给 L6.1。
  → **要规格精确可控就用 `libx264` / `libx265` 软编**，编码时间本来就可以忽略。

### 实测：验证过的 ffmpeg 参数

```
# H.265（推荐；Main profile / Main tier / L6.2，三档码率通用）
-c:v libx265 -preset medium -profile:v main -pix_fmt yuv420p
-colorspace bt709 -color_primaries bt709 -color_trc bt709 -color_range tv
-b:v 200000k -maxrate 200000k -bufsize 400000k
-x265-params bframes=0:open-gop=0:keyint=60:min-keyint=60:scenecut=0:level-idc=6.2:high-tier=0
-tag:v hvc1 -movflags +faststart

# H.264（同样的 GOP 结构；注意 8K 不可硬解）
-c:v libx264 -preset medium -profile:v high -level:v 6.2 -pix_fmt yuv420p
-colorspace bt709 -color_primaries bt709 -color_trc bt709 -color_range tv
-b:v 200000k -maxrate 200000k -bufsize 400000k
-x264-params bframes=0:open-gop=0:keyint=60:min-keyint=60:scenecut=0
-tag:v avc1 -movflags +faststart
```

4K 档在上面基础上加 `-vf scale=4096:2048:flags=lanczos`，码率换成 4k 档位，等级随之降低：

```
# 4k H.264，57/43/28 Mbps：level 5.1（30 fps；60 fps 要 5.2）
-level:v 5.1

# 4k H.265，57/43 Mbps：Main tier L5.2；28 Mbps：Main tier L5.1
-x265-params ...:level-idc=5.2:high-tier=0     # 57k / 43k
-x265-params ...:level-idc=5.1:high-tier=0     # 28k
```

所有档位都要加 **`-an`**（不带音轨）。

`keyint` / `min-keyint` 随帧率走：30 fps → 60，60 fps → 120。
**`-preset` 不能用 `ultrafast`** —— 它会把 H.264 降成 Constrained Baseline（第 2 节约束 10）。

### 实测：码率-质量曲线（真实 8K 母版 90 帧，PSNR 对无损 PNG）

| 配置 | 实际码率 | PSNR | MAE | p99.9 \|误差\| |
|---|---|---|---|---|
| H.265 200k | 208.6 Mbps | **43.06 dB** | 1.43 | 7.8 |
| H.265 150k | 156.5 Mbps | 42.91 dB | 1.45 | 8.0 |
| H.265 100k | 103.6 Mbps | 42.67 dB | 1.48 | 8.2 |
| H.264 200k | 197.6 Mbps | 42.98 dB | 1.43 | 8.0 |
| H.264 100k | 90.2 Mbps | 42.21 dB | 1.52 | 10.0 |

### 实测：4K 码率缩放的实测代价

4096×2048，H.265，参照物是同样 Lanczos 降采样的无损母版（90 帧）：

| 目标码率 | 实际 | bit/像素 | PSNR | 81.1 s 成品 | |
|---|---|---|---|---|---|
| 200 Mbps | 207.0 | 0.795 | 42.64 dB | 2099 MB | 缩放前 |
| 150 Mbps | 154.8 | 0.596 | 42.55 dB | 1569 MB | 缩放前 |
| 100 Mbps | 102.8 | 0.397 | 42.24 dB | 1042 MB | 缩放前 |
| **57 Mbps** | 58.2 | **0.226** | **41.97 dB** | **590 MB** | **缩放后（高）** |
| **43 Mbps** | 44.9 | **0.171** | **41.78 dB** | **455 MB** | **缩放后（中）** |
| **28 Mbps** | 29.3 | **0.111** | **41.35 dB** | **297 MB** | **缩放后（低）** |

**缩放的代价是 0.67–0.89 dB，换来文件小 72%，并且把 4K H.265 从 High tier 拉回 Main tier。**
（200→57 Mbps：−0.67 dB / −72%；150→43：−0.77 dB / −71%；100→28：−0.89 dB / −71%。）

**100 → 200 Mbps 只换来 0.39 dB（8K）/ 0.33 dB（4K）。码率不是画质瓶颈。**
4K 档尤其明显：200 Mbps 在 4096×2048 上是 **0.795 bit/像素**，
比 25 Mbps 也只好 1.55 dB —— 三档码率是按 8K 定的，套到 4K 上全都远过了拐点。
如果 4K 版是为了兼容性和体积，码率应该按像素数缩放（见第 9 节问题 3）。
H.264 与 H.265 在 200 Mbps 上基本打平（差 0.08 dB），只在 100 Mbps 上才拉开 0.46 dB。
→ 选 H.265 的真正理由是**能被硬件解码**，不是画质。

### 实测：4:2:0 vs 4:4:4（历史数字；这条的解释已被 P7 更正，见下两节）

同样 200 Mbps、同样 x265，只改色度格式。**这四行都是走 ffmpeg 的 `yuv420p → rgb24`
量出来的，而那条路自带约 −1.2/255 的偏置** —— 所以下面的差值可以比较，绝对值不要直接用：

| 变体 | PSNR | 平均直流偏移 |
|---|---|---|
| 4:2:0 limited range（**我们交付的这一档**） | 43.21 dB | **−1.23** |
| 4:2:0 limited + `+accurate_rnd+full_chroma_int` | 43.21 dB（**无改善**） | −1.22 |
| 4:2:0 full range（`-color_range pc`） | 43.55 dB（+0.34） | −1.16 |
| **4:4:4**（off-spec，仅诊断） | **47.54 dB** | **+0.05** |

- **full range 那 +0.34 dB 我们不要**：`full_range=True` 只作备用路径留在代码里，
  不暴露开关。理由是风险不对称，不是这 0.34 dB 不香 —— 见第 11 节。
  另外注意 limited **不是用户规格表里的要求**（那张表没写色彩范围），是广播惯例与 ffmpeg 默认。
- 本内容是高饱和的红/橙岩浆，色度细节密集，所以 4:2:0 的代价格外大。
- **ffmpeg 现成的开关救不回来**：swscale 的精确舍入标志毫无变化；
  `zscale` 需要精确的输入色彩标记，我试的两种链路反而更差（28 dB）。
- 按纬度分带看 PSNR（H.265 200k）：**极区 43.78 > 中纬 43.16 > 赤道 42.25**。
  极区压缩反而更「好」，因为它本来就是欠采样的糊图。
  **这说明极区问题在上游采样，不在编码器。**

### P7 实测：那 4.3 dB 的账，算清楚了（`tools/chroma_probe.py`）

**上面那张表里的 4.3 dB 和 −1.23 直流偏移，大部分不在交付文件里，而在我们量它的那条路上。**
两者都是让 ffmpeg 把 `yuv420p` 直接转成 `rgb24` 量出来的，而**那一步本身不中性**。

把编码器拿掉，只留颜色路径（8K 母版 8 帧，2026-09-08 实测）：

| 路径 | PSNR | MAE | 直流偏移 |
|---|---|---|---|
| 4:4:4，精确 BT.709 逆变换 | 52.07 dB | 0.40 | **+0.00** |
| 4:4:4，走 swscale | 52.07 dB | 0.40 | +0.00 |
| 4:2:0，精确逆变换（色度由 ffmpeg 升采样） | **49.70 dB** | 0.52 | **−0.02** |
| 4:2:0，走 swscale（历史上一直用的那条） | 44.25 dB | 1.29 | **−1.18** |

- **4:4:4 那两行完全相同** —— 这就是「精确逆变换」这套度量的自证：它和 ffmpeg 在没有色度
  重采样可做的时候一致到最后一个码字。所以下面两行的差不是我们算错了。
- **色度抽样本身只花 −2.38 dB，而且不带直流偏移。**
- **ffmpeg 的 `yuv420p → rgb24` 那一步花掉 −5.45 dB 和全部的 −1.17 直流偏移** ——
  是色度抽样代价的两倍多。而且它**与亮度无关**：按参考亮度分箱，0–16 / 16–48 / 48–96 /
  96–160 / 160–255 各箱的偏移是 −1.26 / −1.17 / −1.12 / −1.21 / −1.19，是个平坦的偏置，
  不是裁剪、不是插值误差。同一条链路在 `yuv444p` 输入下偏移为 0，所以问题**专属于
  ffmpeg 的 4:2:0→RGB 转换**（本机 ffmpeg 8.0）。
- 顺带一个反直觉的结果：**ffmpeg 的色度升采样是好的**。让它只做 420→444、矩阵留给我们，
  测得 48.01 dB（4k 上量的同一组），比最近邻升采样的 46.53 dB 还高 1.5 dB。坏的是矩阵那一步。

**这条对全项目的度量都成立**：第 5 节的码率-质量曲线（200 Mbps 43.06 dB 之类）都是走那条路量的，
因此**系统性地低估交付文件约 3.7 dB**。要比较不同码率/编码器仍然可以用它（偏置是同一个常数），
但**不要把那些绝对值当成交付画质**，也不要对外声称（第 9 节指标 C）。

**观众会不会看到那 1.2/255**，取决于播放器自己的 YUV→RGB：ffmpeg 那条路会，
头显里常见的 GPU shader 路一般不会。**这只能在真机上看**（第 11 节第 2 项）。

### P7 实测：自己做 4:2:0 值不值（结论：不做）

变体都用交付那一套 x265 参数（8K / h265 / 200 Mbps / 8 帧），只换 RGB→YUV 4:2:0 这一步。
左边是精确逆变换量的（该看的那一列），右边是走 swscale 量的（和历史数字可比）：

| 变体 | PSNR（精确） | 相对基线 | 直流 | PSNR（swscale） | 直流 |
|---|---|---|---|---|---|
| swscale（现状） | 46.87 dB | +0.00 | −0.05 | 43.16 dB | −1.22 |
| 自己做 / gamma 盒式 | **46.96 dB** | **+0.10** | +0.01 | 43.38 dB | −1.15 |
| 自己做 / **线性光**盒式 | 46.95 dB | +0.08 | +0.01 | 43.37 dB | −1.16 |
| 自己做 / 线性光 + 正确取样位置 | 46.60 dB | −0.26 | +0.01 | 43.23 dB | −1.16 |
| 自己做 / 再加抖动 | 45.81 dB | −1.05 | +0.00 | 42.83 dB | −1.16 |

- **线性光色度下采样：没有效果**（−0.02 dB 相对 gamma 盒式）。理由和 P4 那次否决同源：
  重叠 tile 之间只差半个灰阶，而在 2×2 块内做平均，编码空间与线性空间的差异按**差值的平方**增长。
  **这是第二次实测否决线性光了**，别再提第三次。
- **「正确的」取样位置反而更差 −0.26 dB。** 不是实现错了 —— 4k 上把下采样和升采样配对量过：
  盒式下采样 + 有位置感的升采样 **47.80 dB** 最好，位置正确的下采样 + 同样的升采样 46.86 dB，
  两种最近邻升采样 46.53 / 45.78 dB。**下采样只对配套的升采样最优**，而升采样在播放器手里。
  现状（盒式）恰好是实测里最好的那个配对。
- **抖动没有偏置可修**（我们自己的转换直流已经是 +0.01），所以它只是加噪，−1.05 dB。
- 自己做的 gamma 盒式确实比 swscale 好 **+0.10 dB** —— 但代价是管线要改喂 `yuv420p` 而不是
  `rgb24`，放弃「RGB 进、swscale 一次搞定重采样+矩阵 + 色度抽样」那条已经验证过可复现的路径。
  **0.1 dB 不值。判据是事先定好的（ROADMAP P7 第 4 项），照判据停手。**

复现：`python tools/chroma_probe.py --frames 8`（8K 约 15 分钟；`--size 4k --frames 2` 约 1 分钟）。

### 实测：编码器内存与 64 GB 软上限（P3）

**没有内存预算。软上限 64 GB，超出只打 Warning，不报错、不中断**（用户 2026-09-08 决定）。
`vr_compose.memory` 负责测量与告警，`vr-compose sequence` 每次跑完都打印。

**先说度量本身**：比较配置要看 **commit（已提交字节）**，不要看 resident（工作集）。
commit 是进程向系统要了多少、算在系统 commit limit 上的量，跟机器状态基本无关；
resident 是「当时有多少在物理内存里」，Windows 会在缓存压力下修剪它 ——
**同一条 8K 命令的 resident 实测在 6.5 / 8.6 / 14.8 GiB 之间跳过**，拿它比较配置会得出相反结论。

8K h264 200 Mbps，75 帧噪声走管线真实 stdin 路径，`tools/encode_probe.py memory` 可复现：

| x264 参数 | commit | resident | 喂入速率 |
|---|---|---|---|
| **默认（`threads=auto`，本机 48）** | **21.01 GiB** | 5.99 GiB | 7.32 fps |
| `threads=16` | 11.11 GiB | 5.63 GiB | 4.92 fps |
| `threads=8` | 8.59 GiB | 5.64 GiB | 3.23 fps |
| `threads=4` | 7.38 GiB | 6.02 GiB | 2.12 fps |
| `sliced-threads=1:threads=16` | **7.93 GiB** | 5.75 GiB | 5.06 fps |
| `sliced-threads=1:threads=8` | 7.13 GiB | 5.79 GiB | 3.40 fps |

| x265 参数 | commit | resident | 喂入速率 |
|---|---|---|---|
| 默认（`pools=auto`） | 8.29 GiB | 7.22 GiB | 4.94 fps |
| `pools=16` | 7.50 GiB | 6.48 GiB | 4.12 fps |
| `pools=8` | 6.83 GiB | 6.09 GiB | 2.61 fps |
| `frame-threads=2` | 6.99 GiB | 5.95 GiB | 4.39 fps |
| **`pools=8:frame-threads=2`** | **6.49 GiB** | 5.76 GiB | 2.58 fps |

- 吃内存的是**帧级并行**：每个 frame thread 要自己持有 8K 的参考帧与半像素插值平面。
  x264 的 commit 就是按帧线程数走的（21 → 11 → 8.6 → 7.4）。
  切成**片级并行**后与线程数解耦，`sliced-threads=1:threads=16` 是 commit/吞吐最优的一档。
- `-filter_threads 1` 实测无效（6.75 → 6.76 GiB resident）：**swscale 不是贡献者**，别在这上面花时间。
- **`sliced-threads` 不能当 `--deterministic` 的替代品** —— 跑两遍比哈希不一致。已排除。
- **默认不动 `threads=auto`**：21 GiB 远在软上限内，而它同时是最快的一档。
  内存紧的机器用 `--encoder-threads N`：x264 走 `sliced-threads=1:threads=N`，
  x265 走 `pools=N:frame-threads=2`。这只是脚印/吞吐旋钮，**不影响确定性**。
- 管线只需要 **0.48 fps**（2.1 s/帧），上表最慢的一档也有 4 倍余量 —— 所以这里不必心疼编码器速度。

### 编码器的确定性（P2 实测，决定「续跑逐字节一致」能承诺什么）

同一批帧喂两次，比较 elementary stream 与 MP4 的 SHA-256：

| 路径 | x264 | x265 |
|---|---|---|
| ffmpeg 自己读 PNG + lanczos 缩放再编码 | **不一致** | 不一致 |
| **管线真实路径**：stdin 送 raw RGB，只做 RGB→YUV | **一致**（60 帧，4K 与 256×128 都一致） | 不一致 |
| 短片段（10 帧尾段）或显式 `threads=8` | **不一致** | — |
| x264 `threads=1` | 一致，编码器慢 **6.6×**（15.0 → 2.3 fps @4K） | — |
| x265 `frame-threads=1` | — | 仍不一致 |
| x265 `frame-threads=1:wpp=0` | — | **一致**，慢 **7.7×**（10.8 → 1.4 fps @4K） |

- ffmpeg 自己的 PNG 解码 + 缩放路径不可复现（多线程解码/切片缩放），所以
  `tools/encode_probe.py` 这类以 PNG 为输入的实验**不能拿文件哈希比较**；管线不走那条路。
- MP4 容器默认写 `creation_time` 和编码器版本串，每次都不同。管线对分段和 concat 都加了
  `-fflags +bitexact -flags +bitexact`，容器层就稳定了（x265 deterministic 模式下续跑逐字节
  一致，证明的正是这一点）。
- 因此对「续跑」能承诺的是：**默认模式下，续跑得到的文件与不中断的那份结构相同**
  （帧数、GOP、合规性全部一致）；x264 在足够长的分段上通常也逐字节一致，**但末尾短分段不保证**。
  `--deterministic` 把 x264 钉到 `threads=1`、x265 钉到 `frame-threads=1:wpp=0`，两种编码都
  逐字节一致，代价是编码器慢 6.6–7.7×（8K 下会反过来成为瓶颈）。
- 反直觉的一点：**给 x264 显式指定 `threads=N` 反而破坏确定性**（`threads=8` 在 128 行的帧上
  不一致）。别为了「稳定」去钉线程数，除非钉到 1。

### 球面元数据（P7 已实现，`vr_compose.spherical`）

- **ffmpeg 无法写入** spherical/VR 元数据（只有解码侧的 side data）；PyPI 上也没有可用包
  （`spatialmedia` / `spatial-media` 都不存在）。所以**自己写** `sv3d` box ——
  Spherical Video V2 规范就是几个定长 box，2017 年之后没变过，无依赖、可审计、可逐字节断言。
- 写在**视觉采样条目**里（`avc1` / `hvc1`），和 `avcC` / `hvcC` 同级：

  ```
  moov/trak/mdia/minf/stbl/stsd/avc1
      st3d                 立体布局（0 = 单眼）
      sv3d
          svhd             谁写的
          proj
              prhd         姿态 yaw / pitch / roll
              equi         等距圆柱 + 边界
  ```

  默认元数据固定 **104 字节**（有测试钉住这个数字：变了就是规范变了）。
- **最容易写错的一条**：`moov` 长大之后，它后面的所有东西都要往后挪，而
  `stco` / `co64` 里存的是**绝对文件偏移**。我们的文件是 `+faststart`（`moov` 在前），
  所以每个 chunk 偏移都要加上插入的字节数。漏了这一步，文件**照样能解析、照样报出元数据、
  照样显示时长**，但解出来是花的或者什么都没有。`inject()` 因此把所有指向插入点之后的
  chunk 偏移都平移一遍 —— 这条规则对 `mdat` 在前的布局同样正确（没有偏移指向插入点之后，
  于是一个都不动）。测试用**解码后的 md5** 守这一条，两种布局都验。
- **交叉验证用 ffmpeg 的 `mov` demuxer**（一个我们没写的实现）：注入后
  `ffprobe -show_streams` 报 `Spherical Mapping / equirectangular` 与 `Stereo 3D / 2D`。
  `encode.probe()` 把它读进 `StreamInfo.projection`，交付文件的合规检查断言它等于
  `equirectangular`（分段不检查 —— 分段是中间产物，`-c copy` 也不会把 box 带过 concat，
  所以元数据只写**合并后**的成品）。
- GPAC（`winget install GPAC.GPAC`）本机**没装**，所以 MP4Box 那条交叉验证还没做；
  ffprobe 那条已经代替它起作用。真要装再加一条独立验证也行。
- 老文件补写：`vr-compose metadata <file> [--write]`。就地改，已带元数据的不会写第二遍。

### 音频

**不带音轨**（已确认）。显式 `-an`，并且 `conformance_problems` 断言产物里
**没有音频流**（24 种组合都验）—— 不为了「形式上符合规格」去生成静音轨。

---

## 6. 来源目录：可选，且按结构自动发现

来源目录是**参数**，不是常量。`E:\22` 只是「一种符合约定的目录」的样本，
不是唯一形态 —— 后续项目的相机数、内层目录名、文件 stem 都可能不同。

### 识别的结构

```
<来源目录>/
  Camera1/[<任意一层子目录>/]<stem>.<帧号>.png
  Camera2/[...]
  ...
  CameraN/[...]
  FinishTaskOutput/            (可选；也接受 output / out)
```

**全部靠推断，不靠硬编码**（`tools/discover_source.py`，7 项合成用例 + 真实数据已验证）：

| 项 | 怎么来的 |
|---|---|
| 相机数 | 匹配 `Camera<数字>` 的目录个数（大小写不敏感，允许 `Camera_1` / `Camera 1`） |
| 内层目录 | 往下最多找 1 层，谁装着 `<stem>.<数字>.png` 就是它。`Tempory` 这个拼写不作假设 |
| 文件 stem | 从文件名解析。**不同 stem 算不同的集合**，例如不同场景、版本或命名前缀 |
| 帧范围 | 各相机帧号取交集（只有全都有的帧才可处理） |
| tile 尺寸 | 读 PNG 的 IHDR，不解码 |

### 必须报错而不是猜的情况

- 相机编号不连续（不是 `1..N`）
- 各相机 tile 分辨率不一致
- **tile 不是正方形** —— 反解出的 rig 假设方形 90° FOV，长方形说明是另一套装配
- 各相机帧数不一致（不要偷偷取交集就往下跑）
- 同一目录里发现多个集合（多个文件 stem / 多个场景）→ 必须由调用方选，**绝不处理「第一个匹配」**

### 没给来源目录时去哪找

按顺序：exe 自身所在目录 → 它的各个直接子目录 → 上一级目录的各个直接子目录。
第一个能通过校验的就用。冻结后 `sys.executable` 是 exe 本身，所以「exe 同级」就是它的父目录；
从源码跑时等价于工程根目录。

找不到就**明确报错并列出搜过的位置**，不要静默退化成某个默认路径。

### rig 与来源目录的关系（重要）

第 3 节那套 rig（5 扇区 × 3 仰角 / 90° 方形 FOV）是**针对这一套 20 路装配反解出来的**。
换一个来源目录、相机数不是 20，那套映射就不成立。所以：

- rig 必须做成**配置**（按相机数/布局索引的注册表），随 exe 一起发布；
- 遇到未登记的布局要**明确拒绝**，并提示可以用 `tools/fit_rig.py` 配合一份参考全景去反解；
- 现在注册表里只有一条：20 路 → 第 3 节的映射。
- **6 路装配已提出（用户 2026-09-09），记为未来可做，先不做。** 开工条件与预见的技术点
  （零重叠立方体的边界可见性、几何自检在无重叠时要跳过）在 ROADMAP「未来可做 — 6 相机 rig」。
  没有样本 + 参考全景（或 UE 相机设置）之前不登记，登记了也是猜。

---

## 7. GUI 与打包（已实测）

PySide6 + PyInstaller，Windows exe。以下数字来自 `tools/package_probe.py`
（真打了三种配置，跑起来验证过）。

### 入口文件与「一个 exe，两副面孔」

打包入口是根目录的 `main_ui.py`（用户 2026-09-08 要求，与 VR-Installer 的布局一致），
`.spec` 指向它。它不只是个 `import` 转发，有三件事只能在这里做：

- **没有参数 = 开窗口，有参数 = 当 CLI。** 冻结后只有一个可执行文件，
  而窗口跑作业的方式是**启动自己**（`gui.window.worker_command()` 冻结时返回
  `[sys.executable]`）。所以这个 exe 必须两样都是。
  **改掉这条规则等于让打包版的「开始」按钮再开一个窗口。**
- **`multiprocessing.freeze_support()` 是 `__main__` 下第一条语句**（第 2 节约束 4）。
  管线现在用线程，所以还没有东西会触发它；这一句照样放在最前面，
  因为它的失败形态是一个带着应用图标的 fork bomb，等有人加进程池时再补就晚了。
  有测试直接读源码断言它是第一条。
- **src 布局**：`vr_compose` 在 `src/` 下，所以它在 `src/` 存在时把它加进 `sys.path`，
  未安装的检出也能 `python main_ui.py` 跑；冻结后没有 `src/`，那一步自动不生效。

### 打包方式选型

`tools/package_probe.py` 用一个探针应用比过三种配置：

| 配置 | 分发体积 | 冷启动 | **热启动** | 构建 |
|---|---|---|---|---|
| onedir | **158 MB**（整个目录） | 8–11 s | **0.86 s** | 74–100 s |
| onefile | **64 MB**（单文件） | 12–14 s | **5.88 s** | 84 s |

**结论：发 onedir。** onefile 省下的 94 MB 下载量，代价是**每次启动都多花 5 秒**
（它每次都要把整个归档解到临时目录）。热启动 0.86 s 对 5.88 s，差 7 倍。
冷启动那 8–14 s 主要是首次杀毒扫描，不代表日常体验。

### 真打出来的数字（P8 完成，2026-09-08）

上表是探针应用；下表是**本项目**按 `VR-Compose.spec` 真打出来的结果，
命令是 `python tools/build_exe.py --ffmpeg C:/ffmpeg/bin --source E:/22`：

| | 实测 |
|---|---|
| 应用本体（onedir，277 个文件） | **167.5 MiB**（176 MB） |
| `ffmpeg.exe` + `ffprobe.exe` | 370.3 MiB |
| **分发包合计** | **537.8 MiB**（564 MB） |
| 构建耗时 | 74 s |
| `--version` 冷启动 / 热启动 | 3.15 s / **0.37–0.57 s** |
| 冻结产物与源码运行 | **逐字节一致**（两帧 PNG 母版 sha256 相同，构建脚本每次都验） |
| 随包中文说明 | `使用说明.md` + `使用说明.pdf`（A4 4 页，436 KiB，构建时现排，不入库） |

比探针的 158 MB 多出的约 18 MB 是 numpy + Pillow + 本项目自己的代码 ——
**探针的数字不能直接当分发体积用**，这一栏才是。

### onefile 又量了一次（用户 2026-09-08 问「能不能打成单文件」）

真打了本项目的 onefile 版（`EXE` 吞下 binaries/datas，无 `COLLECT`），
产物能跑，**渲染结果与 onedir 逐字节一致**（两帧 sha256 相同）。数字：

| | onedir | onefile |
|---|---|---|
| 应用体积 | 167.5 MiB | **66.6 MiB** |
| `--version` 首次 / 之后连续三次 | 0.61 / 0.39 0.36 0.35 s | 4.98 / **3.60 6.13 6.68 s** |
| 每次启动向 `%TEMP%` 解包 | 无 | **124 MiB / 241 个文件** |
| 构建 | 74 s | 128 s |
| **压缩后**（zip deflate） | **67.2 MiB** | **66.3 MiB** |

**下面这段结论已经作废两次，留着只为存住数字。**
用户 2026-09-08 看过之后仍然要单文件，2026-09-09 又明确
**分发包大小不重要、P8 只要能打开能跑、不用管解包和启动时间** ——
也就是说下面每一条「代价」都不再是判据了。**不要再拿它们回来劝一遍。**
数字本身仍然是实测事实，可以引用；结论不要引用。

~~结论：仍然发 onedir，而且这次的理由比体积更硬。~~

- **那 101 MiB 的体积优势压缩之后就没了**：67.2 对 66.3 MiB，差 0.9 MiB。
  onefile 省下的就是 PyInstaller 自己做的压缩，而分发本来就要打 zip。
- **代价是每次启动 3.6–6.7 s 对 0.35–0.39 s**，10–19 倍。数字会跳是因为
  每次启动都要把 124 MiB 解到 `%TEMP%` 再被杀毒扫一遍。
- **它还留垃圾**：几次测试之后 `%TEMP%` 里剩了 3 个 `_MEI*` 目录、248 MiB
  没被清掉（正常退出会清，被杀掉就不会）。
- **它并不真的是「一个文件」**：`encode.find_tools()` 在**可执行文件同级**找 ffmpeg，
  干净机器上又没有 PATH 里的 ffmpeg，所以 `ffmpeg.exe` + `ffprobe.exe`（370 MiB）
  照样得摆在旁边 —— 一共 3 个文件。把它们也塞进 onefile 就是每次启动解包约 500 MiB。
- **双面孔设计会再吃一遍**：窗口是靠启动自己来跑作业的，所以每次开始渲染时子进程
  又解一次包（作业要跑两小时，这一次不重要，但同时有两份 124 MiB 的解包目录）。
- 还有一条环境风险：有些机器（AppLocker 之类）禁止从 `%TEMP%` 执行，onefile 直接跑不起来。

**要「一个文件」就打 zip**：整个 `dist/VR-Compose/` 压成 **212 MiB** 的 zip（28 s），
一个文件、可以随便拷，而且不用付上面任何一项代价。

### 但用户看过上面这些之后，仍然要单文件（2026-09-08）

**所以两种形态都支持，`tools/build_exe.py --onefile` 出单文件。** 这是用户的决定，
上面那些数字都摆给他看过了，不是没想清楚 —— 别再拿它们回来劝一遍。

两种形态的差别**只在 ffmpeg 放哪儿**：

| | 文件夹版（默认） | 单文件版 `--onefile` |
|---|---|---|
| 产物 | `dist/VR-Compose/` 538 MiB | `dist/onefile/VR-Compose.exe` **210.1 MiB** |
| ffmpeg | exe 同级 | **打进包里**，运行时从 `sys._MEIPASS` 找 |
| 拷贝时要带的东西 | 整个文件夹 | 就那一个文件 |
| 每次启动解包 | 无 | 约 494 MiB（124 应用 + 370 ffmpeg） |

`.spec` 用 `VRC_ONEFILE` / `VRC_FFMPEG_DIR` 两个环境变量分流，由构建脚本设置；
直接手跑 `.spec` 得到文件夹版（安全的默认）。运行时那一半在
`encode._search_dirs()`：冻结后先看 **exe 同级**，再看 `sys._MEIPASS`。
顺序是故意的 —— 这样用户仍然可以把自己的 `ffmpeg.exe` 放在 exe 旁边覆盖掉自带的那份。

**一个没量、而且不用量的数**：单文件版**带 ffmpeg** 之后的启动耗时。
未打包 ffmpeg 的单文件版量到 3.6–6.7 s（解 124 MiB），带上 ffmpeg 是约 4 倍的解包量 ——
**别把这个外推当实测写进文档**。用户 2026-09-09 明确
**P8 只要能打开能跑，不用考虑解包和打开时间**，所以这个数不必补了。

**单文件版双击能开出窗口 —— 已验（2026-09-09）**：双击后是 bootloader + 子进程两个进程，
Qt 窗口（`Qt6112QWindowIcon`，标题 `VR-Compose  0.1.0`）由子进程持有。
**同时也发现了一个真问题并修掉了：那次双击在窗口后面留了一个可见的控制台窗口。**
原因和修法见下面「双击不该有黑窗口」。

**没验、也不必验的那一条**：窗口按「开始」时**子进程会不会再解包一遍**。
PyInstaller 会给子进程传 `_PYI_*` 环境变量来复用父进程解包目录（`multiprocessing`
在 onefile 下就是靠这个），但**本项目没有实测过**，所以不要在文档里当成事实陈述 ——
不过它属于「解包耗时」那一类，而用户 2026-09-09 已经把这一类整体划掉了。
**要紧的那一半反倒验过了**：按「开始」那条路用真 exe 跑通了（管道里 NDJSON 完整、
两帧 PNG 产出、退出码 0），也就是说「会不会再解包」只影响快慢，不影响能不能跑。
GUI 面孔的启动耗时同样没有测（没有可靠的「窗口已出现」信号），不要编一个数字。

### 双击不该有黑窗口：单文件版 windowed，文件夹版留控制台（2026-09-09 实测）

`.spec` 里是 **`console=not ONEFILE`** —— 两种形态在这一项上**故意不同**，两边都有实测理由。

**单文件版 `console=False`。** 之前是 `console=True` 加「启动时自己把控制台藏起来」，
**在单文件版上不成立**：控制台是 bootloader 创建的，早于本项目任何代码，
而藏它的代码要等 494 MiB 解包完才跑得到。
A/B（70 MiB 替身程序，每 0.4 s 采样桌面上的控制台类窗口）：

| 形态 | 桌面上出现过的控制台窗口 |
|---|---|
| `console=True`，不藏（= 用户看到的样子） | Windows Terminal **1129×635，一直可见** |
| `console=True`，藏（第一版修法） | 同一个窗口**可见约 4 秒**再最小化，**还留任务栏按钮** |
| **`console=False`** | **一个都没有**（25 次采样；真实 210 MiB 包 75 次采样也是零） |

**文件夹版留 `console=True`**，双击时由 `main_ui._hide_own_console()` 藏掉。
它 0.35 s 启动、而且只有一个进程，所以控制台**闪约 1–2 秒**（实测采样 2→5）再最小化 ——
**不是零，和改动前一样**（老判据在单进程下本来就成立）。留着它换来的是
**PowerShell 会等一个控制台程序**：windowed 的 exe 在 PowerShell 里**裸跑**
`VR-Compose.exe --version` **既不打印也不给退出码**（实测），因为 shell 不等
GUI 子系统的进程；走管道或 `Start-Process -Wait` 正常（实测过）。**交互式 `cmd` 也不等**（用户 2026-09-09 在另一台机器上实测：两条 `frame` 命令敲完立刻回到提示符，什么都没打印，图在后台慢慢出来）；批处理文件里的 `cmd` 才等。
**所以：要写脚本用文件夹版或源码，要双击用单文件版。**

**一条反直觉的事实**（推翻本文件原来的说法）：windowed 构建**不会**必然让
`sys.stdout` 变成 None —— **Windows 不管子系统都把新进程挂到父进程的控制台上**，
子系统只决定「没有父控制台时是否新建一个」。所以两副面孔都完好，实测：

- 从终端跑 CLI 面孔 → 打印在那个终端里；
- 窗口用 `QProcess` 给管道 → NDJSON 完好（**这是最要紧的一条**，进度就靠它）；
- ~~`cmd` 依然等它结束~~ **不对**：那次是在批处理里量的。交互式 `cmd` 对 GUI 子系统程序立刻返回，而且没有可用控制台时输出进空设备——用户 2026-09-09 亲眼见到「没有生成图」，其实是还在后台算。**命令行一律用文件夹版**，或重定向到文件再等。

`main_ui._attach_parent_console()` 只兜「完全没有流」那种情况（detached 启动、
或从资源管理器带参数启动）：先借父控制台，借不到就把流指到 `os.devnull`，
免得 CLI 面孔第一个 `print` 就抛 `AttributeError`。
windowed 的副作用是好的那种：未捕获异常弹**对话框**，而不是随控制台一起消失。

**排查用的钩子**：`_hide_own_console()` 会把它看到的东西写进
`%VRC_CONSOLE_LOG%`（设了才写）。这条通路只在**打包后**才有意义、而且整段吞异常，
没有这个日志的话，判断错了是完全无声的。

### 两个和直觉相反的实测结果

1. **不需要手写 Qt 模块排除列表。** PySide6 装完 632 MB，但 PyInstaller 的 PySide6 hook
   已经只打包实际 import 到的模块 —— 加了 47 条 `--exclude-module` 之后体积**一点没变**
   （158 MB → 158 MB）。别在这上面浪费时间。
2. **冻结后的进程池是可用的**，前提是第 2 节约束 4 那句 `freeze_support()`。
   三种配置都实测跑通了 `ProcessPoolExecutor`，没有递归重启 GUI。

### ffmpeg 随 exe 分发（已确认）

**ffmpeg 与 ffprobe 都打进分发包**，放在 exe 同级。目标是**解压即用，不需要用户装任何东西**。

| 组成 | 体积 |
|---|---|
| onedir 应用本体 | 167.5 MiB（实测，见上一小节） |
| `ffmpeg.exe`（静态全功能构建） | 185.2 MiB |
| `ffprobe.exe` | 185.1 MiB |
| **合计** | **537.8 MiB** |

查找顺序：**exe 同级的 `ffmpeg.exe` 优先，其次 PATH。** 这样默认自带，
但需要时用户仍可以换成自己的构建。启动时检查可用性与必需编码器
（`libx264` / `libx265`），缺了立刻给明确提示，而不是跑到一半失败。

`ffprobe` 也带上：它是交付合规校验（P7 的 24 项断言）用的，不带的话产品自己没法自检。

**许可**：带 libx264 / libx265 的 ffmpeg 构建是 GPL。此事已提出，
**用户明确决定不处理 —— 这不是商业软件**。记录在此，不再作为待确认项。

### GUI 架构要求（P6 已实现）

**实现落点**：`src/vr_compose/gui/`（`window.py` + `main_window.ui` + `style.qss`），
入口 `vr-compose-gui` / `python -m vr_compose.gui`。
样式与加载方式参考 `D:/VR-DEV/Python/VR-Installer/ui`（用户指定）：
`.ui` 由 `QUiLoader` 运行时加载、`.qss` 用 `setStyleSheet`、`findChild` 按 objectName 绑定、
`variant` / `status` 动态属性承载样式状态。

**GUI 与引擎之间只有一条界面：CLI 的两个选项。** GUI 不 import 管线：

- `--progress-json`：stdout 只走 NDJSON（`start`/`progress`/`log`/`done`/`cancelled`/`error`），
  人类可读的行改走 stderr。**改动这些事件的字段名等于改了 GUI 的接口**，
  `tests/test_gui.py` 里有不依赖 Qt 的断言守着。
- `--cancel-on-stdin`：写一行即取消，**EOF 也算取消**（父进程没了）。
  它设置的就是管线原本接受的那个 `threading.Event`，所以取消的保证没有第二套。

窗口用 `QProcess` 在事件循环上读这条流，所以 GUI 侧没有任何线程。



- **GUI 只能是薄壳。** 它必须调用 CLI 走的同一套代码，不允许出现第二份逻辑。
  先有可用的 CLI，GUI 包在外面。
  **具体到「不许漂移」**：GUI 的下拉框内容来自 `encode.SIZES` / `encode.LADDER` /
  `stitch.SAMPLERS` / `stitch.BIT_DEPTHS`，不在 `.ui` 里写死；有测试断言两边一致。
- **长任务绝不能跑在 GUI 线程上。** 推荐把整个作业放到**独立子进程**里跑，
  GUI 通过管道读进度 —— 而不是只开个 QThread。这样既隔离崩溃，也避开
  Qt 与 multiprocessing 互相干扰的问题（P2 本来就有进程池和 ffmpeg 子进程）。
- 进度要显示：当前帧 / 总帧、s/帧、ETA，并与第 8 节的 46.85 s/帧 基线可比。
- 必须能取消，且取消后作业保持可续跑（第 2 节约束 8）。

---

## 8. 性能基线（同机实测，是所有优化的比较基准）

硬件：AMD Ryzen 9 7950X（16C/32T）、128 GB RAM、RTX 4090、
`E:` = Samsung 870 EVO SATA SSD、`D:` = Kingston NVMe。

**上一代流程：`46.85 s/帧`**（1655 帧耗时 21.54 小时墙钟，由输出文件 mtime 跨度算出）。
按这个速度，剩余 778 帧还要 ~10 小时，双眼全长要 ~63 小时。

单线程 I/O 与计算成本实测：

| 操作 | 耗时 |
|---|---|
| 解码 1 个 1920×1920 tile | 54 ms（15 个 = 0.81 s） |
| 解码 1 个 7680×3840 PNG | 0.42 s |
| 编码 7680×3840 PNG，compress_level=6 | 2.77 s |
| 编码 7680×3840 PNG，compress_level=1 | 0.68 s |
| numpy 最近邻重投影 15 tile → 3840×1920 | 4.7 s（含每帧重新算射线，可预计算掉） |
| 单帧磁盘吞吐 | 读 68 MiB（15 路）+ 写 27 MiB ≈ **95 MiB**（SATA SSD 上约 0.2 s） |

**判断：上一代流程比 I/O 下界慢 15–25 倍，瓶颈不在数据量上。** 重投影是**固定几何**，
可以完全预计算成一张静态 LUT（或者一次性传到 GPU），每帧只剩「解码 → gather+blend → 编码」。

**而且既然交付物是 MP4，中间那层 PNG 编码（0.68–2.77 s/帧）应该整个消失** ——
拼接结果以 rawvideo 直接管道进 ffmpeg。这样单帧成本只剩「15 个 tile 解码（可并行）+ LUT gather」，
而 8K 编码全长只要 ~10 分钟（第 5 节实测）。无损母版 PNG 序列改为**按需产出**（QA 抽帧、归档），
不再是必经环节 —— 顺带也省掉每只眼 64 GiB 的落盘。

---

### P2 实测：序列管线（8K，h264 高档，120 帧，同机）

| 项 | 值 |
|---|---|
| **端到端** | **2.17 s/帧**（上一代 46.85 → **21.5×**） |
| 分解 | 等解码 0.01 s、**warp 1.84 s**、几何门 0.03 s、写管道 0.06 s |
| **778 帧实跑** | **~26 min，2.10 s/帧（22.3×）**，全长平均 **196.9 Mbps**（≤ 200 上限），`conformance: OK`，8 个采样帧几何门 PASS |
| LUT | 8K 73.6 M 条贡献、843 MiB、构建 9.7 s；输出与 P1 逐帧逐字节一致 |
| warp 单独 | 1 线程 3.7 s → 4 线程 1.7 s → 8 线程 1.7 s（16 线程回退到 2.5 s） |

**两个调参结论**（靠 `stages` 分解定位，不是猜的）：

1. 解码开 8 线程时 warp 从 1.7 s 变成 2.8 s —— 不是解码慢（等待只有 0.02 s），而是解码线程
   与 warp 线程在 numpy 调用之间争 GIL。解码降到 **4** 线程、warp 升到 **8** 线程后，
   warp 回到 1.84–1.99 s。**线程总数比单项各多少更重要。**
2. 解码放在线程而不是进程里：Pillow 解 PNG 时释放 GIL，15 个 tile 4 线程 0.3 s，完全藏在
   warp 后面，而且不用跨进程搬 166 MB/帧。`freeze_support()` 约束因此暂无触发点，但保留。

`stages` 分解在每段结束时打印，`vr-compose sequence` 结尾也汇总；以后任何吞吐回归先看它。

### P3 实测：整机内存峰值（8K，h264 高档，同机）

`vr-compose sequence` 自己测自己（`vr_compose.memory`，两侧都取进程的 commit 高水位）：

| 配置 | commit 峰值 | 分解 | resident 峰值 | s/帧 |
|---|---|---|---|---|
| 60 帧 | **23.8 GiB** | 拼接 3.7 + 编码 20.1 | 10.5 GiB | 2.19 |
| **180 帧** | **23.8 GiB** | 拼接 3.7 + 编码 20.1 | 10.5 GiB | 2.24 |
| 180 帧 + `--encoder-threads 16` | **9.8 GiB** | 拼接 3.7 + 编码 **6.1** | 7.0 GiB | 2.33 |

**帧数翻 3 倍，峰值一位小数都没变** —— 这就是「峰值与总帧数无关」的实测证据
（第 2 节约束 7 要求的按帧流式处理确实成立）。23.8 GiB 相对 64 GB 软上限有 2.7 倍余量。

`--encoder-threads 16` 在真实作业上把峰值降 **59%（23.8 → 9.8 GiB）**，
代价只有 **4% 吞吐（2.24 → 2.33 s/帧）**。编码器那份 6.1 GiB 比噪声探针的 7.93 GiB 还低 ——
真实画面要缓冲的比特比纯噪声少，所以探针的数字是保守的上界。

- 报的是**两侧峰值之和**，是真实同时峰值的**上界**（两者的高水位不一定同一瞬间出现）。
  告警宁可偏高。
- 拼接侧的 3.7 GiB 与预期一致：843 MiB LUT + colour/weight/image 缓冲 + 两帧 tile + 每 band 的
  gather 临时量。**注意它是「本进程生命周期」的高水位**，所以在长驻进程里（GUI、pytest）
  会包含这个进程干过的其它事 —— 单次 CLI 运行才等于这次作业。
- 16K 母版的估算：拼接侧约 10–12 GiB（LUT 约 3.4 GiB）+ 编码 20 GiB ≈ **30 GiB**，仍在软上限内。
  真有 16K 数据时要实测。

### P9 实测：光度和谐化（`vr_compose.harmonise`，默认开，2026-09-10）

**做什么**：每个 tile 与全体共识之间的**平滑**亮度差在混合前扣掉，只动低频。起因与诊断见第 9 节
「两种失败签名」和 ROADMAP P9：v004 渲染的体积雾/光柱/运动模糊按视口计算，相邻相机差 4–8 级，
上游不可改。

**怎么做**（常量都在模块顶部，每个都有实测理由）：

| 参数 | 值 | 为什么 |
|---|---|---|
| 校正网格 | 120×60（3°/格），与输出分辩率无关 | 4K 与 8K 得到同一组校正；粗到只剩雾这一级的差异 |
| 估计用的小 LUT | **240** 宽 bilinear | 960/480/360/240 的结果到每个指标的 0.01 都一样（细节由网格与模糊决定，不由样本数决定），耗时 1.05 → 0.48 → 0.38 → **0.30 s** |
| 截断 | ±32 级 | 帧 100 的雾楔需要 24–32；24 时被截，重叠区 std>8 留 2.0%，32 时 1.0% |
| 模糊 | 盒式 ×3，半径 3 格（约 9°） | 半径 3：std>8 1.04%、成品 3.6% 像素动 >2 级；半径 4：1.09% / 6.3%；半径 6：1.30% / 14% |
| 填充 | 归一化卷积，半径 2 起倍增 | 从 8 起步（粗）会把 std>8 抬 0.8 个百分点 |
| 迭代 | 2 轮 | 残差是对含自身的共识算的，1 轮欠校正；第 3 轮不再变 |
| 覆盖门槛 | 格内权重 < 该 tile 最大格的 5% 不信 | 边缘只有几个样本的格不代表这台相机看到的东西 |

**应用**：`WarpPlan.apply(corrections=...)` 与 GPU 核函数各减去校正网格的双线性上采样值
（`warp.upsample_grid`，两边逐语句同序），**CPU/GPU 仍逐字节一致**（tests/test_warp_gpu.py 含校正
比对）。`corrections=None` 时一个字节不变，P1 基准与指标 D 保留。估计在**解码预取线程**里做
（`pipeline._preparer`），与上一帧的 warp/写盘重叠。

**效果**（8K catmullrom，默认设置，`vr-compose frame`）：

| 帧 | 校正前 median / mean / std>8 | 校正后 | 门 |
|---|---|---|---|
| v004 帧 0 | 1.14 / 2.91 / 9.9% | **0.72 / 1.31 / 1.19%** | FAIL → **PASS** |
| v004 帧 100 | 1.22 / 3.02 / 10.5% | **0.75 / 1.30 / 1.08%** | FAIL → **PASS** |
| v004 帧 900 | 1.08 / 2.43 / 2.1% | **0.49 / 0.94 / 0.52%** | WARN → PASS |
| E:\22 帧 1656 | 0.48 / 0.77 / 0.04% | 0.42 / 0.64 / 0.03% | PASS → PASS |

- **成品改动**：v004 帧 100 有 3.6% 的像素动了 >2 级，最大 7 级，没有像素动 >8；
  **E:\22 最大 3 级，动 >2 级的像素 0.001%**，21% 的像素动 1 级。对本来一致的数据接近不动。
- **时域**：连续 30 帧（100–129）校正网格帧间变化 median 1.9、p95 6.3、max 7.1 级——看着大，
  但成品的帧间变化**与不校正时完全一样**（上三分之一 mean |Δ| 0.374 对 0.374，12 帧逐帧相同），
  因为雾本来就是逐视口变的，校正只是让各 tile 跟着同一个共识走。**没有引入闪烁。**
  刻意没做时域平滑：那会让续跑的分段起点带状态，破坏「续跑逐字节一致」。
- **估计成本**：0.30 s/帧单独测（任何 tile 尺寸都差不多，是固定开销），在预取线程里做。

**吞吐代价（8K catmullrom，v004，默认设置）**：

| 路径 | 不校正 | 校正（默认） | 差在哪 |
|---|---|---|---|
| CPU（20 帧） | ~11.0 s/帧 | **13.7 s/帧**（14.25 含建表摊销） | 全分辩率混合里每个样本多一次校正上采样（74 M 条 × 4 次 gather），约 +2.6 s；估计藏在预取线程里（等解码 0.03 s） |
| GPU（60 帧） | 0.48 s/帧 | **0.82 s/帧** | warp 只有 0.10 s，藏不住 0.3–0.5 s 的 CPU 估计；预取从 1 帧加深到 2 帧只从 0.93 降到 0.82（两个估计线程争 GIL） |

CPU 多 25%、GPU 多 70%，都**没有达到**ROADMAP P9 写的「CPU ≤ 15%、GPU 可忽略」。如实记下；两条后续优化
都明确：(a) 把每条 LUT 的上采样几何（格角、权重）缓存进 plan 换掉 div/mod 与浮点转换，CPU 侧估计能省一半；
(b) 估计器改 cupy 版跑在 GPU 上（bincount/gather 都有），GPU 侧回到 0.5 s 附近——但要注意 GPU 估计器与
CPU 估计器的求和顺序不同，会让两条路径的**校正量**略有差异，从而破坏 CPU/GPU 逐字节一致；要做就得把
估计器也按同序转写。778 帧现在：CPU 约 3 小时，GPU 约 11 分钟。

**它不能做的**：运动模糊的清晰度差（帧 900 相机快速移动时接缝两侧模糊程度不同，只抹掉了伴随的
亮度偏差）；渲染本身的瑕疵（体积雾网格在天空里的多边形块）。上游能改的话见 UPSTREAM.md 第 6 条。

### GPU 加速（已实现，`--device cuda`，仅命令行；2026-09-09 实测）

**为什么做了**：P4 把默认采样器换成 catmullrom 之后，8K warp 从 1.84 s 变成 **11.0 s/帧**
（16 个 gather），778 帧母版约 2.4 小时。warp 的形状（静态 LUT + 每帧一次 gather/加权累加）正是
GPU 擅长的。**规则由用户定（2026-09-09）**：库与 CLI 默认 `cpu`；`--device cuda` 明确要 GPU，
没有 NVIDIA 卡、`cupy` 没装、或显存**低于 12 GB（十进制）**时打 Warning 回退 CPU，作业照常跑
（最初定 16 GB，用户同日降到 12 GB）；**GUI 固定发 `--device auto`**，同一道门禁，过了用 GPU，
没过**静默**走 CPU——只在日志里写一行原因，不进 warnings，因为用户没有要求过什么。GUI 里没有开关
（最初的规则是 GUI 完全不碰 GPU，用户同日改成 auto）。用十进制是因为标称 12 GB 的卡驱动报 12 282 MiB
（略小于 12 GiB），它必须过关；8K 的 plan 加权重加缓冲占 4.6 GB，12 GB 有余量。

**实现**（`vr_compose.device` 门禁、`vr_compose.warp_gpu` 核函数；`tools/gpu_probe.py` 是探针也是原型）：

- `GpuWarpPlan.from_plan(plan)` 把 `WarpPlan` 的每 tile 数组上传常驻显存。**几何只有一份**
  ——fingerprint、LUT 缓存、pipeline 的匹配检查都是 CPU plan 的；GPU 只做逐帧的 gather 与混合。
- **与 CPU 逐字节一致**（指标 D），三个采样器、8/16 位、含几何门统计三项，合成数据与
  5 张真实 8K 帧上都验证过。四个前提缺一不可：`--fmad=false`（关掉乘加融合）；
  逐语句转写 `bilinear_blend` / `cubic_blend` / `luma_bt709` / `quantise` 的运算顺序；
  catmull-rom 权重在主机侧用**同一个** `catmull_rom_weights` 算好再上传（32 字节/条，约 2.4 GB）；
  **每 tile 一次 launch、按 plan 顺序串行累加、不用原子操作**（tile 内 out_index 唯一，
  跨 tile 的求和顺序与 CPU 的 `for tile in self.tiles` 完全相同）。结果与 block 大小、显卡无关。
- 门禁和上传失败都是 Warning + CPU，warning 进 `Summary.warnings`，CLI 结尾打 `warp : cpu/cuda (...)`。

**探针实测**（`tools/gpu_probe.py`，RTX 4090，8K，单帧，中位数）：

| 采样器 | CPU 8 线程 | 上传 | 核函数 | 下载 | GPU 合计 | 加速 | 指标 D |
|---|---|---|---|---|---|---|---|
| nearest | 2.69 s | 16.2 ms | 5.6 ms | 17.1 ms | **39 ms** | 69× | 逐字节一致 |
| bilinear | 4.32 s | 14.7 ms | 6.2 ms | 15.0 ms | **36 ms** | 120× | 逐字节一致 |
| **catmullrom** | **11.02 s** | 15.6 ms | 8.5 ms | 15.9 ms | **40 ms** | **275×** | 逐字节一致 |

带统计（几何门那一帧）核函数多 4 ms。显存：plan + 权重 + 缓冲 **4.57 GB**（catmullrom），
上传 3.1 s。传输已经是大头（PCIe 上传 166 MB + 下载 88 MB ≈ 32 ms），核函数只有 8.5 ms。

**管线实测**（`vr-compose sequence --device cuda`，8K h264 高档 catmullrom，100 帧真实数据）：

| 项 | 值 |
|---|---|
| **端到端** | **0.64 s/帧**（含建表 10.7 s + 上传 3.1 s 摊到 100 帧），CPU 路径同配置约 11 s/帧 |
| 分解（解码 4 线程，CPU 的默认） | 等解码 **0.24 s**、warp 0.09 s、几何门 0.01 s、写管道 0.10 s |
| 内存峰值 | 32.5 GiB commit（拼接 9.6 + 编码器 22.9），19.8 GiB 常驻 |
| 合规 | `conformance: OK`，198.7 Mbps，几何门 PASS（median 0.52，与 CPU 相同） |

新瓶颈换人：**PNG 解码**（等待 0.24 s）与 **x264 8K 软编**（写管道阻塞 0.10 s）。

**解码线程扫描**（同一 100 帧，`--device cuda`）：CPU 路径当年把解码压到 4 线程是为了不和
warp 线程争 GIL；GPU 路径没有 warp 线程，这个约束消失了。

| `--decode-workers` | 端到端 | 等解码 | warp | 写管道 |
|---|---|---|---|---|
| 4（CPU 的默认） | 0.64 s/帧 | 0.24 s | 0.09 s | 0.10 s |
| **8** | **0.48 s/帧** | 0.13 s | 0.07 s | 0.11 s |
| 12 | 0.46 s/帧 | 0.14 s | 0.07 s | 0.07 s |
| 16 | 0.45 s/帧 | 0.14 s | 0.06 s | 0.07 s |

4→8 拿走大半（等待 0.24→0.13），再往上只剩 0.02–0.03 s。所以**默认按设备取**
（`pipeline.DECODE_WORKERS`：cpu 4、cuda 8），且在**设备落定之后**才取——`--device cuda` 回退到
CPU 的作业拿到的是 4，不会把 CPU warp 拖慢。等待 0.13 s 是 15 张 PNG 用 Pillow 解码的地板，
再快要换解码器，不在这一项范围内。778 帧 h264 高档按 0.48 s/帧约 **6 分钟**（CPU catmullrom 约 2.4 小时）。

**PNG 母版 sink 在 GPU 路径上**（30 帧，解码 8 线程）：

| 位深 | `--write-workers` | 端到端 | 阻塞在写 | 文件 |
|---|---|---|---|---|
| 16 | 1 | 4.68 s/帧 | 4.22 s | 133.5 MiB/帧 |
| 16 | **4** | **1.54 s/帧** | 1.09 s | |
| 8 | 1 | 1.26 s/帧 | 1.06 s | 39.4 MiB/帧 |
| 8 | **4** | **0.51 s/帧** | 0.24 s | |

warp 只剩 0.06–0.14 s，**PNG 编码成了唯一的大头**。P3 时 1 个写线程最快，是因为第二个写线程
会拖慢 CPU warp；GPU 路径上没有这个代价，写线程默认也按设备取（`pipeline.WRITE_WORKERS`：
cpu 1、cuda 4）。16 位母版 778 帧从 CPU 的约 2.4 小时变 **20 分钟**。

---

## 9. 怎么证明一次改动是对的

不要靠肉眼看缩略图判断拼接正确 —— 球面对称性会掩盖上下颠倒、扇区错位这类错误。用下面两个指标：

**指标 A：重叠区自一致性（首选，不需要参考产物，也不受场景运动影响）**
同一帧的 15 个 tile 重投影到球面后，在重叠方向上互相之间的亮度差。
由 `vr_compose.verify.agreement()` 计算，`vr-compose frame` 直接打印并给 PASS/FAIL。

rig 正确时（标称角度、BT.709 亮度），**按采样器分别记**（`verify.BASELINES`）：

| 分辨率 | 采样器 | median | mean | p95 | std>8 占比 | 环绕接缝 |
|---|---|---|---|---|---|---|
| 3840×1920 | nearest | 0.68 | 1.02 | 3.10 | 0.250% | 1.61 |
| 3840×1920 | bilinear | 0.48 | 0.77 | 2.52 | 0.036% | 1.36 |
| 3840×1920 | **catmullrom** | 0.52 | 0.81 | 2.58 | 0.047% | 1.52 |
| 7680×3840 | nearest | 0.67 | 1.02 | 3.10 | 0.247% | 1.23 |
| 7680×3840 | bilinear | 0.48 | 0.77 | 2.52 | 0.037% | 0.78 |
| 7680×3840 | **catmullrom（默认）** | 0.52 | 0.81 | 2.58 | 0.048% | 0.90 |

**注意 catmullrom 在这张表上「输」给 bilinear，但它对源的还原好 1.24 dB。**
这个指标量的是一致性不是保真度，别当质量分用 —— 见第 8 节「三个采样器」。

**这张表里最重要的一行是 std>8 掉了 6.8 倍。** P4 换成双线性之后 median 从 0.68 到 0.48 ——
也就是说**原来这个指标里有四分之一测的是我们自己采样器的误差**，不是 rig 误差：
最近邻把每个样本放在离正确位置最多半像素的地方，两个 tile 对同一个方向取整的方向不同时，
差出来的就是那半像素上的图像梯度。剩下的 0.48 更接近源本身的下限（tile 间 TAA 抖动 ≤1 灰阶）。
**门限没有跟着调紧**：门是用来抓「rig 上下颠倒」的（误差是几十灰阶量级），
而且预览用的 nearest 也必须能过。

顺带：8K 环绕接缝 1.23 → **0.78**（参考产物是 1.15）。这是无参考的自一致性指标，
但注意它不是和参考产物比同一帧 —— 见指标 C 的限制。

门限**分两级**（P9，2026-09-10）：`median ≤ 1.0 且 mean ≤ 1.5` 是**警告级**（`GATE_MAX_*`，超过则
进 `Summary.warnings` 并继续跑）；`median > 2.5 或 mean > 4.0` 是**致命级**（`GATE_FATAL_*`，停下来）。
分级的依据是下面「两种失败签名」的实测：所有 rig 级错误都在致命级之上（视场角差 1° 约 2.0 再往上、
镜像反 5.3、仰角反 13.9），而按视口渲染的内容差异落在 1.1–1.3 的警告区，那不是几何的错。
`--gate off` 完全跳过。**统计量取的是光度校正之后的样本**（`harmonise` 开着时），所以 v004 这类数据
默认设置下能过：帧 0/100 校正后 median 约 0.7–0.9。

**指标 A 的两种失败签名（2026-09-09 实测，E:\22 帧 1656，3840 宽，bilinear）**。
另一台机器上一套新渲染（`D:\CR\0909C`，20 路）过不了门：median 1.19、mean 2.91、p95 10.5、
**std>8 占 9.99%**。要分清是「内容不一致」还是「几何不一致」，看 **std>8 那一列**：

| 情形 | median | mean | p95 | std>8 |
|---|---|---|---|---|
| 正确 rig | 0.48 | 0.77 | 2.5 | 0.036% |
| 一半相机提亮 3% / 6%（曝光、bloom 之类） | 0.58 / 0.80 | 0.88 / 1.15 | 2.8 / 3.5 | **0.04% / 0.08%** |
| 一半相机 +4 灰阶（雾） | 1.71 | 1.63 | 3.8 | **0.04%** |
| 单相机 yaw 偏 0.5° / 1° / 2° / 5° | 0.51–0.53 | 0.94–1.36 | 2.9–4.0 | 0.6% / 1.0% / 1.4% / 2.2% |
| rig FOV 90.5° / 89.5°（tile 实为 90°） | 1.52 / 1.49 | 2.66 / 2.63 | 8.8 | **5.9%** |
| rig FOV 91° / 89° | 1.96 / 1.93 | 3.55 / 3.50 | 12.2 | **10.1% / 9.8%** |
| 两个环仰角各偏 1° | 2.23–2.30 | 4.1 | 14.4 | 12.4% |
| 仰角符号反 | 13.9 | 18.7 | 53 | 71% |
| 镜像标志反 | 5.30 | 11.1 | 44.6 | 38.6% |

- **内容差异抬 median 和 mean，但 std>8 几乎不动**（≤ 0.08%）：它是整体偏移，重叠区两边各差一点。
- **几何差异让 std>8 成片上涨**：0.5° 就到 6%，1° 到 10%。新渲染的 10% 属于这一类 ——
  最像 **FOV 差了约 1°**，或各环仰角有系统性偏差，不是雾或曝光。8K 上这是 tile 边缘十几像素的
  重影，缩到 2000 宽肉眼看不出，门看得出。
- 顺带发现：**现在的门抓不到单相机偏 5°**（mean 1.36 < 1.5）。门只对 rig 级的错误敏感，这和它
  的设计目标一致（抓「装反」），但别把它当精度证明。
- `tools/rig_probe.py` 按这个原理扫 FOV / 环仰角 / 镜像，E:\22 上唯一极小值落在 90° / 0° / True，
  谷很尖（±0.5° 就把 std>8 从 0.036% 抬到 6%）。拿到那台机器的一帧 tile 就能定出它真实的 rig。

任何改动如果让 median 明显高于 1，几何就是错的。

> 早期探查脚本记录的是 0.70 / 1.04 / 3.14 / 0.26%，那是用 BT.601 亮度权重算的。
> 产品代码改用 BT.709（与交付规格标记的色彩空间一致），所以数字略低。
> 这是同一个量的两种算法，不是画质变化。

**指标 B：与参考产物比对**
参考帧只有 1..1655，而在手的源帧是 1656..2433，**没有同帧可比**。
只能拿源帧 1656 对参考帧 1655（相邻帧），此时 `PSNR ≈ 26 dB`，差值主要来自一帧的场景运动，
**不能当精度指标用**；它只能用来确认没有整体翻转/偏移
（最佳环绕位移 ≤1 px / 0.5°，零位移 NCC = 0.964；上下翻转 0.08、左右镜像 0.30，差距足够判别）。

**指标 C：与参考产物的对照 —— 逐像素对比做不到，接受这个限制**

参考产物覆盖帧 1..1655，而它们的源 tile **已被删除**；我们手上的源 tile 是 1656..2433，
参考产物在这个区间没有输出。**两者永远没有同一帧。**

唯一能造出可比一对的办法是让上一代流程再处理 10 帧（并事先备份源 tile），
**用户已明确否决**（第 2 节约束 2）。所以「超过参考产物」只能用无参考的方式论证：

1. **无参考指标**（都在我们自己的母版上算，不需要对照）：
   - 重叠一致性（指标 A）—— 几何正确性；
   - 接缝梯度连续性 —— 等距抽 20 个 tile 边界位置做不连续性检查；
   - **极区细节保留度** —— 拿重投影结果对源 tile 的对应区域算，衡量重采样丢了多少；
   - 时域稳定性 —— 连续 10 帧的极区方差，不高于源 tile 自身的时域方差。
2. **相邻帧视觉 A/B**：我们的 1656 对参考的 1655，在镜头运动小的区域（岩石等静态几何）
   做局部目视与局部指标对比。**这是定性的，不要当成定量结论。**
3. **可复现的方法学论证**：极区最好 tile 的采样密度只有中心的 0.355（第 3 节实测），
   而参考产物用的滤波未知；我们的多 tile 联合重建（ROADMAP P4）在原理上能利用
   2–3 个 tile 的亚像素错位，这一点可以在**自己的两个版本之间**做 A/B 量化
   （最近邻 vs EWA vs 多 tile），从而证明「我们做到了什么」，
   而不是「我们比那个不可测量的东西好」。

**汇报时要如实说明这个限制**，不要把无参考指标包装成对参考产物的胜出。

**P7 加的一条（同样影响能声称什么）**：我们量交付 MP4 的 PSNR 一直是让 ffmpeg 把
`yuv420p` 直接转成 `rgb24`，而那一步自带约 −1.2/255 的平坦偏置，**把绝对值压低约 3.7 dB**
（见第 5 节「4:2:0 的账」）。所以：

- **比较**不同码率、编码器、采样器仍然有效 —— 偏置是同一个常数，差值不受影响；
- **绝对值不要对外讲**，也不要写进交付文档当画质指标；
- 要绝对值就走精确逆变换（`tools/chroma_probe.py` 里的 `decode_exact`，
  在 4:4:4 上与 ffmpeg 逐码字一致，这就是它的自证）。

跑 `python tools/stitch_probe.py` 就会打印指标 A/B 并按 `median<=1.0 且 mean<=1.5` 给出 PASS/FAIL。

**指标 D：LUT 与逐帧计算逐字节一致（P2 起）**
`WarpPlan.apply()` 的输出必须与 `stitch_bands()` 逐字节相同，任意线程数下都是；
`tests/test_warp.py` 断言了这一点。它抓到过一个真问题：亮度用 `sampled @ LUMA_BT709`
走 BLAS 点积，求和顺序随批长度变化，同一像素在两条路径上末位不同 —— 改成逐元素
float32 运算后才一致。**任何「更快的 warp」（numba、GPU、换布局）都必须先过这条。**

**回归测试写法**：把上面这些数字作为断言写进 pytest，用单帧（如 1656）跑，标 `@pytest.mark.slow`，
因为它需要访问 `E:\22`。纯数学部分（射线生成、旋转矩阵、equirect 映射、序号→视角的映射表）
必须有不依赖磁盘的单元测试。

---

## 10. 工程约定

Python **3.13**，src 布局，包名 `vr_compose`（分发名 `vr-compose`）。
系统的 `python` 是 3.9，**一律走 venv**，不要用裸 `python`。

```bash
py -3.13 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

```bash
.\.venv\Scripts\python.exe -m pytest ; .\.venv\Scripts\python.exe -m ruff check . ; .\.venv\Scripts\python.exe -m mypy
```

- `mypy` 是 **strict + warn_unreachable**，`ruff` line-length 100。提交前三样都要过。
- Python 依赖：`numpy>=2.1`、`pillow>=11.0`（已声明在 `pyproject.toml`）。
- **外部依赖：ffmpeg**（本机 8.0-full，带 libx264 / libx265 / hevc_nvenc / aac，已验证）。
  以 subprocess 调用，不引 Python 绑定。启动时检查可执行文件与必需编码器是否存在。
- `mypy` 覆盖 `src` / `tests` / `tools` 三者，`ruff` 覆盖全仓库。
- **`tools/` 里是本文档所有数字的复现脚本**，见 [tools/README.md](tools/README.md)。
  改动几何、滤波或编码参数之后跑一遍，数字漂移了就说明有事发生。
  `tools/_rig.py` 是 rig 定义的**临时住所**，P1 搬进 `src/vr_compose/rig.py`。
- 依赖在 `pyproject.toml` 里显式声明；新增重依赖（torch / cupy / opencv / ffmpeg 绑定）
  先在 ROADMAP 对应阶段说明理由，别顺手加。已有的例外：`cupy-cuda12x` 在 **`[gpu]` extra** 里，
  默认安装不带，发布 exe 也不带（spec 里 exclude）。
- 长任务用 CLI 子命令暴露，参数显式，不要在代码里硬编码 `E:\22` 之外的路径；
  `E:\22` 作为只读参考路径可以出现在测试与文档里。
- 日志用 `logging`，进度信息带上帧号与速率（s/帧），方便和第 5 节的基线对比。

### P4 实测：接缝、羽化指数、时域稳定性、16-bit

**接缝（第 4 项）**：`tools/seam_probe.py` 在 tile 相邻的大圆上等距取 20 个位置，
量「跨边界的梯度 / 沿边界的梯度」—— 场景内容在这个比值里对消，只有单方向的不连续会推高它。
8K 帧 1656，140 个位置：

| 羽化指数 | 接缝比 median | p95 | max | 环绕接缝 |
|---|---|---|---|---|
| 0.5 | 0.818 | 1.81 | 4.31 | 1.535 |
| 1 | 0.810 | 1.81 | 4.29 | 1.527 |
| **2（默认）** | **0.793** | 1.81 | 4.26 | 1.521 |
| 4 | 0.796 | 1.81 | 4.21 | 1.523 |
| 8 | 0.795 | 1.80 | 4.21 | 1.523 |
| 16 | 0.797 | 1.79 | 4.25 | 1.538 |

**median 0.79 < 1，也就是跨边界的梯度比沿边界还小 —— 没有接缝**（阶梯边会远大于 1）。
p95/max 那些高值在所有羽化下**完全相同**，说明它们是真实景物边缘穿过边界，不是接缝伪影。
顺带一个有用的性质：**指标 A 完全不随羽化变化**（0.524 一路不动）——
它统计的是各 tile 的亮度离散度，不带混合权重，所以它是纯几何检查，调混合调不动它。

**羽化指数（第 4 项）与「加权平均是不是在模糊」（第 2 项的前提）**：
`_feather` 现在是 `距边缘^power`，`power` 可调（默认 2，逐字节不变）。
用往返保真度扫这个指数（3840 宽）：

| 羽化指数 | 0.5 | 1 | **2（默认）** | 4 | 8 | 16（≈只取最好的 tile） |
|---|---|---|---|---|---|---|
| 对源还原 | 44.04 | 44.43 | **44.76** | 44.87 | 44.76 | 44.55 dB |

**曲线在默认值附近见顶，并且朝「只取最好 tile」的方向下降。**
所以 ROADMAP 原文那句「现在的加权平均是在平均这些错位，等于主动模糊」**不成立**：
平均不是在损失细节，它在降噪（每个 tile 的插值都是同一个输出方向上的无偏估计，
平均若干个无偏估计不会引入模糊）。而且这个探针按每个 tile 的中央 50% 打分，
**本身偏袒高指数**，即便如此高指数还是输了 —— 结论更稳。
最优在 2–4 之间，差 0.11 dB 且不是每个 tile 都一致，**不值得为它换默认值并重记所有基线**。

**时域稳定性（第 5 项）**：`tools/temporal_probe.py`，连续 10 帧（1656–1665），8K。

| 纬度带 | 母版方差 | 源 tile 方差 | 比值 | 最大帧间跳变（母版/源） |
|---|---|---|---|---|
| 极冠 >85° | 8.825 | 7.739 | **1.140** | 54.7 / 63.7 ✅ |
| 极区 70–85° | 10.653 | 12.498 | 0.852 | 76.7 / 91.7 ✅ |
| 中纬 30–70° | 53.904 | 77.131 | 0.699 | 233.0 / 233.7 ✅ |
| 赤道 <30° | 72.212 | 77.564 | 0.931 | 231.3 / 228.7 ⚠ 1.011 |

**判据原文（母版时域方差 ≤ 源 tile 时域方差）在极点是不成立的比较**，而且能证明：
`nearest` 与 `bilinear` 是**凸组合**（权重非负且和为 1，含羽化混合），
输出必然被输入夹住，**数学上不可能制造方差**；可是 nearest 在极冠也给出 1.159，
比 catmullrom 的 1.140 还高。所以那个 >1 是**两个网格差 16 倍**造成的比较偏差，不是抖动。
于是探针改报一个**与网格无关的界**：任一输出像素的最大帧间跳变 vs 任一源像素的最大跳变。
凸核必然 ≤ 1（nearest 四带全过，印证了度量本身是对的）；
catmullrom 因为有负瓣，在赤道超出 **1.1%**（231.3 对 228.7，是 1400 万像素里最极端那一个，
而那里本来就在发生 228 灰阶的跳变）。**结论：没有可感知的新增抖动**，
超出的那 1.1% 是更锐的核在硬边上过冲的已知代价。

**16-bit 母版（第 6 项）**：`--bit-depth 16`。源是 8-bit，所以 16 位不带来渲染器的新信息，
它保住的是**重采样与混合产生的亚灰阶精度**（8-bit 量化会扔掉），
这对「还要被下游再重采样一次的母版」有意义，也是将来源头换 EXR 时的落点。

- **Pillow 写不了 16-bit RGB PNG**（`Image.fromarray` 直接拒绝三通道 uint16），
  所以 `io.write_png16` 自己写 PNG。用 filter type 2（Up，逐字节减上一行）——
  equirect 相邻行相似、极点附近几乎相同，正好对上。
  正确性用 Pillow 自己的读取来校验（它会降到高字节，但那是一个**独立解码器**）。
- 实测 8K：**133.4 MiB/帧、13.14 s/帧**（8-bit 是 39.3 MiB、11.01 s）。
  Up 滤波把它从 153.9 MiB 降到 133.4（−13%，不是想象中的减半 ——
  16 位的低字节本来就接近噪声）。
  **778 帧 16-bit 母版 = 101 GiB**，磁盘预检按 5.0 字节/像素估（实测 4.53，留了余量）。

### P4 实测：极区细节的「余量」与按纬度的对比（`tools/detail_probe.py`）

第 9 节要求的「极区细节保留度」指标。**问「有没有细节可救」必须量源 tile，不能量母版** ——
极点附近一个源像素被拉成约 16 个输出像素，母版在那里天生就是平的，
拿母版的锐度去推断内容平坦是错的（我自己先犯了一次）。

**源 tile 的局部对比度，按这些像素落到的纬度分（mean |Laplacian|，帧 1656）：**

| 目标纬度带 | 源 tile 细节 | 母版 nearest | 母版 bilinear |
|---|---|---|---|
| 极冠 >85° | **2.23** | 1.55 | 1.15 |
| 极区 70–85° | **2.83** | 1.86 | 1.18 |
| 中纬 30–70° | 4.14 | 3.03 | 1.65 |
| 赤道 <30° | 3.75 | 3.38 | 2.27 |

- **源在极区方向上确实带着细节**（2.23–2.83，是中纬/赤道的 60–75%）——
  所以 P4 第 2 项（极区多 tile 联合重建）**有真实的余量**，不是无米之炊。
- 源与母版两列**不能相除**：局部对比度是尺度相关的量，而极点附近母版的网格比源密得多，
  比值说的是两个网格的关系，不是重采样的损失。只能源对源、母版对母版地比。
- 母版两列之间：bilinear 把局部对比度降了 **26%（极冠）到 46%（中纬）**。
  **这里要如实说清楚**：有四条独立证据表明降掉的主要是最近邻**复制**出来的假高频 ——
  指标 A 的离群点少了 6.8 倍、环绕接缝 1.23→0.78、PNG 反而小了 6%、
  而且几何上 σ<1 意味着输出网格比源密，真信号在输出尺度上本来就该是平滑的。
  **但 bilinear 也是所有合理插值核里最软的一个**，那 26–46% 里有多少是真被磨掉的边缘，
  单靠这些数字判断不了 —— 见 ROADMAP P4 第 1 项末的待决问题。

### P4 实测：线性光混合 —— 验证过了，结论是**不做**

ROADMAP P4 第 3 项说「混合必须在线性空间做，否则重叠区会系统性偏暗」。
拆成两件事分别实测（`tools/linear_light_probe.py`，假定源是 sRGB 传输函数）：

| 改动 | 平均差 | p99 | p99.9 | 有符号均值 | max |
|---|---|---|---|---|---|
| **只把混合**改到线性（nearest，不含插值） | **0.017** | 1 | 1 | +0.017 | 51 |
| 混合 + 插值都改到线性（bilinear） | **0.040** | 1 | 2 | +0.040 | 51 |
| *参照：换采样器本身*（都在编码空间） | 0.491 | 5 | 11 | +0.006 | 100 |

帧 2433 复核：0.009 / 0.031，同一量级。

- **方向是对的（线性光确实更亮一点），量级是 0.017 灰阶 —— 不到一个量化步，肉眼不可能看见，
  比换采样器小 12 倍。**
- 原因是可以推出来的，不是巧合：共节点装配下重叠的 tile 看的是同一束光，
  彼此只差约 0.5 灰阶（第 9 节），而编码空间与线性空间的平均值之差**按差值的平方增长**。
  两个几乎相等的数，在哪个空间平均都一样。这条对这套 rig 的任何场景都成立。
- 代价却不小：解码可以用 256 项查表（免费），但**编码要对 8850 万个浮点做一次幂运算**，
  8K 下约 1.5–2 s/帧，等于把帧成本再翻一倍 —— 换 0.04 灰阶。**不做。**
- **顺带更正**：AGENTS.md 原文说这条是「P7 色度工作的前提」。不是。
  4:2:0 色度下采样发生在 **ffmpeg 的 swscale 里**，不是我们的混合环节；
  要在线性光下做色度抽样，得我们自己做 RGB→YUV，和这里改不改混合空间无关。
- **P7 后记**：那件事后来真做了，**结论同样是不做** —— 自己在线性光下做色度下采样
  相对 gamma 盒式只差 −0.02 dB。两次否决线性光的机制是同一个（上面那条平方律），
  所以这条可以当结论用，不必再验第三次。
- 什么时候要重新考虑：源头换成 16-bit / EXR（线性数据）之后，
  管线就本来在线性空间里了，那时这条自动成立，不需要额外的编解码。

### P4 实测：三个采样器（8K，同机）

| 项 | nearest | bilinear | **catmullrom（默认）** |
|---|---|---|---|
| **对源的还原 PSNR** | 47.85 dB | 47.66 dB | **48.90 dB** |
| 指标 A median | 0.67 | **0.48** | 0.52 |
| 指标 A std>8 | 0.247% | **0.037%** | 0.048% |
| 8K 环绕接缝 | 1.23 | **0.78** | 0.90 |
| LUT | 843 MiB | 1404 MiB | 1404 MiB |
| warp | 1.97 s | 3.62 s | **10.11 s** |
| PNG 母版端到端 | 2.49 s/帧 | 4.10 s/帧 | **11.01 s/帧** |
| 内存峰值（母版路径） | 3.7 GiB | 4.5 GiB | 6.5 GiB |
| 母版文件大小 | 39.6 MiB | 37.1 MiB | 39.3 MiB |

**这张表最重要的一点：两组指标给出相反的排序，而只有一组回答「还原」这个问题。**

- **指标 A 不是保真度指标。** 它量的是重叠 tile 之间**一致**到什么程度，
  所以越模糊的重建越容易得高分；而更锐的核在边缘会过冲，
  两个 tile 从不同亚像素位置采同一方向就会更不一致。
  catmullrom 的一致性（0.52）**不如** bilinear（0.48），
  但它对源的还原**好 1.24 dB**。两件事都是真的，只有后一件跟细节有关。
- **保真度要用往返测量**（`tools/fidelity_probe.py`）：把母版按 tile 的真实射线方向重采样回
  tile 自己的像素网格，和渲染器出的那张 tile 比 PSNR。重采样丢掉的信息回不来，
  所以还原得最好的那个就是留下最多的那个。回程对三者用同一个双线性，是恒定成本不是偏袒。
  三个仰角环各测一个 tile，**结论一致**。
- **bilinear 在保真度上并不比 nearest 好**（47.66 对 47.85）——
  它去掉了半像素位置误差，却换来了模糊，两者大致抵消。
  catmullrom 是去掉位置误差**而不付模糊代价**的那一个。
- **代价是 warp 从 1.97 s 到 10.11 s**（16 个 tap 对 1 个）。778 帧母版约 2.4 小时、
  交付 MP4 约 2.2 小时（对 bilinear 的 53 min、nearest 的 32 min）。
  默认取 catmullrom 是因为用户 2026-09-08 明确要「尽可能还原单帧」；
  要快就 `--sampler bilinear`，代价是 −1.24 dB。
  **这也是 GPU 那一项真正开始值钱的地方**：16 个 gather 正是 GPU 白送的东西。
- LUT 每条 20 字节 = out_index 4 + src_index 4 + weight 4 + fx 4 + fy 4，
  bilinear 与 catmullrom **同样大小** —— 存「左上角 + 两个小数」，tap 在 apply 时用索引算。
- 母版文件大小：nearest 39.6 / bilinear 37.1 / catmullrom 39.3 MiB。
  bilinear 最小是因为它把最近邻的块状阶梯（压不动的假高频）磨平了；
  catmullrom 回到 39.3 是因为它把**真实**的高频留住了 —— 与还原 PSNR 的排序一致。

### P3 实测：PNG 母版 sink（8K，同机）

`vr-compose sequence --out-format png`，10 帧真实数据：

| 项 | 值 |
|---|---|
| 端到端 | **2.40 s/帧**（`--write-workers 1`）/ 2.52 s/帧（2 个写线程） |
| 分解（1 个写线程） | 等解码 0.03 s、warp **2.06 s**、几何门 0.09 s、阻塞在写 **0.12 s** |
| 分解（2 个写线程） | 等解码 0.03 s、warp **2.28 s**、几何门 0.10 s、阻塞在写 0.02 s |
| 文件大小 | **39.6 MiB/帧**（compress_level 1）= **1.41 字节/像素** |
| 内存峰值 | 3.7 GiB commit（没有编码器那一份） |

- **1 个写线程最快**，而且理由与第 8 节解码池那条一样：第二个写线程几乎把写完全藏住了
  （阻塞从 0.12 s 降到 0.02 s），代价是把 warp 从 2.06 s 拖到 2.28 s ——
  **总线程数比单项各多少更重要**。默认 `write_workers=1`。
- **无损 PNG 在这个内容上只省不到一半**（1.41 字节/像素对原始 RGB 的 3）。
  高频的岩浆几何压不动。778 帧 8K 母版 = **30 GiB**，是同长度 MP4 的 15 倍
  —— 所以母版是按需产出的，不是必经环节。磁盘预检按 1.6 字节/像素估（留余量）。
- 母版**像素**与 `frame` 命令、与 MP4 路径完全相同（指标 D 延伸到 sink，有测试）。
  **文件**逐字节相同还要求同一个 compress_level —— zlib 等级改的是编码，不是图像。

### P3 实测：取消语义（Windows 的 Ctrl-C 会打到 ffmpeg）

Windows 的 Ctrl-C 发给**控制台里的每一个进程**，而 ffmpeg 自己处理 SIGINT。所以在加
`CREATE_NEW_PROCESS_GROUP` 之前，被中断的一次运行是**编码器先死**，管线随后撞上断管，
报出来的是 `ffmpeg exited early`（而且消息是空的）—— 也就是说
**「用户按了 Ctrl-C」和「编码器真的挂了」在退出码和消息上完全无法区分**。
磁盘状态一直是对的（完成分段保留、无 `.part`），错的是分类。

已修，三处：

1. **ffmpeg 进自己的进程组**（`encode.child_creation_flags()`），Ctrl-C 只到 Python，
   杀 ffmpeg 重新成为 `SegmentWriter.abort()` 的专属职责。
2. **取消是一等结果**：`pipeline.Cancelled`，由 `_CancelScope` 把 SIGINT 变成一个标志，
   帧循环在**帧边界**检查它 —— 所以不会有半帧写进编码器，代价是取消最多晚一帧生效
   （8K 约 2 s）。再按一次 Ctrl-C 会恢复默认处理器，立刻硬中断。
   GUI（P6）与测试走同一个机制：`run_sequence(..., cancel=threading.Event())`，不碰信号。
3. **`concat` 也清自己的 `.part`**，中断落在合并阶段时输出旁边不留残留。

**顺手抓到的一个真 bug**：Windows 上被 kill 的 ffmpeg，往它 stdin 写数据抛的是
`OSError: [Errno 22] Invalid argument`，**不是** `BrokenPipeError`。原来只接
`BrokenPipeError`，所以真的编码器死亡会以裸 `OSError` 冒出去，CLI 的
`except RuntimeError` 接不住 —— 用户会看到 traceback。现在接 `OSError` 全族，
并把退出码与 stderr 一起放进消息。

`SegmentWriter` 的 stderr 现在有**常驻 drain 线程**：原来只在结束时读一次，
理论上 ffmpeg 输出超过 64 KiB 管道缓冲就会双向死锁（`-loglevel error` 只是让它很难触发）。

### P5：两种输出模式的硬边界（用户 2026-09-08 指定）

| | 视频模式（默认） | 帧模式 `--out-format png` |
|---|---|---|
| 尺寸 | 按 `--size` 交付，需要时 ffmpeg 里 Lanczos 降采样 | **按 input 自己的密度**（`Rig.native_width(tile_size)`），**不接受 `--size`** |
| 压缩 | H.264/H.265 有损 | **无损** |

**这两条是结构性的，不是约定**：

- 帧模式那条路径**没有** codec / 码率 / 帧率 / 交付尺寸可用（`MasterJob` 里根本没有这些字段），
  在 CLI 上传了会**明确报错**而不是被忽略。
- `MasterJob.__post_init__` 校验 `width == rig.native_width(source.tile_size)`，
  别的宽度直接拒绝。想要别的尺寸用 `frame --width` 出单张。
- 「不要压缩」指的是**有损**压缩。PNG 的 zlib 等级（`--compress-level`）在任何取值下都是无损的，
  0 到 9 解码出完全相同的像素（`tests/test_master.py` 用 0/1/9 三档断言了这一点）。
  默认 1；要字面意义上「不压缩」就传 0，磁盘约 2.3 倍。

## 11. 尚未确认的事（不要自己替用户决定）

**已确认**：帧率默认 30 fps；交付 MP4，**不带音轨**；
**输出命名与两种模式的边界**（P5，见上）：视频 `<stem>_<月日>_<时分>.mp4`、
帧模式同名目录 + `<stem>_S_<帧号>.png`，帧模式按 input 密度且无损；
**交付尺寸小于母版时按母版拼接、编码时 Lanczos 重采样**（见第 5 节，实测 +2.52 dB；
同一条路径支持「16K 渲染、8K 交付」）；
尺寸 8k = 7680×3840 默认 / 4k = 4096×2048 可选；
码率单位 kbps，每尺寸一套档位并按像素缩放 —— 8k 200/150/100 Mbps、4k 57/43/28 Mbps；
H.264 默认、H.265 可选；**产品形态是 PySide6 GUI + Windows exe，ffmpeg/ffprobe 打进包里，解压即用**；
GPL 许可问题用户明确决定不处理（非商业软件）；
**来源目录可选，不指定时在 exe 同级按结构自动发现**；
**不启动上一代流程、不做备份**（所以放弃逐像素对照，见第 9 节指标 C）。

**2026-09-09 确认，之一：七条收窄范围的**（所以单独列出来）：

| | 确认的事 | 直接后果 |
|---|---|---|
| 1 | **上游不可修改** | 上游那三项搬去 [UPSTREAM.md](UPSTREAM.md)，ROADMAP 不再收录；8-bit 是永久天花板 |
| 2 | 硬编码来源路径**不算风险** | 约束 3 的规则照旧（代码已干净），只是不再当风险跟踪 |
| 3 | **分发包大小不重要** | 体积不再是形态、依赖、GPU 方案的判据 |
| 4 | **目标头显可以播 8K H.264** | 8K + H.264 是可交付默认；`--size 4k` 降为「想要小文件」的选项 |
| 5 | **中断就重跑** | 约束 8 里「可续跑」降级为「已经有了就留着」 |
| 6 | 这台机器的盘**只做测试** | 「`D:` 只剩 131 GB」不再是风险；预检代码留着 |
| 7 | **P8 只要能打开能跑** | 解包与启动耗时不再是判据；**Ctrl-C 验收也已通过** |

**2026-09-09 确认，之二：三条「就这样，不用再问」的**（其中色彩范围在代码里留了后路）：

- **色彩范围：交付 limited（tv），`full` 作为备用路径留着**（用户 2026-09-09 拍板）。
  实测 full 多 **0.34 dB** —— 和「码率 100 → 200 Mbps」买到的 0.39 dB 几乎一样，而且不花比特，
  所以它不是没有吸引力；不选它的理由是**风险不对称**：
  `video_full_range_flag` **规范缺省就是 limited**，于是标记一旦丢了或被忽略，
  文件是 limited 就**猜对了**，文件是 full 就把 0–255 当 16–235 去拉伸、
  **裁掉 16 以下和 235 以上**（暗部糊死、高光烧掉，不可逆）。
  这内容是高饱和红橙岩浆、高光多，最怕后一种。
  **实现上不用做任何事**：`EncodeSpec.full_range=False` 是默认，`True` 那条路径在
  （`-color_range pc` + `out_range=full`，有 `test_range_flag_follows_the_spec` 守着），
  CLI/GUI 故意不暴露开关 —— 要用就加个 flag，几行的事，但**换过去必须在目标头显上验一眼**
  暗部与高光，因为整个风险都在播放侧。
  （**一处更正**：色彩范围**不在用户给的那张规格表里**，那张表只到 Container / Codec /
  FourCC / Profile / 4:2:0 8-bit / B=0 / GOP / 无音轨。limited 是广播惯例与 ffmpeg 默认，
  不是用户的规格要求 —— 之前这里写成「规格与广播惯例」，说重了。）

- **GUI 就按现在这样**（用户 2026-09-09：不重要，现在这样挺好）。
  预览、批量队列、日志导出、记住上次设置 —— **都不做**，不必再问。
  最小可用集 = 选目录 + 选参数 + 跑 + 看进度 + 取消，已经是全部需求。

- **rig 注册表不必为未来预留**（用户 2026-09-09：不重要）。
  只登记 20 路那一套；遇到别的相机数**明确拒绝**而不是猜（第 6 节末）——
  这个行为本身就是正确答案，等真有新装配了再用 `tools/fit_rig.py` 反解一套加进去。

**仍待确认（第 2 条 2026-09-09 已做完，留作记录；第 3、4 条 2026-09-09 新增）**：

1. **播放侧只剩一个问题：那 1.2/255 的偏暗在真机上看不看得出来。**
   设备能力这一半已经答了（上表第 4 条：8K H.264 能播）。剩下的是
   交付文件本身证明干净（第 5 节「4:2:0 的账」），而**播放器自己的 YUV→RGB 可能引入**
   约 1.2/255 的压暗 —— ffmpeg 那条路会（已实测），头显里常见的 GPU shader 路一般不会。
   **只能在真机上看一眼亮度。**
2. ~~**GPU 加速做不做、何时做**~~ **已做**（2026-09-09，第 8 节「GPU 加速」）：`--device cuda`，
   CLI 默认 cpu，GUI 固定 auto；`cuda` 无卡或显存小于 12 GB 时 Warning 回退 CPU，`auto` 静默回退；
   与 CPU 逐字节一致，指标 D 不用改容差。
   发布 exe **不带** cupy（PyInstaller spec 显式 exclude）——GPU 路径是源码运行的开发机用的。
3. **6 相机 rig**：用户要同一程序兼容 20 路与 6 路。**记下，先不做**（2026-09-09）。需要用户提供
   6 路样本与同帧参考全景（或 UE 相机设置）才能反解登记；细节见 ROADMAP「未来可做 — 6 相机 rig」。
4. **各相机帧数不一致时怎么办**：另一台机器上 `D:\CR\0909C` 的 `C_AbyssalGrottos_v004` 被拒
   （1 / 279 / 1363 帧），这是第 6 节的既定行为。报错信息可以更具体（列每个相机的帧数与共有范围），
   是否加显式的「按共有帧处理」开关要用户拍板；两者都未做。
   （同一批数据拷全之后是 `L_Cathedral_v004`，1800 帧齐全；它暴露的是另一个问题——按视口渲染的
   效果让门过不了——P9 已解决，见第 8 节「P9 实测」。）