# AGENTS.md — VR-Compose

给在本仓库工作的 AI agent 与新同事的上下文文件。**先读完本文件再动手。**
路线图与阶段目标见 [ROADMAP.md](ROADMAP.md)。

---

## 1. 项目是什么

把 Unreal Engine 渲染出的 **20 路方形相机序列帧**合成为 **360° 球形全景视频**（equirectangular，等距圆柱投影）。

- 当前只处理**左眼**（文件名前缀 `L_`），输出命名 `MonoEye.*` —— 右眼 / 立体是后续阶段。
- **最终交付物是 MP4**（H.264 默认 / H.265 可选，30 fps 默认，
  输出尺寸 **8k = 7680×3840** 默认 / **4k = 4096×2048** 可选，码率按尺寸取档）。
- **产品形态是带 GUI 的 Windows exe**（PySide6 + PyInstaller）。
  **来源目录可选**；不指定时在 exe 同级按目录结构自动查找。见第 6 节。
  完整规格与实测验证见第 5 节。目标是**画质超过现有参考产物**。
- 参考数据（**只读**）：`E:\22`。这是唯一一份、且正在被上一代流程消耗的生产数据。
- 参考产物质量基线：`E:\22\FinishTaskOutput`，1655 帧已完成。

**代码现状**（详见 [ROADMAP.md](ROADMAP.md) 的「当前进度」）：**P0、P1 已完成**。
`src/vr_compose/` 下有 `projection` / `rig` / `source` / `stitch` / `verify` /
`io` / `cli` 七个模块；`vr-compose frame` 能拼出单帧并自己判定几何是否正确
（指标 A PASS，见第 9 节）。下一步是 P2：序列吞吐 + 直出 MP4。

---

## 2. 硬约束（违反会造成不可恢复的损失）

1. **绝不写入、移动、删除 `E:\22` 下的任何内容。** `Camera*/Tempory/` 是上一代流程的输入队列，
   它会在处理完成后删除源帧 —— 帧 1..1655 已经被消耗掉，**没有备份**。剩余 778 帧是唯一的源数据。
2. **不要启动上一代流程，也不要为它做备份**（用户已明确否决）。
   要验证什么就自己写脚本跑 —— `tools/` 就是干这个的。
   代价是**永远拿不到与参考产物逐像素可比的一对帧**，见第 8 节指标 C。
3. **产品代码里不准出现 `E:\22` 或任何硬编码来源路径。**
   来源目录是参数（CLI `--source` / GUI 选择器），不指定时按目录结构自动发现（第 6 节）。
   `E:\22` 只能作为只读参考出现在文档、测试和 `tools/` 的默认值里。
4. **`multiprocessing.freeze_support()` 必须是 `__main__` 下的第一条语句。**
   PyInstaller 冻结后，进程池靠重新执行 `sys.executable` 派生 worker；缺了这一句，每个 worker
   都会启动一份完整 GUI，而且是递归的。已实测加上之后正常（第 7 节）。
5. 所有产物写到 `E:` 或 `D:` 上的**新目录**，路径由参数显式给出，不设默认写入路径。
6. 磁盘余量：`E:` 978 GB（SATA SSD，读写都在这块盘上）、`D:` 131 GB（NVMe，空间紧）。
   单帧无损输出约 27 MiB；直出 MP4 之后落盘量骤减（每只眼 0.3–2 GB 而非 64 GiB）。
   **跑长任务前先算空间。**
7. 不要一次性把整个序列读进内存。单帧输入 15×1920×1920×3 = 166 MB，输出 7680×3840×3 = 88 MB。
   机器有 128 GB，但必须按帧流式处理，内存占用与总帧数无关。
8. 任务必须**可中断、可续跑、幂等**。MP4 是单一输出文件，靠「文件已存在」判断不了进度，
   所以按 GOP 边界分段 + `-c copy` concat（见 ROADMAP P2）。
9. **8K 的 H.264 必须用 `libx264`（软编）**。`h264_nvenc` 编不了 7680×3840
   （实测 `No capable devices found`，NVENC 的 AVC 上限是 4096×4096）；`libx264` 可以。
10. **`libx264` 的 preset 会悄悄改 profile**：`-preset ultrafast` 输出的是
    **Constrained Baseline**（关了 CABAC 和 8×8 变换），即使显式写了 `-profile:v high` 也一样。
    规格要求 High profile → **preset 必须 medium 或更慢**。只能靠 `ffprobe` 断言发现。
11. **`libx265` 的 auto level 会产出非法等级**：150/200 Mbps 下实测输出 **Level 7.1**，
    而 HEVC 标准最高只定义到 **6.2** —— 没有解码器会接受。
    **等级必须显式算出来**，见 `tools/encode_probe.py: select_level()`。
12. **交付用的 MP4 必须是 I+P only / B=0 / 闭合 GOP**。`x265` 的 open-GOP **默认是开的**，
    必须显式 `open-gop=0`；只写 `-bf 0 -g 60` 不够，还要 `min-keyint` 和 `scenecut=0`。

---

## 3. 相机装配（rig）—— 已从数据中反解并验证

**这是本项目最重要的领域知识。已经解出来了，不需要再做特征匹配或标定。**

反解方法：把每个 tile 投影到成品 equirect 上做 NCC 搜索（yaw/pitch/roll/FOV），
再用理论标称值重新拼接一帧、测量重叠区一致性。结论：

| 项 | 值 |
|---|---|
| 相机模型 | 理想针孔（pinhole），无畸变 |
| FOV | **水平 = 垂直 = 90.0°**（正方形画幅，`tan(FOV/2) = 1`，等价于 cubemap 的一个面） |
| roll | 0 |
| 节点 | 15 路**共节点**（同一光心），重叠区无视差 |
| 方位角 yaw | 5 个扇区：0°, 72°, 144°, 216°, 288° |
| 仰角 elevation | 每扇区 3 档：**+45°（朝上）, 0°（水平）, −45°（朝下）** |
| 唯一视角数 | **15**（不是 20） |

### 文件序号 → 视角

设 `i` 为 `CameraN` 的 N（1..20），`s = (i-1) // 4`（扇区 0..4），`slot = (i-1) % 4`：

| slot | yaw | elevation | 说明 |
|---|---|---|---|
| 0 | `72·s` | **+45°** | 朝上 |
| 1 | `72·s` | **−45°** | 朝下 |
| 2 | `72·s` | **0°** | 水平 |
| 3 | `72·(s+1) mod 360` | **+45°** | **下一扇区 slot 0 的重复渲染** |

因此存在 5 组**逐像素完全相同**的文件：

```
Camera4 ≡ Camera5    Camera8 ≡ Camera9    Camera12 ≡ Camera13
Camera16 ≡ Camera17  Camera20 ≡ Camera1
```

（帧 0000 上逐字节像素相同；后续帧因渲染噪声/TAA 最大差 1 灰阶。）

**结论：每帧只需读 15 个文件，`Camera4/8/12/16/20` 整目录可跳过 —— 直接省掉 25% 的读 I/O。**

### 坐标与投影约定（与成品完全一致，已验证无翻转、无全局 yaw 偏移）

世界系右手、Z 轴向上。方向向量由经纬度给出：

```
d(lon, lat) = (cos(lat)·cos(lon),  cos(lat)·sin(lon),  sin(lat))
```

Equirect 采样（W=7680, H=3840）：

```
u = (lon / (2π) + 0.5) · W        lon ∈ [-π, π)，图像最左 = -π
v = (0.5 - lat / π)   · H        lat = +π/2 在图像最上
```

Tile 像素 → 方向（N = 1920，`t = tan(45°) = 1`）：

```
a = 2·(px + 0.5)/N - 1           # 列，左 -1 → 右 +1
b = 2·(py + 0.5)/N - 1           # 行，上 -1 → 下 +1
d_cam   = normalize(1, a·t, -b·t)
d_world = Rz(yaw) · Ry(-elevation) · d_cam

Rz(θ) = [[cosθ, -sinθ, 0], [sinθ, cosθ, 0], [0, 0, 1]]
Ry(θ) = [[cosθ, 0, sinθ], [0, 1, 0], [-sinθ, 0, cosθ]]
```

注意 `Ry(-elevation)` 的负号：elevation 为正表示**朝上**。这个符号搞错的话画面会上下颠倒地拼合，
而且因为球面是对称的，低分辨率预览不一定看得出来 —— 一定用第 6 节的重叠一致性指标验证。

### 覆盖与采样密度（已实测）

- 球面覆盖 **100%**，无空洞；加权平均每个方向被 **2.50** 个 tile 看到，最大 **4** 个。
  按立体角加权的覆盖分布：1 个 tile 仅 **0.03%**（tile 边界的数值边缘）、2 个 **61.2%**、
  3 个 **27.5%**、4 个 **11.3%**。极区（|lat|>85°）实测覆盖 2–3 个。
- 分辨率是**精确匹配**的，不是巧合：`1920 px / 90° = 7680 px / 360° = 21.33 px/度`。
  输出宽度 7680 就是 tile 中心的像素密度，别随意改。
- 但针孔投影的角分辨率按 `cos³(离轴角)` 衰减。取每个方向上**最好的那个 tile**，
  相对中心的采样密度：median 0.78、p5 0.43、**min 0.355**。
  最差区域是**天顶与天底附近（约 0.37）**，即比输出网格欠采样约 2.7×。
  → 极区是画质软肋，任何锐化/降噪/重采样滤波的评估都必须单独看极区。

---

## 4. 数据布局

### 输入（只读）

```
E:\22\Camera{1..20}\Tempory\L_Cathedral.{frame:04d}.png
```

- 1920×1920，**8-bit RGBA**，PNG，单文件 3.6–5.5 MiB（平均 4.6 MiB，随画面复杂度变化）。
- **alpha 通道全为 255，没有信息** —— 25% 的字节是浪费的，读进来直接丢掉 alpha。
- 帧号现状：每个相机目录有 **779** 个文件 = 帧 `0000` + 帧 `1656..2433`。
  帧 `1..1655` 已被上一代流程消耗删除。`0000` 是留在那里的参考帧，**不在 1..2433 的正片序列里**。
- 序列总长 **2433** 帧。fps 未确认（见第 8 节）。
- PNG 里的 XMP 只有 SkyGuard DLP 水印，`Reason="UnrealEditor.exe"` —— 证明来源是 UE，
  **没有**相机内外参、帧率、色彩空间等有用元数据。别指望从 PNG 元数据里读配置。

### 参考输出（只读，质量基线）

```
E:\22\FinishTaskOutput\MonoEye.L_Cathedral.{frame:04d}.png
```

- 7680×3840（2:1）、8-bit RGBA、约 28 MiB/帧，帧 `0001..1655` 连续无缺口，共 43.41 GiB。
- 左右边界环绕连续（首末列平均差 1.15/255）。
- alpha 为 253..255（96.7% 是 255）—— 说明上一代流程做的是 alpha 加权混合，且有取整损失。
  **我们的输出应当输出不带 alpha 的 RGB，或者 alpha 恒为 255。**

### 待办工作量

| | 帧数 | 状态 |
|---|---|---|
| 左眼 1..1655 | 1655 | 已完成（参考产物） |
| **左眼 1656..2433** | **778** | **待处理，源数据在手** |
| 右眼 1..2433 | 2433 | 源数据尚未渲染 |

---

## 5. 交付规格（已确认 + 实测验证）

最终交付物是 **MP4**。以下参数已由用户确认，编码器行为已在本机实测。

### 用户给定的规格

| 参数 | H.264 | H.265 |
|---|---|---|
| Container | MP4 | MP4 |
| Codec | AVC / H.264（**默认**） | HEVC / H.265（可选） |
| FourCC | `avc1` | `hvc1` 优先 |
| Profile | High | Main |
| Pixel Format / Chroma / Bit Depth | `yuv420p` / 4:2:0 / 8-bit | 同左 |
| Frame Structure | **I + P only，B=0** | 同左 |
| GOP | 2 秒（30 fps → 60，60 fps → 120），**Open GOP 关闭** | 同左 |
| Audio | ~~AAC-LC 48 kHz~~ → **不带音轨** | 同左 |

- 输出尺寸：默认 **8k = 7680×3840**，可选 **4k = 4096×2048**（都是 2:1 等距圆柱）。
  4k 由 8k 母版 Lanczos 降采样得到。
  （备注：`3840×1920` 是母版的整数 2× 降采样，重采样更干净；`4096×2048` 多 14% 像素、
  且 4096 是多数解码器的宽度上限。按用户指定用 4096×2048。）
- 帧率：默认 **30 fps**，可选。2433 帧 @30fps = **81.1 s**。
- 码率上限：单位是 **kbps（已确认）**。**每种尺寸一套档位，按像素数缩放（已确认）**：

  | 档位 | 8k 7680×3840 | 4k 4096×2048 | bit/像素 |
  |---|---|---|---|
  | 高（默认） | **200 Mbps** | **57 Mbps** | 0.226 |
  | 中 | **150 Mbps** | **43 Mbps** | 0.170 |
  | 低 | **100 Mbps** | **28 Mbps** | 0.111 |

  缩放系数是像素比 `(7680×3840)/(4096×2048) = 3.515625`，即**保持 bit/像素恒定**
  （实测两列对齐：8k 0.226/0.170/0.113 对 4k 0.226/0.171/0.111）。
  81.1 s 成品：8k 2.03/1.52/1.01 GB，4k 0.59/0.46/0.30 GB。
  代价与收益见下面的「4K 码率缩放的实测代价」。

- **不带音轨**（已确认）。源头是渲染帧，本来没有声音；规格表里的 AAC-LC 48 kHz 一行按此作废。
  编码时显式 `-an`，**不要**为了「形式上符合规格」去生成静音轨。

### 实测：可用的编码路径

| 编码器 | 7680×3840 | 4096×2048 | 速度 (8K) | 2433 帧耗时 |
|---|---|---|---|---|
| `libx264` | ✅ | ✅ | 4.59 fps | **8.8 min** |
| `libx265` | ✅ | ✅ | 3.76 fps | **10.8 min** |
| `hevc_nvenc` | ✅ | ✅ | 很快 | — |
| `h264_nvenc` | ❌ `No capable devices found` | ✅ | — | — |

**H.264 是能输出 7680×3840 的** —— 用 `libx264` 软编，实测通过，High / Level 6.2 / `avc1` / B=0，
全长只要 8.8 分钟。编不了 8K 的只有硬件编码器 `h264_nvenc`。
**真正的限制在解码端**：消费级硬件 H.264 解码器普遍卡在 4096×4096，8K AVC 只能软解
（PC 播放器可以，头显不行）。这正是 `--size 4k` 存在的意义。

**结论：编码不是瓶颈。**8K 全长编码只要 ~10 分钟，相比 P2 的拼接耗时可以忽略。
更重要的是 —— **拼接结果直接管道进 ffmpeg，就完全省掉了 0.68–2.77 s/帧 的 PNG 编码**，
而这正是第 8 节里识别出的吞吐瓶颈。

### level / tier：由规范表算出最小等级，不查表也不交给编码器

等级不是查表钉死的，而是由 `tools/encode_probe.py: select_level()` 按
H.264 Table A-1 与 HEVC Tables A.6/A.9 算出**最小合规等级**：画面尺寸、采样率、
码率三者各给一个下限，取最大。等级越低对解码器的要求越低，兼容性越好。
HEVC 优先 Main tier，只有 Main tier 无解时才退到 High tier。

**绝不能把等级交给编码器决定**（第 2 节约束 10）。

30 fps 下的结果（12 种组合已逐项 `ffprobe` 实测通过，
profile / level / FourCC / `yuv420p` / **B 帧数 = 0** / I 帧间隔 = 60 全部符合）：

| size | codec | 码率 | profile | level | tier | FourCC |
|---|---|---|---|---|---|---|
| 8k 7680×3840 | H.264 | 200 / 150 / 100 Mbps | High | **6.0** | — | `avc1` |
| 8k 7680×3840 | H.265 | 200 / 150 Mbps | Main | **6.2** | **Main** | `hvc1` |
| 8k 7680×3840 | H.265 | 100 Mbps | Main | **6.1** | **Main** | `hvc1` |
| 4k 4096×2048 | H.264 | 57 / 43 / 28 Mbps | High | **5.1** | — | `avc1` |
| 4k 4096×2048 | H.265 | 57 / 43 Mbps | Main | **5.2** | **Main** | `hvc1` |
| 4k 4096×2048 | H.265 | 28 Mbps | Main | **5.1** | **Main** | `hvc1` |

60 fps 下采样率上限抬高一档：8k H.264 → 6.1，4k H.264 → 5.2，H.265 各档不变。

**4K 码率缩放顺带解决了一个兼容性问题**：原来的 200/150 Mbps 会把 4K H.265 逼到
Main tier L6.2（给一个 4K 文件打 6.2 的标记会拖低兼容性），只能退到 High tier L5.2；
缩放到 57/43/28 Mbps 之后**全部落回 Main tier L5.1/L5.2** —— 这是 4K 解码器最普遍支持的档。
4K H.264 也从 L5.2 降到 L5.1，8K H.264 从 6.2 降到 6.0。

关键的规范数字：

- **H.264 MaxFS**：7680×3840 = 115,200 个宏块，超过 Level 5.2 的 36,864 → 8K 必须 6.0+。
  4096×2048 = 32,768 ≤ 36,864 → 4K 用 5.1 就够（30 fps 时 MaxMBPS 刚好等于上限 983,040）。
- **HEVC Main tier MaxBR**：L5.1 = 40、L5.2 = 60、L6.0 = 60、L6.1 = 120、L6.2 = 240 Mbps。
- **HEVC MaxLumaPs**：L6.2 上限 35.65 Mpx。7680×3840 = 29.49 Mpx 可以；
  立体 over-under 7680×7680 = 58.98 Mpx **编不出来**，x265 直接报
  `picture dimensions are out of range for specified level`。实测 5760×5760（33.2 Mpx）可以。
  → 立体方案见 ROADMAP P5。
- **`hevc_nvenc` 的坑**：显式 `-level 6.1/6.2` 会被拒（`Invalid Level`），除非加 `-tier high`；
  auto 模式静默输出 Main tier L6.1 —— 200 Mbps 下不合规。
- **`h264_nvenc` 的坑**：4K + `-level 5.2` + 200 Mbps 会被拒；auto 给 L6.1。
  → **要规格精确可控就用 `libx264` / `libx265` 软编**，编码时间本来就可以忽略。

### 实测：验证过的 ffmpeg 参数

```
# H.265（推荐；Main profile / Main tier / L6.2，三档码率通用）
-c:v libx265 -preset medium -profile:v main -pix_fmt yuv420p
-colorspace bt709 -color_primaries bt709 -color_trc bt709 -color_range tv
-b:v 200000k -maxrate 200000k -bufsize 400000k
-x265-params bframes=0:open-gop=0:keyint=60:min-keyint=60:scenecut=0:level-idc=6.2:high-tier=0
-tag:v hvc1 -movflags +faststart

# H.264（同样的 GOP 结构；注意 8K 不可硬解）
-c:v libx264 -preset medium -profile:v high -level:v 6.2 -pix_fmt yuv420p
-colorspace bt709 -color_primaries bt709 -color_trc bt709 -color_range tv
-b:v 200000k -maxrate 200000k -bufsize 400000k
-x264-params bframes=0:open-gop=0:keyint=60:min-keyint=60:scenecut=0
-tag:v avc1 -movflags +faststart
```

4K 档在上面基础上加 `-vf scale=4096:2048:flags=lanczos`，码率换成 4k 档位，等级随之降低：

```
# 4k H.264，57/43/28 Mbps：level 5.1（30 fps；60 fps 要 5.2）
-level:v 5.1

# 4k H.265，57/43 Mbps：Main tier L5.2；28 Mbps：Main tier L5.1
-x265-params ...:level-idc=5.2:high-tier=0     # 57k / 43k
-x265-params ...:level-idc=5.1:high-tier=0     # 28k
```

所有档位都要加 **`-an`**（不带音轨）。

`keyint` / `min-keyint` 随帧率走：30 fps → 60，60 fps → 120。
**`-preset` 不能用 `ultrafast`** —— 它会把 H.264 降成 Constrained Baseline（第 2 节约束 10）。

### 实测：码率-质量曲线（真实 8K 母版 90 帧，PSNR 对无损 PNG）

| 配置 | 实际码率 | PSNR | MAE | p99.9 \|误差\| |
|---|---|---|---|---|
| H.265 200k | 208.6 Mbps | **43.06 dB** | 1.43 | 7.8 |
| H.265 150k | 156.5 Mbps | 42.91 dB | 1.45 | 8.0 |
| H.265 100k | 103.6 Mbps | 42.67 dB | 1.48 | 8.2 |
| H.264 200k | 197.6 Mbps | 42.98 dB | 1.43 | 8.0 |
| H.264 100k | 90.2 Mbps | 42.21 dB | 1.52 | 10.0 |

### 实测：4K 码率缩放的实测代价

4096×2048，H.265，参照物是同样 Lanczos 降采样的无损母版（90 帧）：

| 目标码率 | 实际 | bit/像素 | PSNR | 81.1 s 成品 | |
|---|---|---|---|---|---|
| 200 Mbps | 207.0 | 0.795 | 42.64 dB | 2099 MB | 缩放前 |
| 150 Mbps | 154.8 | 0.596 | 42.55 dB | 1569 MB | 缩放前 |
| 100 Mbps | 102.8 | 0.397 | 42.24 dB | 1042 MB | 缩放前 |
| **57 Mbps** | 58.2 | **0.226** | **41.97 dB** | **590 MB** | **缩放后（高）** |
| **43 Mbps** | 44.9 | **0.171** | **41.78 dB** | **455 MB** | **缩放后（中）** |
| **28 Mbps** | 29.3 | **0.111** | **41.35 dB** | **297 MB** | **缩放后（低）** |

**缩放的代价是 0.67–0.89 dB，换来文件小 72%，并且把 4K H.265 从 High tier 拉回 Main tier。**
（200→57 Mbps：−0.67 dB / −72%；150→43：−0.77 dB / −71%；100→28：−0.89 dB / −71%。）

**100 → 200 Mbps 只换来 0.39 dB（8K）/ 0.33 dB（4K）。码率不是画质瓶颈。**
4K 档尤其明显：200 Mbps 在 4096×2048 上是 **0.795 bit/像素**，
比 25 Mbps 也只好 1.55 dB —— 三档码率是按 8K 定的，套到 4K 上全都远过了拐点。
如果 4K 版是为了兼容性和体积，码率应该按像素数缩放（见第 9 节问题 3）。
H.264 与 H.265 在 200 Mbps 上基本打平（差 0.08 dB），只在 100 Mbps 上才拉开 0.46 dB。
→ 选 H.265 的真正理由是**能被硬件解码**，不是画质。

### 实测：真正的画质瓶颈是 4:2:0

同样 200 Mbps、同样 x265，只改色度格式：

| 变体 | PSNR | 平均直流偏移 |
|---|---|---|
| 4:2:0 limited range（规格要求） | 43.21 dB | **−1.23** |
| 4:2:0 limited + `+accurate_rnd+full_chroma_int` | 43.21 dB（**无改善**） | −1.22 |
| 4:2:0 full range（`-color_range pc`） | 43.55 dB（+0.34） | −1.16 |
| **4:4:4**（off-spec，仅诊断） | **47.54 dB** | **+0.05** |

- **4:2:0 色度下采样一项就吃掉约 4.3 dB**，并带来系统性的 **−1.2/255 整体压暗**；
  在 200 Mbps 下它占总 MSE 的 **46%**。压缩量化本身几乎是透明的。
- 本内容是高饱和的红/橙岩浆，色度细节密集，所以 4:2:0 的代价格外大。
- **ffmpeg 现成的开关救不回来**：swscale 的精确舍入标志毫无变化；
  `zscale` 需要精确的输入色彩标记，我试的两种链路反而更差（28 dB）。
  → 这 4.3 dB 是一个**已量化的机会**，但不是一个现成开关。我们的管线里本来就是 float32 RGB，
  在线性光下做色度下采样有希望吃回一部分，但必须实测验证，不能假定有效。见 ROADMAP P3/P5。
- 按纬度分带看 PSNR（H.265 200k）：**极区 43.78 > 中纬 43.16 > 赤道 42.25**。
  极区压缩反而更「好」，因为它本来就是欠采样的糊图。
  **这说明极区问题在上游采样，不在编码器。**

### 球面元数据

- **ffmpeg 无法写入** spherical/VR 元数据（只有解码侧的 side data）；PyPI 上也没有可用包
  （`spatialmedia` / `spatial-media` 都不存在）。
- 方案：自己写 `sv3d` / `st3d` box（Spherical Video V2 规范很短且稳定，无外部依赖、可审计），
  或安装 GPAC（`winget install GPAC.GPAC`，实测源里有 26.07）做交叉验证。

### 音频

**不带音轨**（已确认）。显式 `-an`。

---

## 6. 来源目录：可选，且按结构自动发现

来源目录是**参数**，不是常量。`E:\22` 只是「一种符合约定的目录」的样本，
不是唯一形态 —— 后续项目的相机数、内层目录名、文件 stem 都可能不同。

### 识别的结构

```
<来源目录>/
  Camera1/[<任意一层子目录>/]<stem>.<帧号>.png
  Camera2/[...]
  ...
  CameraN/[...]
  FinishTaskOutput/            (可选；也接受 output / out)
```

**全部靠推断，不靠硬编码**（`tools/discover_source.py`，7 项合成用例 + 真实数据已验证）：

| 项 | 怎么来的 |
|---|---|
| 相机数 | 匹配 `Camera<数字>` 的目录个数（大小写不敏感，允许 `Camera_1` / `Camera 1`） |
| 内层目录 | 往下最多找 1 层，谁装着 `<stem>.<数字>.png` 就是它。`Tempory` 这个拼写不作假设 |
| 文件 stem | 从文件名解析。**不同 stem 算不同的集合**，所以 `L_` / `R_` 天然分成两只眼 |
| 帧范围 | 各相机帧号取交集（只有全都有的帧才可处理） |
| tile 尺寸 | 读 PNG 的 IHDR，不解码 |

### 必须报错而不是猜的情况

- 相机编号不连续（不是 `1..N`）
- 各相机 tile 分辨率不一致
- **tile 不是正方形** —— 反解出的 rig 假设方形 90° FOV，长方形说明是另一套装配
- 各相机帧数不一致（不要偷偷取交集就往下跑）
- 同一目录里发现多个集合（多只眼 / 多个场景）→ 必须由调用方选，**绝不处理「第一个匹配」**

### 没给来源目录时去哪找

按顺序：exe 自身所在目录 → 它的各个直接子目录 → 上一级目录的各个直接子目录。
第一个能通过校验的就用。冻结后 `sys.executable` 是 exe 本身，所以「exe 同级」就是它的父目录；
从源码跑时等价于工程根目录。

找不到就**明确报错并列出搜过的位置**，不要静默退化成某个默认路径。

### rig 与来源目录的关系（重要）

第 3 节那套 rig（5 扇区 × 3 仰角 / 90° 方形 FOV）是**针对这一套 20 路装配反解出来的**。
换一个来源目录、相机数不是 20，那套映射就不成立。所以：

- rig 必须做成**配置**（按相机数/布局索引的注册表），随 exe 一起发布；
- 遇到未登记的布局要**明确拒绝**，并提示可以用 `tools/fit_rig.py` 配合一份参考全景去反解；
- 现在注册表里只有一条：20 路 → 第 3 节的映射。

---

## 7. GUI 与打包（已实测）

PySide6 + PyInstaller，Windows exe。以下数字来自 `tools/package_probe.py`
（真打了三种配置，跑起来验证过）。

### 打包方式选型

| 配置 | 分发体积 | 冷启动 | **热启动** | 构建 |
|---|---|---|---|---|
| onedir | **158 MB**（整个目录） | 8–11 s | **0.86 s** | 74–100 s |
| onefile | **64 MB**（单文件） | 12–14 s | **5.88 s** | 84 s |

**结论：发 onedir。** onefile 省下的 94 MB 下载量，代价是**每次启动都多花 5 秒**
（它每次都要把整个归档解到临时目录）。热启动 0.86 s 对 5.88 s，差 7 倍。
冷启动那 8–14 s 主要是首次杀毒扫描，不代表日常体验。

### 两个和直觉相反的实测结果

1. **不需要手写 Qt 模块排除列表。** PySide6 装完 632 MB，但 PyInstaller 的 PySide6 hook
   已经只打包实际 import 到的模块 —— 加了 47 条 `--exclude-module` 之后体积**一点没变**
   （158 MB → 158 MB）。别在这上面浪费时间。
2. **冻结后的进程池是可用的**，前提是第 2 节约束 4 那句 `freeze_support()`。
   三种配置都实测跑通了 `ProcessPoolExecutor`，没有递归重启 GUI。

### ffmpeg 随 exe 分发（已确认）

**ffmpeg 与 ffprobe 都打进分发包**，放在 exe 同级。目标是**解压即用，不需要用户装任何东西**。

| 组成 | 体积 |
|---|---|
| onedir 应用本体 | 158 MB |
| `ffmpeg.exe`（静态全功能构建） | 185 MB |
| `ffprobe.exe` | 185 MB |
| **合计** | **约 528 MB** |

查找顺序：**exe 同级的 `ffmpeg.exe` 优先，其次 PATH。** 这样默认自带，
但需要时用户仍可以换成自己的构建。启动时检查可用性与必需编码器
（`libx264` / `libx265`），缺了立刻给明确提示，而不是跑到一半失败。

`ffprobe` 也带上：它是交付合规校验（P4 的 24 项断言）用的，不带的话产品自己没法自检。

**许可**：带 libx264 / libx265 的 ffmpeg 构建是 GPL。此事已提出，
**用户明确决定不处理 —— 这不是商业软件**。记录在此，不再作为待确认项。

### GUI 架构要求

- **GUI 只能是薄壳。** 它必须调用 CLI 走的同一套代码，不允许出现第二份逻辑。
  先有可用的 CLI，GUI 包在外面。
- **长任务绝不能跑在 GUI 线程上。** 推荐把整个作业放到**独立子进程**里跑，
  GUI 通过管道读进度 —— 而不是只开个 QThread。这样既隔离崩溃，也避开
  Qt 与 multiprocessing 互相干扰的问题（P2 本来就有进程池和 ffmpeg 子进程）。
- 进度要显示：当前帧 / 总帧、s/帧、ETA，并与第 8 节的 46.85 s/帧 基线可比。
- 必须能取消，且取消后作业保持可续跑（第 2 节约束 8）。

---

## 8. 性能基线（同机实测，是所有优化的比较基准）

硬件：AMD Ryzen 9 7950X（16C/32T）、128 GB RAM、RTX 4090、
`E:` = Samsung 870 EVO SATA SSD、`D:` = Kingston NVMe。

**上一代流程：`46.85 s/帧`**（1655 帧耗时 21.54 小时墙钟，由输出文件 mtime 跨度算出）。
按这个速度，剩余 778 帧还要 ~10 小时，双眼全长要 ~63 小时。

单线程 I/O 与计算成本实测：

| 操作 | 耗时 |
|---|---|
| 解码 1 个 1920×1920 tile | 54 ms（15 个 = 0.81 s） |
| 解码 1 个 7680×3840 PNG | 0.42 s |
| 编码 7680×3840 PNG，compress_level=6 | 2.77 s |
| 编码 7680×3840 PNG，compress_level=1 | 0.68 s |
| numpy 最近邻重投影 15 tile → 3840×1920 | 4.7 s（含每帧重新算射线，可预计算掉） |
| 单帧磁盘吞吐 | 读 68 MiB（15 路）+ 写 27 MiB ≈ **95 MiB**（SATA SSD 上约 0.2 s） |

**判断：上一代流程比 I/O 下界慢 15–25 倍，瓶颈不在数据量上。** 重投影是**固定几何**，
可以完全预计算成一张静态 LUT（或者一次性传到 GPU），每帧只剩「解码 → gather+blend → 编码」。

**而且既然交付物是 MP4，中间那层 PNG 编码（0.68–2.77 s/帧）应该整个消失** ——
拼接结果以 rawvideo 直接管道进 ffmpeg。这样单帧成本只剩「15 个 tile 解码（可并行）+ LUT gather」，
而 8K 编码全长只要 ~10 分钟（第 5 节实测）。无损母版 PNG 序列改为**按需产出**（QA 抽帧、归档），
不再是必经环节 —— 顺带也省掉每只眼 64 GiB 的落盘。

---

## 9. 怎么证明一次改动是对的

不要靠肉眼看缩略图判断拼接正确 —— 球面对称性会掩盖上下颠倒、扇区错位这类错误。用下面两个指标：

**指标 A：重叠区自一致性（首选，不需要参考产物，也不受场景运动影响）**
同一帧的 15 个 tile 重投影到球面后，在重叠方向上互相之间的亮度差。
由 `vr_compose.verify.agreement()` 计算，`vr-compose frame` 直接打印并给 PASS/FAIL。

rig 正确时（标称角度、最近邻采样、BT.709 亮度）：

| 分辨率 | median | mean | p95 | std>8 占比 |
|---|---|---|---|---|
| 3840×1920 | **0.68** | **1.02** | **3.10** | **0.25%** |
| 7680×3840 | 0.67 | 1.02 | 3.10 | 0.25% |

门限是 `median ≤ 1.0 且 mean ≤ 1.5`（`vr_compose.verify.GATE_MAX_*`）。
任何改动如果让 median 明显高于 1，几何就是错的。

> 早期探查脚本记录的是 0.70 / 1.04 / 3.14 / 0.26%，那是用 BT.601 亮度权重算的。
> 产品代码改用 BT.709（与交付规格标记的色彩空间一致），所以数字略低。
> 这是同一个量的两种算法，不是画质变化。

**指标 B：与参考产物比对**
参考帧只有 1..1655，而在手的源帧是 1656..2433，**没有同帧可比**。
只能拿源帧 1656 对参考帧 1655（相邻帧），此时 `PSNR ≈ 26 dB`，差值主要来自一帧的场景运动，
**不能当精度指标用**；它只能用来确认没有整体翻转/偏移
（最佳环绕位移 ≤1 px / 0.5°，零位移 NCC = 0.964；上下翻转 0.08、左右镜像 0.30，差距足够判别）。

**指标 C：与参考产物的对照 —— 逐像素对比做不到，接受这个限制**

参考产物覆盖帧 1..1655，而它们的源 tile **已被删除**；我们手上的源 tile 是 1656..2433，
参考产物在这个区间没有输出。**两者永远没有同一帧。**

唯一能造出可比一对的办法是让上一代流程再处理 10 帧（并事先备份源 tile），
**用户已明确否决**（第 2 节约束 2）。所以「超过参考产物」只能用无参考的方式论证：

1. **无参考指标**（都在我们自己的母版上算，不需要对照）：
   - 重叠一致性（指标 A）—— 几何正确性；
   - 接缝梯度连续性 —— 等距抽 20 个 tile 边界位置做不连续性检查；
   - **极区细节保留度** —— 拿重投影结果对源 tile 的对应区域算，衡量重采样丢了多少；
   - 时域稳定性 —— 连续 10 帧的极区方差，不高于源 tile 自身的时域方差。
2. **相邻帧视觉 A/B**：我们的 1656 对参考的 1655，在镜头运动小的区域（岩石等静态几何）
   做局部目视与局部指标对比。**这是定性的，不要当成定量结论。**
3. **可复现的方法学论证**：极区最好 tile 的采样密度只有中心的 0.355（第 3 节实测），
   而参考产物用的滤波未知；我们的多 tile 联合重建（ROADMAP P3）在原理上能利用
   2–3 个 tile 的亚像素错位，这一点可以在**自己的两个版本之间**做 A/B 量化
   （最近邻 vs EWA vs 多 tile），从而证明「我们做到了什么」，
   而不是「我们比那个不可测量的东西好」。

**汇报时要如实说明这个限制**，不要把无参考指标包装成对参考产物的胜出。

跑 `python tools/stitch_probe.py` 就会打印指标 A/B 并按 `median<=1.0 且 mean<=1.5` 给出 PASS/FAIL。

**回归测试写法**：把上面这些数字作为断言写进 pytest，用单帧（如 1656）跑，标 `@pytest.mark.slow`，
因为它需要访问 `E:\22`。纯数学部分（射线生成、旋转矩阵、equirect 映射、序号→视角的映射表）
必须有不依赖磁盘的单元测试。

---

## 10. 工程约定

Python **3.13**，src 布局，包名 `vr_compose`（分发名 `vr-compose`）。
系统的 `python` 是 3.9，**一律走 venv**，不要用裸 `python`。

```bash
py -3.13 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

```bash
.\.venv\Scripts\python.exe -m pytest ; .\.venv\Scripts\python.exe -m ruff check . ; .\.venv\Scripts\python.exe -m mypy
```

- `mypy` 是 **strict + warn_unreachable**，`ruff` line-length 100。提交前三样都要过。
- Python 依赖：`numpy>=2.1`、`pillow>=11.0`（已声明在 `pyproject.toml`）。
- **外部依赖：ffmpeg**（本机 8.0-full，带 libx264 / libx265 / hevc_nvenc / aac，已验证）。
  以 subprocess 调用，不引 Python 绑定。启动时检查可执行文件与必需编码器是否存在。
- `mypy` 覆盖 `src` / `tests` / `tools` 三者，`ruff` 覆盖全仓库。
- **`tools/` 里是本文档所有数字的复现脚本**，见 [tools/README.md](tools/README.md)。
  改动几何、滤波或编码参数之后跑一遍，数字漂移了就说明有事发生。
  `tools/_rig.py` 是 rig 定义的**临时住所**，P1 搬进 `src/vr_compose/rig.py`。
- 依赖在 `pyproject.toml` 里显式声明；新增重依赖（torch / cupy / opencv / ffmpeg 绑定）
  先在 ROADMAP 对应阶段说明理由，别顺手加。
- 长任务用 CLI 子命令暴露，参数显式，不要在代码里硬编码 `E:\22` 之外的路径；
  `E:\22` 作为只读参考路径可以出现在测试与文档里。
- 日志用 `logging`，进度信息带上帧号与速率（s/帧），方便和第 5 节的基线对比。

## 11. 尚未确认的事（不要自己替用户决定）

**已确认**：帧率默认 30 fps；交付 MP4，**不带音轨**；
尺寸 8k = 7680×3840 默认 / 4k = 4096×2048 可选；
码率单位 kbps，每尺寸一套档位并按像素缩放 —— 8k 200/150/100 Mbps、4k 57/43/28 Mbps；
H.264 默认、H.265 可选；**产品形态是 PySide6 GUI + Windows exe，ffmpeg/ffprobe 打进包里，解压即用**；
GPL 许可问题用户明确决定不处理（非商业软件）；
**来源目录可选，不指定时在 exe 同级按结构自动发现**；
**不启动上一代流程、不做备份**（所以放弃逐像素对照，见第 9 节指标 C）。

**仍待确认**：

1. **色彩范围**：limited（tv）还是 full（pc）？full 实测多 0.34 dB，但依赖播放器正确读标记。
   建议 limited，更稳。
2. **立体**：右眼何时渲染？over-under 7680×7680 在 HEVC 里编不出来（第 5 节），
   需要在「两个独立文件」和「降到 5760×5760 以内」之间选。
3. **目标播放设备型号**（头显 / 播放器）—— 决定最终验收怎么做。
4. **后续项目的装配会不会变**（相机数、yaw/elevation 组合）。
   来源目录既然可选，rig 就必须做成注册表（第 6 节末）；现在只登记了 20 路这一条，
   遇到别的布局会明确拒绝。需要知道预期还会出现哪些布局。
5. **源头能不能改**：UE 侧直接输出 EXR/16-bit、去掉无用 alpha、不再重复渲染那 5 路重复视角？
   这三项能同时提升画质上限并减少 25% I/O，性价比远高于下游优化。
6. **GUI 的具体需求**：除了「选目录 + 选参数 + 跑 + 看进度」，是否需要预览、
   批量队列、日志导出、上次设置记忆等。现在按最小可用集设计。
